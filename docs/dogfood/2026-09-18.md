# Dogfood 2026-09-18 — the loop restarted after a weekly limit: twenty-one landings, four phases closed by script, and a required gate that printed pass on a red it had not earned

<!-- Not a retroactive report. The pass ran after this format landed, so no
     `Retroactive:` bullet and no `not recorded` cell: every number below is
     measured, and each states the command or the file it comes from at the
     point it is derived.

     What the pass was: the repository's own loop — `claim.mts`, a worktree per
     issue, an implementer, CI (test, scope, negative control), an isolated
     read-only reviewer returning a verdict, the orchestrator applying the
     label, `land.mts` — resumed after the account-wide weekly limit that ended
     the 2026-09-17 pass, and run until midnight UTC. It merged twenty-one pull
     requests, closed four phases through `scripts/close-milestone.mts` for the
     first time, and migrated the eight open milestone descriptions into the
     format that script can parse.

     The window the numbers measure: 2026-09-18T16:28:56.809Z to
     2026-09-18T23:48:56.220Z, the first and last timestamp of the orchestrating
     session's transcript on that date. The open is not an arbitrary cut: the
     session's first event of the day is the resumed turn that was told it had
     hit the weekly limit, and the first working turn follows it nineteen
     seconds later, so the window includes no idle block waiting for a reset.
     The close is the last event the session recorded on that date, twelve
     minutes before the window of `docs/dogfood/2026-09-19.md` opens at
     00:00:55.039Z, so the two passes very nearly partition the calendar
     boundary between them and nothing is double-counted.

     ONE orchestrating session, and that was checked rather than assumed. The
     2026-09-19 pass had two; this one had one. The workspace's projects
     directory holds three session transcripts, and on 2026-09-18 only
     `453d878d-7f6a-45b0-8943-85b67e30bf6e` carries any event at all:
     `88efcdba-112f-4f8c-bad9-58b289d28a81` opens at 2026-09-19T01:11:58.556Z
     and `ccdca43a-5ba8-49ff-b5c3-4f54524e5f5d` was last written on 2026-09-15.
     So the populations of this report's `Turns` and the 2026-09-17 report's are
     the same kind — one orchestrator — and the 2026-09-19 report's 631 is the
     one that spans two.

     The derivation method was validated against both published passes before it
     was used here, the way the 2026-09-19 implementer validated its own.
     Against the 2026-09-17 report's stated bounds it answers 775 turns, 954
     minutes and 660 orchestrator tool calls — that report's three numbers
     exactly. Against the 2026-09-19 report's stated bounds it answers 512 plus
     119 = 631 turns, 1024 minutes, 538 tool calls and US$574.98, with every
     per-model token total in that report's cost note reproduced to the token.
     The one difference, stated rather than tuned away: run against 2026-09-17
     the cost script answers $743.16 where that report states $742.59. Fable and
     Sonnet reproduce to the token; the whole $0.57 sits in Opus, 0.10% of the
     Opus figure and 0.08% of the total. That is the same delta the 2026-09-19
     report discloses for the same comparison, reached independently here.

     Two things this pass did not do, so a reader does not go looking. It
     created no repository, disposable or otherwise — every repository on the
     account outside this one was created on 2026-09-19 — so this report's pull
     request carries no `Delete after review:` line. And no pull request merged
     on 2026-09-18 before the window opened, so the window's twenty-one and the
     UTC day's twenty-one are the same set. -->

- Repository: marcelusfernandes/agentic-setup
<!-- The loop ran against this repository itself, by agents. -->
- Commit: 1ddc867d82e0886aaba71edf074be176a8925e30
<!-- `git log origin/main --first-parent --until=2026-09-18T23:48:56Z -n 1`
     answers this sha: the tip `main` was at when the window closed, which is
     also the merge commit of PR #333, the last pull request to merge inside the
     window (23:15:20Z). Verified against
     `gh pr view 333 --json mergeCommit`, which answers the same 40 characters.
     Every claim this report makes about the tree is measured at this commit,
     not at `main`, and every command below is pinned to it — a sha's tree is
     immutable, which is stronger than restricting a command to paths, because
     this file does not exist in it and so no command below can read it. -->
- Turns: 765
<!-- Unique assistant response ids in the orchestrating session's transcript
     inside the window, subagents excluded: each subagent keeps its own
     transcript and its work is counted in the scoreboard's `tool calls` column
     instead. That is the unit of the 2026-09-17 report's 775 and of the
     2026-09-19 report's 631, and the method reproduces both (see the note
     above). The UTC day holds the same 765; the window and the day differ in no
     turn, because the session's first and last events of the date are the
     window's own bounds.
     Twenty-one pull requests merged inside the window, so 36.4 orchestrator
     turns per landing, against 12.7 for 2026-09-17 (775 over 61) and 39.4 for
     2026-09-19 (631 over 16). The three are comparable in unit and not in kind:
     2026-09-17 spent 35 of its 61 on one-round approvals, and this pass spent
     its turns on grants, sequencing, eight review rounds on one document and
     the four phase closes. The scoreboard is where the difference is visible. -->
- Minutes: 440
<!-- Last timestamp minus first timestamp of that date in the same transcript,
     wall clock: 16:28:56.809Z to 23:48:56.220Z is 439.99 minutes. It includes
     the operator's idle time between dispatches, which is the honest reading of
     "how long did the pass take" and the reading `docs/dogfood/README.md` asks
     two passes to be compared on. It does not include the 22 hours the account
     was rate-limited before the window opened: the session's first event of the
     date is the refused turn, and its first working turn is nineteen seconds
     later, so no part of the wait is inside these bounds. -->
- Cost (USD): 648.62
<!-- A reconstruction, not a billed amount, and still the one number in this
     header the runtime does not give the writer: an agent running inside Claude
     Code can read its token counts and has no cost accounting exposed to it at
     all. The 2026-09-17 report carries that as a finding of its own, resolved to
     #295, and nothing about it changed here, so it is not raised again below.
     How it was derived, the method of `memory/pass-metrics-2026-09-17.md`
     followed rather than reinvented: token usage summed per model over the
     orchestrating transcript plus every subagent transcript beside it,
     deduplicated by response id, then priced at the list prices published at
     platform.claude.com on 2026-09-17 — the same table, so the three passes are
     priced identically. 5,253 priced responses over the 317 transcripts read,
     of which 73 carried a priced response inside the window.
     Totals behind it, as input / 5m cache write / 1h cache write / cache read /
     output:
       Opus 5      10,490 / 18,951,702 / 1,335,486 / 944,437,977 / 1,235,719 = $634.97
       Fable 5.1       18 /          0 /   617,389 /   4,534,109 /     3,501 = $13.66
     List prices used, per million tokens: Opus 5 $5 in, $6.25 5m write, $10 1h
     write, $0.50 read, $25 out; Fable 5.1 $10 / $12.50 / $20 / $0.25 / $50.
     Per landing: $30.89 against 2026-09-17's $12.17 and 2026-09-19's $35.94.
     Two limits, both unchanged from the previous passes: list price is not the
     price billed to a subscription account, and the figure excludes what the
     operator's own interactive turns cost outside this session.
     One more measurement, which has no bullet of its own because the format
     carries six: the orchestrator made 678 tool calls in the window. The
     scoreboard's `tool calls` column counts the implementers instead. -->
- Transcripts: the orchestrating session transcript and the per-agent subagent transcripts beside it, under the Claude Code projects directory for this workspace, outside this repository

## Scoreboard

<!-- One row per case, where a case is a group of pull requests that exercised
     the same thing, the way the two previous reports grouped sixty-one into
     eleven rows and sixteen into nine. Rows 1 to 6 partition the twenty-one
     pull requests that merged inside the window exactly —
     4+11+1+2+1+2 = 21 — rows 7 and 8 are the two orchestrator-owned exercises
     that produced no pull request of their own, and row 9 is the four pull
     requests open when the window closed.

     The twenty-one, verified with `gh pr list --state merged --json
     number,title,mergedAt,mergeCommit` filtered to the window, never from
     memory, with the issue each one links read from its own body. The UTC day
     answers the same twenty-one: nothing merged between midnight and the
     window's open. The pairs, as pull request then issue:
       288->260  287->265  290->214  292->168  306->227  285->157  314->158
       317->229  319->309  313->230  320->159  311->231  325->266  329->302
       331->237  307->278  337->289  334->315  328->232  335->326  333->324

     The pull requests behind each row, so the grouping can be checked:
      1. 285 287 288 290
      2. 292 306 314 317 319 320 325 331 334 335 337
      3. 307
      4. 311 328
      5. 333
      6. 313 329
     Row 9 is #342 (issue #238), #346 (#316), #348 (#339) and #349 (#330).

     How to read the three numbers:
     - `exit` is the outcome of the land for the case: 0 where every pull
       request in the row merged inside this pass, 1 where it did not.
     - `error class` is the blocker class, `none` for a row that ended clean.
     - `tool calls` is the implementers' own count, summed over the issues in
       the row: `tool_use` blocks in the assistant messages of each
       implementer's transcript, counted inside the window and nowhere else. A
       round that straddles either boundary contributes only the calls it made
       between 16:28:56.809Z and 23:48:56.220Z. That is the 2026-09-19 report's
       rule, not the 2026-09-17 report's, which counted a round whole once it
       had started inside its window; it is named here because this is the
       column the format exists to compare two passes on.
       Two rounds straddle the close and are the reason the rule has to be
       written down: #316's implementer began 22:36:06Z and made 154 calls of
       which 105 are in-window, and #330's began 22:55:38Z and made 90 of which
       71 are. The 2026-09-19 report carries the other side of both — 49 and 17.
       105+49 accounts for all 154 of #316's; 71+17 leaves two of #330's calls
       in the twelve-minute gap between the two windows, counted by neither
       report, and they are disclosed here rather than quietly absorbed.
       Per issue, in-window calls: #157 38, #265 36, #260 0, #214 33, #168 33,
       #227 128, #158 76, #229 122, #309 65, #159 292, #266 41, #237 118,
       #315 92, #326 61, #289 398, #278 196, #231 200, #232 138, #324 133 (two
       rounds, 55 and 78), #230 109, #302 201, #238 117, #316 105, #339 88,
       #330 71. No issue is counted in two rows. #260 contributes 0 because its
       implementer finished on 2026-09-17; the pull request merged here with no
       implementer round inside the window at all.
     - Row 7 counts the same unit read from the milestone-migration agent's own
       transcript. Row 8 carries 0 and that is a measurement, not a gap: the
       four closes were run from the orchestrator's own turns rather than by a
       subagent, so their cost sits in the 678 orchestrator tool calls the
       `Cost (USD)` note records and in no implementer transcript.

     What the mechanism caught and what a reader caught, which
     `docs/dogfood/README.md` asks a scoreboard to separate, and the honest
     answer is lopsided. Two verdicts in this window came from a required check
     and both were about the negative control, in opposite directions: it
     refused a change to test-only machinery that no work by its implementer
     could have satisfied, and on another pull request it printed `pass` off a
     red it had not earned, because the only failure in the run it spawned named
     no file the overlay had placed. Everything else that held or sent back a
     pull request here was found by an agent reading a line — the unswept
     finding column of the previous report, three separate rounds of citations
     that a merge had moved, a risk note and a failure mechanism each drawn from
     a probe that had not established it, and a grant line whose justification
     granted a path nobody meant to grant. That is this report's bound on
     itself: the pass's most expensive findings were reached by reading, and a
     reader is not a control.

     Review rounds are counted from the reviewer subagent transcripts, not from
     the verdict comments on GitHub, and the two disagree by a wide margin:
     forty-six reviewer rounds started inside the window across twenty-four pull
     requests, and twenty-one comments carrying the reviewed-sha marker were
     posted inside it — one per pull request that merged. The difference is
     twenty-five, because the orchestrator posts a verdict at landing rather
     than when it arrives, so an intermediate round leaves nothing on the pull
     request a later reviewer can read. That gap is a finding below, and it is
     why row 3's eight rounds and row 4's ten are readable here and not on the
     pull requests. -->

| case | exit | error class | tool calls | decision | reason |
| --- | --- | --- | --- | --- | --- |
| carried in from the previous pass's cutoff and closed out here | 0 | none | 107 | keep | four pull requests were open when the weekly limit ended the 2026-09-17 pass and all four landed inside this one; two needed a single closing round and two needed two, so the pass paid for the rounds and not for the findings that caused them |
| claim, implement, isolated review, land — approved in one round | 0 | none | 1426 | keep | eleven of the twenty-one landings took exactly one review round, which is this pass's baseline and the number the next pass is compared against |
| the previous pass's own report, eight review rounds | 0 | report-column-unswept | 196 | fix | every number in it was verified and re-verified and one column never was: the hundred and two prose descriptions were sampled each round, every round that sampled found something false, and the eighth round found one more, which is the defect this report's own pull request is held to |
| two changes to the grant parser, five review rounds each | 0 | contract-file-contention | 338 | fix | both edit the same decision item and the same skill card, so the second was held behind the first and then had to re-point every citation into a file the first had grown from 439 lines to 483; the re-pointing was correct when written, correct when reviewed and wrong the moment it landed, because a squash renumbers with no textual conflict to announce it |
| a decision item re-reviewed after a merge GitHub called clean | 0 | citation-broken-by-merge | 133 | fix | the sibling landed with no textual conflict, and the second review found one citation of the item pointing at a bare brace while the code it quoted had moved 38 lines down inside a file that grew 44 lines; the review was dispatched on the standing rule that a clean merge and a resolving citation are different assertions |
| sent back once, each time on an account of why rather than on the code | 0 | unreproduced-mechanism | 310 | keep | one implementer shipped a risk note drawn from a probe run through a shell that had rewritten the path, and the other reported a failure mechanism its own probe had not established; in both the code was right and the explanation was not, each cost one round, and an isolated review disproved both by measuring rather than by arguing |
| the eight open milestone descriptions migrated to the format the closing script parses | 0 | exit-criteria-unparseable | 74 | fix | every open phase wrote its exit criteria as prose the parser cannot read, so every phase of the run was unclosable and nothing said so until someone read the parser; the defect is recorded in the previous pass's report and this is the migration that paid for it |
| four phases closed through the closing script, the first real closes | 0 | none | 0 | keep | M11, M12, M15 and M14 closed between 22:28:09Z and 23:05:54Z against their evidence files, which is the first end-to-end use of the mechanism three phases were built for |
| open at the window close, three under review and one still being written | 1 | in-review-at-cutoff | 381 | keep | three had a reviewer running at 23:48:56Z — two first rounds dispatched in the last ten minutes and one narrow re-check of a citation round — and the fourth had no reviewer yet, its implementer still working; a case that straddles the boundary carries the cost it had spent and no outcome, which is what the boundary means, and after the window all four merged inside the next pass, one of them only after the gate it could not satisfy became an issue of its own |

## Findings

<!-- `origin` names where the finding came from and is copied verbatim into the
     `Origin:` line of the issue a row names, so the two records can be compared
     mechanically. Where a row's outcome is a bare issue number, the origin cell
     was read back from that issue's own `Origin:` line with
     `gh issue view <n> --json body -q .body | grep '^Origin:'` rather than
     copied from the pass's notes, because the two can differ and the issue is
     the artefact a later reader has. Run over every row that resolves to a bare
     issue number, joining an origin line's continuation lines before comparing so
     a wrapped sentence is not read as a disagreement: ten rows, ten agreeing
     word for word, zero disagreeing.
     Four more rows name an issue and deliberately do not resolve to a bare
     number, because that issue's own `Origin:` line records a different source —
     #316 was filed from the review of PR #311 rather than from the grant audit,
     #341 from the review of PR #334 rather than from the wrapping finding, #308
     from the review of PR #251, and #238 from a pull-request report of the
     previous pass. Each carries a written reason naming the issue that covers
     it instead of a bare number claiming a provenance the issue does not carry.

     The rule the provenance check cannot reach is the one that matters most
     here: twenty-four of these thirty-four rows resolve to a written reason or
     to a merged pull request rather than to a bare issue number, and no
     mechanical check reaches any of them. They were read one by one against the
     artefact each names; the count is in the pull request.

     The `finding` column was swept end to end before this file landed, not
     sampled: thirty-four descriptions, thirty-four checked, thirty-one held as
     first written, three false and corrected above in the row rather than in a
     note. Two further rows state, at the pinned commit, a fact the source they
     came from had wrong, and those two are the ones worth naming here, because
     they are this report repeating the mistake it is reporting: both numbers
     were copied from an issue and believed because they were written down
     there. The three that were false in the draft, and the method, are in the
     pull request, which is where `docs/dogfood/README.md` asks for them.
     - the row on the attribution trailers. The claim, written in the pass's
       notes and carried verbatim into #343, is that the most recent commit on
       `main` carrying a trailer predates this phase of work and every commit
       since has none. At the commit this report is pinned to that is false:
       `git log 1ddc867 --grep='Co-Authored-By' -n 1` answers 1ddc867 itself,
       the merge of PR #333, which landed 33 minutes before the window closed —
       and PR #333 is the very pull request whose implementer kept the trailers
       and said so. The sentence was true when it was written and false by the
       end of the same window. The counts move with it: 49 of 141 commits carry
       `Co-Authored-By` at this commit, not 48 of 140.
     - the row on the files `grep` cannot read. The finding says exactly three
       tracked test files contain a NUL byte, having already corrected itself
       from two. At this commit there are four, and the fourth is not a test
       file at all, which is why a scan restricted to the test tree kept missing
       it: `scripts/lib/adopt/workflows.mts` alongside `tests/adopt-pr.test.mts`,
       `tests/adopt-workflows.test.mts` and `tests/close-milestone.test.mts`.
       The issue's own title already says four; its body still says three.

     One description of the thirty-four rests on evidence that did not outlive
     the run, and it is named here so a later reader can tell an unverifiable
     row from an unchecked one: the row on the phase that grows faster than it
     is worked states the issue count that phase was created with, and nothing
     readable today carries it — the milestone has since closed and its issue
     set is the set it ended with. All three of that row's counts are the pass's
     own reading of the milestone at the time, none of them resolves against the
     tree or against GitHub now, and that is why the row resolves to a planning
     decision rather than to a number anyone is asked to trust.

     Commands run rather than cited, pasted because citing one is not running
     it. Every one is pinned to 1ddc867, whose tree does not contain this file.
     - `git ls-tree -r --name-only 1ddc867` lists 179 tracked files; reading each
       blob and testing it for a 0 byte answers the four named above.
     - `grep -c invariant` on the blob of `tests/adopt-pr.test.mts` extracted
       from that commit printed NOTHING and exited 1. Not a zero — no output at
       all. `git grep -c invariant 1ddc867 -- tests/adopt-pr.test.mts` printed
       `1ddc867…:tests/adopt-pr.test.mts:2` on the same bytes.
     - over the extracted test tree, `grep -rio invariant` printed 21 and
       `git grep -io invariant 1ddc867 -- 'tests/*'` printed 24; by file, 16
       against 18.
     - `git grep -c 'scope:' 1ddc867 -- ci/scope-check.mts` printed
       `1ddc867…:ci/scope-check.mts:1` — one line, the log prefix.
     - `git grep -ci label 1ddc867 -- ci/lib/scope.mts` printed NOTHING and
       exited 1, which is how this repository's grep reports no match. The
       sentence that goes with it is "no line of that module names a label", and
       it is supported by a command that printed nothing, not by one that
       answered 0.
     - `git show 1ddc867:.github/workflows/agentic-checks.yml` carries
       `types: [opened, synchronize, reopened, edited, labeled, unlabeled]`
       under `pull_request`, and a header saying both checks "read the PR body
       and labels, so they re-run when the PR is edited or relabelled".
     - `git log 1ddc867 --since=2026-09-18T16:28:56Z --format=%h` lists 21
       commits and `--grep='Co-Authored-By'` narrows them to one, 1ddc867.

     Where this report contradicts something already shipped, it names both.
     Three places, and none of them is a quiet supersession:
     - `docs/dogfood/2026-09-17.md` resolves its label-re-trigger row to two
       issues about waiting the re-run out. The row below says the cause was
       never asked for a day and sixty-one landings, and that the workflow's own
       header gives a reason neither half of which survives reading the code.
       Both records stand: the workaround was real and so is the cause.
     - the same report's scoreboard row for the four pull requests that merged
       with no review is confirmed rather than contradicted — re-running its
       filter returns exactly those four — and the row below adds that the
       practice recurred three more times on 2026-09-18, after the finding had
       shipped, by an orchestrator following a card correctly.
     - #343 and #350 each carry a sentence this report measured as false at the
       pinned commit, and the corrections are in the rows above rather than
       here, because a note cannot stop a sentence asserting what it still says.

     Three issues filed inside this window carry an `Origin:` line dating their
     source review to 2026-09-19, a day that had not begun: #324 created
     19:34:17Z, #326 at 19:48:25Z and #336 at 21:59:48Z, each naming a review
     that ran on 2026-09-18. It is a row below rather than a note, because the
     date is the one part of an origin cell a later reader can check
     mechanically. -->

| finding | origin | outcome |
| --- | --- | --- |
| a pull request exists from its first push, so the monitor's notification of a new one is not an implementer finishing; a review dispatched off that signal raced the implementer, the head moved under it, and the reviewer's dispatch brief tells it to stop and report the head changed rather than review a diff that is no longer there | dogfood 2026-09-17, PR #307 (issue #278) | not work as code: the rule that settles it is the orchestrator's own and was already written down before this happened — read a head only from the implementer's final report — and the cost was one wasted review. Two things are worth being exact about. That stop rule lives in the dispatch prompt and not in `agents/reviewer.md`, which at the commit this report pins names no such stop at all; and nothing mechanical separates the two signals, because the in-review label sits on the pull request from the moment the implementer applies it |
| the phase that collects review findings grows faster than it is worked, and its exit criterion is a condition on a set the work keeps extending: thirteen issues at creation, twenty-four open beside fifteen merged | dogfood 2026-09-17, counting M17's open issues on 2026-09-18 | answered by splitting the phase rather than by changing the mechanism: M18 holds the deferred remainder and this report's own issue is filed under it, which is a planning decision and not work on the loop |
| two parsers read an authorised grant line and neither knows the other exists; on a bare comma-separated list they diverge, so a path enters the ordinary glob list while never appearing under the authorised heading, and both documents invited exactly that form | dogfood 2026-09-17, implementing #231 | already covered by #231, landed as PR #311 inside this window, which reads a bullet-form grant once and as a grant |
| six grant lines across three issues granted whatever their justification quoted, all written by the orchestrator, and one of the six granted a real file the issue never meant to grant | dogfood 2026-09-17, audit prompted by the #231 implementer's observation | already covered by #316, whose own origin line records the review of PR #311 rather than this audit, and which landed as PR #346 after this window; it refuses a multi-span grant line instead of narrowing it to the first span, because narrowing would trade a silent over-grant for a silent under-grant |
| a report is written after the window it measures by an agent still inside the run, so three pull requests it called open had merged before the commit carrying that sentence was authored | dogfood 2026-09-17, review of PR #307 round 2 | already covered by #295, which asks the format to say what a report may claim about a time other than its window; the sentence itself was corrected inside PR #307 before it landed |
| a pull request's file list lagged its push, so four reads agreed that no open branch held the decision register while an unmerged sibling had already taken the next number and edited the sentence naming it | dogfood 2026-09-17, implementing #229 round 2 | already covered by #265, landed as PR #287 inside this window, whose card sentence states the index lag; this is that finding at a new endpoint — a file list after a push rather than a listing after a write — and the sentence reaches both |
| a probe run through the shell answered about a path the shell had rewritten: an unbraced revision and path let a colon be read as a parameter-expansion modifier, so every answer was about a path that did not exist, and the risk note drawn from it was backwards in both directions | dogfood 2026-09-17, fix round of PR #313 (issue #230) | not work: the production code passes argument arrays to the process with no shell, so only the evidence-gathering was ever affected, and PR #313 landed inside this window with the note corrected and the tree-based presence read established as load-bearing rather than cosmetic |
| a literal-string replacement keyed on a dependency label ate an acceptance criterion of another issue that quoted the same label as prose, leaving a sentence with no verb and an unterminated backtick | dogfood 2026-09-17, review of PR #307 round 3 | not work: repaired by hand during the pass from the revision GitHub retains, and the part worth keeping is the blast radius — a pull request about a documentation file damaged an issue body, which no check on that pull request reads, and the issue lint passed it because its checks are about globs and sections rather than about whether a criterion is a sentence |
| a merge GitHub called clean broke a decision item's citations into the documents it merged: twenty-two lines inserted high in one and twelve in another moved three of the item's citations, two of them landing inside an issue template, while the file carrying them was never touched by the merge | dogfood 2026-09-17, finding D38 | #323 |
| the same defect arrived a second time by a route nobody was watching: a re-pointing verified at the branch's own head, and approved on that verification, was wrong the moment the squash landed, because the default branch had moved underneath it | dogfood 2026-09-18, second-pass review of PR #333 after PR #328 merged | not work as a rule the agent can follow: every verification in the pipeline runs strictly before the only event that decides the answer, so the remedy is either a check that runs after the merge or citations that do not name lines; #323 is the first of those two and the second is not filed |
| finding a rule and applying it to the instance in front of you instead of to every instance the rule reaches was the run's most repeated failure, in three agents and twice more in the orchestrator, and the sharpest diagnosis came from the agent that made it twice in one pull request | dogfood 2026-09-17, six rounds of review on PR #307 | already covered by #332, filed from the eighth round of that same review, which asks the next report to sweep its finding column and write the count into the pull request rather than sample it; four of the six instances were caught by an isolated review reading the artefact against the rule as written, which is the argument for the review being isolated rather than collaborative |
| an issue whose first premise a merge falsified stayed open two days with nobody owning the amendment, and the pull request that falsified it had no reason to look at an issue it did not close | dogfood 2026-09-17, review of PR #307 round 6 | already covered by #238, which the previous report also carries from an earlier occurrence; this row is the second measurement, and what it adds is the cost — an implementer claiming it would have found the criterion apparently satisfied and had to decide alone whether the issue or the code was wrong, which this run hit three times |
| the rejection label was used to mean "sent back for more work" with no review having run, and an implementer could not tell the two apart; it happened three more times after the finding was written, twice on the same pull request and once in a new sense again | dogfood 2026-09-17, PR #328 (issue #232) | not work yet, and the reason is that the label is the only channel available: the states as written cover the review verdict and nothing else, there is no state for "the orchestrator is deciding something the implementer asked for", and knowing the gap did not stop the misuse. #388 asks reconcile to name an issue whose state label disagrees with its pull request, which is a neighbouring half of the same gap and not this one |
| a failure mechanism a probe had not established was reported, amplified by the coordinator that had only the report, repeated to the user, and disproved by an isolated review that built a thirty-thousand-path fixture and measured nine buffer sizes | dogfood 2026-09-17, review of PR #329 (issue #302) | not work: the code is right either way — the overflow path fails closed, which is what the issue said — and PR #329 landed inside this window with the account of why corrected in all three places that carried the false one |
| a coordinator's endorsement of a finding is not evidence for it, and both parties treated it as if it were; the implementer located its own error more precisely than the coordinator had, as reading past a number in its own probe output because the number fit the story | dogfood 2026-09-17, PR #329 round 2, stated by the implementer | not work: it is a statement of how the loop's parties relate to evidence, recorded because the orchestrator is the only party that sees every report and none of the evidence, so it is the worst placed to raise a claim's confidence and the best placed to lower it |
| a correct new lint makes nineteen of thirty-two open issues undispatchable where thirteen fail today, and the cost was measured against the unmerged branch before it landed rather than one refused claim at a time | dogfood 2026-09-17, review of PR #328 (issue #232), measured by the orchestrator against the branch | not work: the check is right — a granted file is a file the pull request may touch, so it belongs on both sides of the disjointness comparison — and the measurement corrects its own first framing, since of 109 overlapping pairs over those issues 87 are scope against scope and only 22 involve a grant with no scope overlap, so grants made a pre-existing shape more visible rather than imposing one |
| a search that reads one line at a time reports clean on a sentence that wraps, and this repository's prose wraps at about seventy-two columns, so three sweeps in one run reported zero disagreements while rows disagreed | dogfood 2026-09-17, PR #307 round 8, named by the implementer | already covered by #341, whose own origin line records the review of PR #334 rather than this finding: it asks the closing script to read a reference that wraps onto a continuation line, which is the same blindness in a parser rather than in a hand sweep. The provenance check this report runs joins continuation lines before comparing, for exactly this reason |
| the overlap the new lint would have caught was created by the orchestrator while that lint sat unmerged in the pull request the overlap was with, an hour after it had measured that same migration and not consulted it | dogfood 2026-09-17, dispatching #324 alongside PR #328 | not work: the cost was small and recoverable, which is the useful part — it demonstrates the gap on work the run could afford to lose — and #232 landed as PR #328 inside this window, after which the lint that would have caught it is the lint that runs |
| an orchestrator copying the type label onto a pull request for tidiness is, in the same keystroke, waiving the run's only quality gate, because that label is an override rather than a relaxation and beats a base ruleset that has deliberately turned review on | dogfood 2026-09-18, PRs #334/#335/#337, reported independently by two implementers | already covered by #308, filed from the review of PR #251 and landed as PR #369 after this window, after which the exemption reads the changed paths instead of the label |
| the same practice recurred after the report carrying the finding merged at 21:11:33Z: the three pull requests opened between 21:56Z and 22:02Z each gained the copied label, and a fourth went through the unreviewed mode because a label already sitting on it was not removed — the rule had been written as a rule about copying and the act that mattered was not removing | dogfood 2026-09-18, PRs #333, #334, #335 and #337 | not work as a second issue: #308 is the fix and the recurrence is the argument for it. The shape worth keeping is that the difference between a landing the mechanism reviewed and one it did not was whether the label was remembered in the ninety seconds before landing, and a rule that has to be remembered at a moment nobody is watching is not a control |
| an implementer clearing what it believed were its own stale processes killed the test run of every worktree on the machine, and the only reason the record is accurate is that it said so against its own interest | dogfood 2026-09-18, findings D56 and D48 | #351 |
| two implementers in separate worktrees with disjoint globs both wrote their pull request body to one filename in the shared scratchpad, and a pull request was created carrying a closing keyword for the other one's issue | dogfood 2026-09-18, findings D56 and D48 | #351 |
| an implementer finishing a scoped task named a consequence of someone else's change that fell outside its globs, bounded exactly what it had and had not verified, and stopped, instead of widening its own scope to chase it or staying silent because nobody asked | dogfood 2026-09-18, PR #334 (issue #315), raised by the implementer | not work, and the residual is real in form and empty in fact: the milestone list shows M1 through M9 last written at or before 2026-09-17T12:24:31Z, which is before this window opened, so the description rewrite touched only open phases and the nine retroactive closeouts it might have invalidated are untouched. Recorded because the answer is not the useful part — the behaviour is what the scope rules exist to produce, and this is the run's clearest instance of it |
| whether an issue is dispatchable has two different answers and the cheaper one looks authoritative: the lint printed a boolean called ok for three issues in a row and the claim script then refused all three, because the lint reads a blocking relation only to excuse an overlap and never to enforce it | dogfood 2026-09-18, three refused claims in a row | already covered by #299, which asks the lint to say when a run could not check glob disjointness — the same shape of an `ok` field answering a narrower question than its caller is asking. What the three refusals add and that issue does not carry: the correct probe is the claim script itself, which refuses without side effects, and reading the blocking line by pattern has its own trap met in the same session, where a match landed on an acceptance criterion quoting the label rather than on the dependency line |
| the disjointness lint compares an issue's globs only against open issues in the same milestone, so of six overlapping issues it reported five and stayed silent about the sixth, and the sixth was the only one with code already written against the file | dogfood 2026-09-18, finding D50 — refused claim of #238 | #338 |
| two implementers an hour apart met the same conflict between a harness reminder asking for attribution trailers and the operator's rule disabling them, both flagged it unprompted, and resolved it opposite ways; the squash inherits whatever a branch wrote, and the force-push denial means no branch commit can be corrected after its push | dogfood 2026-09-18, finding D53 | #343 |
| a cost was recorded as a property of the system and worked around for a day and sixty-one landings, and the two greps that would have ended it were never run: a label edit re-runs two checks whose verdicts no label can change, and the workflow's own header gives a reason neither half of which survives reading the code | dogfood 2026-09-18, reading the workflow while diagnosing a failing scope check that turned out to be unrelated | #347 |
| four tracked files contain a NUL byte, so this platform's grep classifies them as binary and suppresses their matches while printing nothing at all — not a count, not a binary-file line, no signal a caller would notice — and one of the four is not a test file, which is why a scan restricted to the test tree kept reporting three | dogfood 2026-09-18, finding D55 — AC4 sweep of #330 | #350 |
| the negative control asks only whether the overlaid run exited non-zero, so an unrelated failure inside the run it spawns is read as the tests biting and the gate prints pass on a change nothing depends on; the discrimination it already makes one level down, requiring a diagnostic to name an overlaid path, is missing where it decides a pass | dogfood 2026-09-18, finding D57 — reconciliation of a local `vacuous` against a CI `pass` on PR #349 | #354 |
| facing two measurements that disagreed, the orchestrator ranked the machine above the agent, wrote the finding in those terms and released the implementer from the remedy; the implementer kept the work on merit and found the cause in the log, and had it complied the pull request would have landed green on a verdict that was wrong | dogfood 2026-09-18, PRs #348 and #349 | already covered by #354, which is the code half; this half is the framing, and the correction is that the question was never which source is authoritative but which measurement is correct, which is answered by reading logs rather than by ranking sources |
| a change to test-only machinery cannot be proved by an overlay at all, because the overlay carries the fix with it, and every route out closes: extracting the scanner overlays it too, moving it to the pure-function directory makes the base unresolvable, a proof declaration still applies the overlay, and the skip variable would silence the gate by configuration | review of PR #348 (2026-09-18), the sole blocking finding | #355 |
| five review agents doing exactly what they were told exhausted a shared quota that is load-bearing in four places at once and invisible until it is gone, and the obvious diagnostic reports full remaining while every request is refused, because it is a secondary limit | dogfood 2026-09-18, three consequences observed in one evening | #356 |
| a verdict that arrives and is not posted until landing exists nowhere a later reviewer can read: forty-six reviewer rounds started inside this window and twenty-one marker comments were posted, one per landing, so twenty-five rounds left nothing behind | dogfood 2026-09-18, the reviewer transcripts read against the pull requests | not work as code: the marker comment already ties a verdict to the head it judged and the landing script refuses a stale one, so a verdict posted early and superseded is unambiguous rather than misleading; what is owed is the habit of posting when it arrives, and twenty-five is the measured cost of not having it |
| three issues filed inside this window carry an origin line dating their source review to the following day, which had not begun when they were created | dogfood 2026-09-18, the provenance sweep of this report | not work, and not fixable from this branch: an implementer may not run the issue-editing command at all, so the three are named here for whoever can. The date is the one part of an origin cell a later reader can check mechanically, and three of the twenty-two issues this report resolves against carry one that had not happened |
