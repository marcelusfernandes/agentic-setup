# Dogfood 2026-09-17 — the loop at scale on this repository: sixty-one pull requests through claim, isolated review and land

<!-- Not a retroactive report. The pass ran after this format landed (PR #253,
     issue #183), so no `Retroactive:` bullet and no `not recorded` cell: every
     number below is measured, and each one states where it comes from at the
     point it is derived.

     What the pass was: the repository's own loop — `claim.mts`, a worktree per
     issue, an implementer, CI (test, scope, negative control), an isolated
     read-only reviewer returning a verdict, the orchestrator applying the
     label, `land.mts` — run by agents against this repository for a day, to
     take milestones M11 through M17 from open to done.

     The window the numbers measure: 2026-09-17T02:09:03Z to
     2026-09-17T18:03:07Z, the first and last timestamp of the orchestrating
     session's transcript on that date. The pass did not end there. It
     continued past 18:03Z and was interrupted minutes later, at about 18:05Z,
     by an account-wide weekly rate limit that ended six subagents in the same
     minute (finding D25, the last scoreboard row); work resumed on 2026-09-18
     after the limit reset. This report is dated for, and counts, the day the
     pass ran. Four pull requests were open when the limit hit — #285, #287,
     #288 and #290 — and none of them is counted as landed here. -->

- Repository: marcelusfernandes/agentic-setup
<!-- The pass ran against this repository itself, by agents, rather than
     against a disposable one. The one disposable repository of the pass is the
     SubagentStop experiment's, recorded in its own scoreboard row. -->
- Commit: 55ecf9aadb12d9527f6fd7e4544fb1c9cc8db361
<!-- `git log origin/main --first-parent --until=2026-09-17T18:03:07Z -n 1`:
     the tip `main` was at when the window closed, which is also the merge
     commit of PR #286, the last pull request to merge that day (17:57:51Z).
     Verified against `gh pr view 286 --json mergeCommit`. -->
- Turns: 775
<!-- Unique assistant response ids in the orchestrating session's own
     transcript dated 2026-09-17. Subagent turns are not counted here: each
     agent keeps its own transcript, and its work is counted in the scoreboard's
     `tool calls` column instead. -->
- Minutes: 954
<!-- Last timestamp minus first timestamp of that date in the same transcript,
     wall clock. It includes the operator's idle time between dispatches, which
     is the honest reading of "how long did the pass take" and the reading
     `docs/dogfood/README.md` asks two passes to be compared on. -->
- Cost (USD): 742.59
<!-- A reconstruction, not a billed amount, and the single number in this
     header that the runtime does not give the writer: an agent running inside
     Claude Code can read its token counts but has no cost accounting exposed to
     it at all, so nothing here could be "read off" a bill.
     How it was derived: token usage summed per model over the orchestrating
     session's transcript plus every subagent transcript under the session's
     tasks directory, deduplicated by response id, then priced at the list
     prices published at platform.claude.com on 2026-09-17. Totals behind it,
     as input / 5m cache write / 1h cache write / cache read / output:
       Fable 5.1   14,092 / 2,430,717 / 2,789,824 / 266,575,077 / 407,990  = $173.36
       Opus 5      12,046 / 19,826,981 / 1,059,829 / 794,717,120 / 1,188,932 = $561.66
       Sonnet 5       378 / 844,578 / 0 / 25,284,576 / 39,251             = $7.56
     List prices used, per million tokens: Fable 5.1 $10 in, $12.50 5m write,
     $20 1h write, $0.25 read, $50 out; Opus 5 $5 / $6.25 / $10 / $0.50 / $25;
     Sonnet 5 $2 / $2.50 / $4 / $0.20 / $10.
     Two limits worth stating: list price is not the price actually billed to a
     subscription account, and the figure excludes what the operator's own
     interactive turns cost outside this session. That the format asks for a
     number the runtime does not state is itself a finding of this pass; it has
     its own row in `## Findings`.
     One more measurement, which has no bullet of its own because the format
     carries six: the orchestrator made 660 tool calls in that window,
     counted as `tool_use` blocks in the same assistant messages. The
     scoreboard's `tool calls` column counts the implementers instead. -->
- Transcripts: the orchestrating session's transcript and the per-agent task outputs beside it, under the Claude Code projects directory for this workspace, outside this repository

## Scoreboard

<!-- One row per case, where a case is one issue taken through claim,
     implement, isolated review and land. Sixty-one pull requests merged on
     2026-09-17; sixty-one separate rows would be unreadable and would say
     nothing a reader could compare against the next pass, so the rows below
     group by what the pass exercised. Rows 1 to 11 partition those sixty-one
     pull requests exactly — 35 + 6 + 3 + 1 + 2 + 2 + 1 + 5 + 4 + 1 + 1 = 61 —
     and rows 12 and 13 are cases that did not land.

     How to read the three numbers:
     - `exit` is the outcome of the land for the case: 0 where every pull
       request in the row merged, 1 where the case did not reach a merge inside
       this pass.
     - `error class` is the blocker class, `none` for a row that ended clean.
     - `tool calls` is the implementers' own count, summed over the issues in
       the row: `tool_use` blocks in the assistant messages of each
       implementer's transcript, over every round it ran. No issue is counted in
       two rows.

     The pull requests behind each row, so the grouping can be checked:
      1. 185 188 192 194 196 197 199 216 221 222 223 224 225 228 240 243 244 245
         247 248 249 250 251 252 254 269 270 272 274 275 276 281 283 284 286
      2. 187 191 220 236 242 246
      3. 186 219 268
      4. 190
      5. 273 279
      6. 253 282
      7. 193
      8. 210 217 241 255 280  (#255 is in row 8, not row 1)
      9. 189 195 200 218
     10. 235
     11. 271
     Three pull requests of the pass have no row here and are not dropped:
     #287 (#265), #288 (#260) and #290 (#214) were open when the limit hit and
     are still open. Their cases are not finished, so their exit, their error
     class and their final tool-call count do not exist yet; inventing any of
     the three is the reconstruction this format refuses. They belong to the
     report of the pass that finishes them. #285 is the exception and does have
     a row, because its review did reach a verdict. So rows 12 and 13 are the
     cases that did not land *and* have numbers, not every case that did not
     land.

     One asymmetry between this comment and the header, stated rather than
     left to be found: the cost figure sums every subagent transcript in the
     window, including the three implementers above and the reviewers, while
     this column counts only the implementers of the issues in these rows. The
     two halves measure different populations on purpose — cost is what the
     pass spent, tool calls are what a case cost — and neither is derivable
     from the other.

     Verified with `gh`, not taken from the run ledger: 61 pull requests merged
     between 2026-09-17T00:00:00Z and 2026-09-18T00:00:00Z, of which 16 took at
     least one rejection round (17 rejection rounds in all, #193 taking two) and
     5 recorded a second approval after a merge from main changed the head.
     Row 8 contains those 5; rows 2 to 7 contain the 16, minus #271, which has
     row 11 of its own. Four pull requests merged with no verdict comment and no
     review at all; they are row 9.
     Row 8 counts #255 once. Issue #154 has two merged pull requests, #191 and
     #200; its implementer count is charged to row 2 only, so row 9's 109 covers
     issues #176, #161 and #180 and not the #154 follow-up. -->

| case | exit | error class | tool calls | decision | reason |
| --- | --- | --- | --- | --- | --- |
| claim, implement, isolated review, land — approved in one round | 0 | none | 1942 | keep | 35 of the 61 pull requests that merged took exactly one review round, which is the pass's baseline and the number the next pass is compared against |
| sent back once — a rule documented outside the issue's globs left stale | 0 | docs-not-equal-code | 449 | fix | six round-1 rejections of one shape: an implementer may not touch a document outside its `## Files`, so a rule stated elsewhere stays stale until a review finds it and the grant arrives a round late |
| sent back once — a behaviour defect no check reached | 0 | behaviour-defect | 257 | keep | a non-JSON ruleset read swallowed as an empty object, a gap list printed from the wrong object, and a human gate reachable around through a documented sequence; the isolated review was the only reader that found them |
| sent back once — the header's crash policy not honoured by the code | 0 | crash-policy-drift | 77 | keep | `reconcile.mts` names its soft exceptions in its own header and the change added a third without saying so; the invariant is prose and the review is its only reader |
| sent back once — a pin that does not discriminate | 0 | vacuous-pin | 100 | fix | one card slice bounded on a blank line ran to the end of the file, proved by mutation, and one message claimed the entry point is never overlaid, disproved by probe in two reachable cases |
| sent back once — hygiene the required checks already state | 0 | hygiene | 163 | keep | a raw NUL byte turned a test file into a binary diff, and a test file crossed the 800-line cap and made `scope` red; both cost exactly one round and no judgement |
| sent back twice, landed on round three | 0 | behaviour-defect | 110 | keep | `adopt`'s inventory read `.git/hooks` and ignored `core.hooksPath` in round one, and round two found a second defect; three rounds on one issue, each finding a different class |
| an approved head went stale when a sibling merged | 0 | head-changed-after-approval | 353 | fix | five pull requests took a merge from main after approval and needed a fresh review; a conflicting head carries no check runs, so the wait loop read the empty rollup as queued and slept toward a timeout instead of naming the conflict |
| landed with no isolated review at all | 0 | docs-mode-no-review | 109 | fix | four decision-register pull requests carried `type:docs`, which puts `land.mts` in docs mode and asks for no reviewed-sha marker, so the label exempted from review three changes to the register the whole gate exists to protect |
| the SubagentStop gate proven live against a disposable repository | 0 | gate-silent-no-op | 92 | fix | three headless runs: the gate blocked three stops when the session cwd or an isolated agent worktree was the repository, and did nothing at all when the subagent merely changed directory into a worktree, which is the pattern the orchestrator brief used all day |
| the retroactive closeout of M1 to M13 | 0 | brief-false-premise | 128 | fix | the orchestrator's brief told the implementer that `close-milestone.mts` rewrites closeout files, which it never does, and the file was drafted before four issues merged, so all four were written up as left out and both gates accepted it |
| the cross-route lock, sent back on every round and still open at the cutoff | 1 | fix-worse-than-bug | 120 | fix | each round moved the exception to where the last review pointed and left another reader of the same state licensing the behaviour: resolving a Codex lock in `branchFor` moved a Codex-held issue into the bucket the card dispatches, and the next round wrote the exception into three rows of the state table and not the two that matter |
| an implementer ended mid-task on a weekly account limit | 1 | rate-limit | 49 | fix | three commits sat unpushed in the worktree of issue #168 while the issue read `state:in-progress` against an empty lock branch, which is exactly how an implementer that had done nothing reads from outside |

## Findings

| finding | origin | outcome |
| --- | --- | --- |
| the format asks for a cost the runtime never states to the writer | dogfood 2026-09-17, header of this report | #295 |
| the orchestrator writes the type: label after the pull request is created, so the first checks run without one | dogfood 2026-09-17, finding D1 | #301 |
| an issue's ## Files may authorise .claude/settings.json while the implementer card forbids editing .claude/ at all | dogfood 2026-09-17, finding D2 | accepted: the role card is the narrower rule and wins, and the orchestrator does not grant a path the card forbids; a mechanical deny inside issue-lint costs more than the one disagreement |
| prose outside the issue's globs still said the merge deny covers only --admin | dogfood 2026-09-17, finding D3 | already covered by PR #191, whose round-2 grant fixed `docs/orchestration.md`, and by #204, after which neither that file nor the orchestrate card states an --admin-only deny |
| the Map says init installs agents, skills, templates and docs; it copies three things | dogfood 2026-09-17, finding D4 | #202 |
| two orchestrator sessions resumed one conversation and duplicated the GitHub writes | dogfood 2026-09-17, finding D5 | #301 |
| every new script leaves the Map stale, because the Map is prose with no consumer | dogfood 2026-09-17, finding D6 | #202 |
| adopt restates the installer's label and workflow lists, a third copy of the same names | dogfood 2026-09-17, finding D7 | already covered by PR #220, which made labels.json the source, and PR #249, which made the inventory derive the lists |
| two of the first three reviewed pull requests were rejected, both on what CI cannot see | dogfood 2026-09-17, finding D8 | not work: it is the isolated review doing the job the checks cannot, and the scoreboard records its cost at 16 send-backs over 61 landings |
| issue-lint with no milestone-issues file skips the disjointness check and prints ok | dogfood 2026-09-17, finding D9 | #299 |
| applying the approval label re-triggers the checks, so land run straight after hits a queued merge | dogfood 2026-09-17, finding D10 | #265 for the relabel order the card now names, and #239 for the land-side wait |
| a queued auto-merge merged a later conflict-resolution commit that no review had seen | dogfood 2026-09-17, finding D11 | already covered by PR #216, which pins the merge to the reviewed sha |
| two agents recorded register items under one delegation with different Status values | dogfood 2026-09-17, finding D12 | #300 |
| the decision log has a kind for opening every gate and none for closing one | dogfood 2026-09-17, finding D13 | #300 |
| gh pr merge --auto sends the expected head only when auto-merge is enabled, not when it fires | dogfood 2026-09-17, finding D14 | already covered by PR #216 and PR #255; the approved-mode half is #238 |
| a conflicting head carries no check runs, so the wait loop reads a conflict as a queue | dogfood 2026-09-17, finding D15 | #239 |
| three label edits start three runs, and the live one has no startedAt, so latest-run picks a cancelled one | dogfood 2026-09-17, finding D16 | #239 |
| the negative control's structural-red detector never fires on this repository's own harness output | dogfood 2026-09-17, finding D18 | #297 |
| a docs-only change to an agent card is in no negative-control skip class | dogfood 2026-09-17, finding D19 | #301 |
| land in docs mode armed auto-merge on a conflicting pull request | dogfood 2026-09-17, finding D20 | #239 |
| two implementers that both append a decision item always collide on the register file | dogfood 2026-09-17, finding D21 | #301 |
| a closeout drafted during an active run is stale before it is pushed, and both gates accept it | dogfood 2026-09-17, finding D22 | #298 |
| a card pin whose slice is bounded on a blank line silently widens to the end of the file | dogfood 2026-09-17, finding D23 | #296 |
| a finding recorded as a symptom became a card sentence about a mechanism, and only a reproduction caught it | dogfood 2026-09-17, finding D24 | #295 for the rule the format lacks; the corrected card sentence is #265, and docs/dogfood/2026-09-10.md finding L22 is left as it landed with this row as its correction |
| a weekly account limit ended six agents in one minute and nothing could tell finished work from lost work | dogfood 2026-09-17, finding D25 | #291 |
| a contract stated in two files drifts apart one clause at a time and no pin checks the two agree | dogfood 2026-09-17, finding D26 | #305 |
| the provisional closeout paragraph tells the next person to do what the closeout grammar forbids | dogfood 2026-09-17, finding D27 | #298 |
| all eight open milestones wrote exit criteria as prose the closing script cannot parse, and three criteria could never be true | dogfood 2026-09-17, finding D28 | done during the run, after the day this report counts: all eight descriptions now carry the Exit criteria checkbox list and the self-referential, retroactive and out-of-repository criteria were rewritten |
| the orchestrator and the implementer share one GitHub login, so label authorship cannot be checked from the timeline, including on a label that selects the merge mode | dogfood 2026-09-17, finding D29 | not work: the identity collapse was accepted deliberately for the review gate and decision item 18 records its cost; what is new is only that the same collapse makes a label-authorship criterion unanswerable, which is why no milestone should write one. The clearest instance is this report's own pull request, opened the day after the day this report counts: the implementer opened it carrying `state:in-review` alone, the orchestrator added `type:docs` five minutes later, and the timeline attributes both to the same login — so the one label that decides whether `land` demands a review has no readable author, and the record is known to be right only because the orchestrator said so outside it. #308 removes the consequence by making the mode read the changed paths instead of the label; the unreadable authorship stays |
| guard-main's retry path ran live on main after PR #195 and logged both attempts | dogfood 2026-09-17, runtime proof P1 | already covered by PR #185: the proof is the evidence that issue's change works, not a defect |
| three guard-main runs on main after PR #223 succeeded with no retry needed | dogfood 2026-09-17, runtime proof P2 | already covered by PR #223: the lifted-step test keeps the retry path covered where a live run cannot |
| the reviewed-sha pin refused a land after an unreviewed push, before the merge rather than after it | dogfood 2026-09-17, runtime proof P3 | already covered by PR #216: the refusal is the designed behaviour, and the orchestrator lesson is in #301 |
| the SubagentStop gate blocks three stops from the session checkout and from an isolated agent worktree, and is a silent no-op when a subagent only changes directory into one | dogfood 2026-09-17, runtime proof P4 and finding D17 | #301 |
| log-decision parses a ref loosely and leaks its temporary stage directory | review of PR #194 (2026-09-17), non-blocking finding | #203 |
| the Map and init's copy list are not pinned against scripts/*.mts | review of PR #194 (2026-09-17), non-blocking finding | #202 |
| a gh pr merge written with a global flag slips both the hook and the deny list | review of PR #191 (2026-09-17), non-blocking finding | #204 |
| the reviewer-checks summary omits two of the six checks and milestoneLint | review of PR #190 (2026-09-17), non-blocking finding | #205 |
| reconcile miscounts fenced and commented milestone text and reads only the first page of open milestones | review of PR #190 (2026-09-17), non-blocking finding (two rounds; the milestone-lookup gap was reported independently in both) | #206 |
| guard-main merges stderr into its success path and nothing pins its two copies identical | review of PR #185 (2026-09-17), non-blocking finding | #207 |
| the closeout provenance pin resolves its ref loosely and does not enforce row order | review of PR #192 (2026-09-17), non-blocking finding | #208 |
| nothing said the closeout evidence file is a required step before a milestone closes | review of PR #192 (2026-09-17), non-blocking finding | #209 |
| the register's placement rule and its index disagree with where items 14 and 15 were written | review of PR #195 (2026-09-17), non-blocking finding (three related findings, folded into one relocation) | #211 |
| three documents and land.mts still said the review count is set by hand | review of PR #186 (2026-09-17), non-blocking finding | #212 |
| claim carries an unreachable type-label branch and never clears a disagreeing type | review of PR #187 (2026-09-17), non-blocking finding | #213 |
| the negative control can skip a change to its own code in an adopting repository, and the docs-writer flow never sets type:docs | review of PR #187 (2026-09-17), non-blocking finding (round 2) | #214 |
| adopt reads labels one page deep, leaves errors unnamed and interpolates unquoted values | review of PR #193 (2026-09-17), non-blocking finding (round 2) | #215 |
| create-subissue reads its parent loosely and leaves its post-creation failures unnamed | review of PR #197 (2026-09-17), non-blocking finding | #226 |
| close-milestone reads one page of the milestone's issues and crashes where it should refuse | report of PR #199 (2026-09-17), non-blocking finding | #227 |
| init --rules can create a second ruleset on a read it could not parse | review of PR #186 (2026-09-17), non-blocking finding (round 2) | #229 |
| the negative control treats a declaration it cannot read as an absent one and falls back to globs | review of PR #217 (2026-09-17), non-blocking finding | #230 |
| a bullet-form authorised grant is read twice, once as a grant and once as a misplaced line | review of PR #210 (2026-09-17), non-blocking finding | #231 |
| issue-lint never validates the authorised globs it passes through | review of PR #210 (2026-09-17), non-blocking finding (round b) | #232 |
| adopt's inventory restates the installer's lists instead of deriving them | review of PR #193 (2026-09-17), non-blocking finding (round 3) | #233 |
| protect-worktree treats the worktree's node_modules symlink differently from the directory | review of PR #216 (2026-09-17), non-blocking finding | #234 |
| nothing denies an authorised grant written from a worktree, so an implementer could grant itself | review of PR #210 (2026-09-17), non-blocking finding (round b) | #237 |
| land's approved mode never compares the approving review's commit, so a stale approval merges a new head | review of PR #216 (2026-09-17), non-blocking finding | #238 |
| the dogfood pin lets a live pass take the retroactive escape, accepts not recorded in an error class, and leaves five guarded cells without their own fixture | review of PR #270 (2026-09-17), non-blocking finding | #295 |
| the report regex is unanchored, so a nested path or a backup file counts as a report and silences the nudge | review of PR #253 (2026-09-17), non-blocking finding | #295 |
| four more card pins are bounded loosely: one on a blank line, one by a superset span, one by an index pair and one by a proximity window that never discriminates | review of PR #286 (2026-09-17), non-blocking finding | #296 |
| the proof runner and the negative control spawn with no maxBuffer and no timeout, and read a declaration through two different parsers | review of PR #236 (2026-09-17), non-blocking finding | #297 |
| the adoption pull request's own scope and negative-control jobs cannot run, and its plan-issue selection can pick a closed decided issue | report of PR #268 (2026-09-17), non-blocking finding | #302 |
| adopt leaves a temporary index directory behind on one refusal path and caps its tree read at 1 MB | review of PR #268 (2026-09-17), non-blocking finding (round 2) | #302 |
| every gh list read is bounded at 200 and none of them says when it truncated, while two checks that compare against such a list fail open | review of PR #250 (2026-09-17), non-blocking finding | #303 |
| sync-codex-plugin gates the published manifest's existence and never its bytes, so a moved snapshot with no version change passes | review of PR #281 (2026-09-17), non-blocking finding | #303 |
| the contract calls the modules in ci/lib pure while detect now shells out to git and python3 | review of PR #275 (2026-09-17), non-blocking finding | #304 |
| the two contract files still open Codex-first with a legacy Claude route after the README made them peers | review of PR #225 (2026-09-17), non-blocking finding | #304 |
| the loop's rules are stated in both the orchestration document and the card, and clauses were found on one side only in both directions | review of PR #285 (2026-09-17), non-blocking finding (rounds 2 and 3) | #305 |
| the negative control's vacuous message describes the globs even when a declaration suppresses them | review of PR #273 (2026-09-17), non-blocking finding (round 2) | #297 |
| one of reconcile's fifteen new checks is vacuous on the base and the pin's comment follows the emitted literal rather than the documented order | review of PR #282 (2026-09-17), non-blocking finding (round 2) | #303 |
| the transitive-chain lint has no fixture without state:ready and globs, and a cycle among unreachable issues is not reported | review of PR #274 (2026-09-17), non-blocking finding | #299 |
| nothing in tests checks the relative links in docs, so a dead link is caught only by a reader | review of PR #272 (2026-09-17), non-blocking finding | #304 |
| an implementer ran git stash inside its worktree, which its own card's Never list forbids | report of PR #222 (2026-09-17), process finding | accepted: no damage, the stash was popped in the same worktree, and a hook denying git stash costs more than the one occurrence the card already forbids |
| the run brief told implementers to apply a scope: label while the implementer card says the orchestrator writes it | report of PR #280 (2026-09-17), process finding | not work: the card is the law, three implementers said so, and the brief was corrected during the pass rather than the card |
| issue #147 was closed as a duplicate of #148 although nothing else asks for the reviewer identity or for the gate to be switched on | report of PR #271 (2026-09-17), non-blocking finding | already covered by the owner decision recorded as register item 18 and landed by PR #251: the one review mode is an isolated agent with the label written by the orchestrator, so no second identity is created and the review count stays 0 |
| M10's milestone description cited a report that is not in this repository, and issue #154 carries two closing pull requests | report of PR #271 (2026-09-17), non-blocking finding | half done: the M12 closeout records #154 once against the merge commit of PR #191 and names the second pull request in its own note, but M10's description still cites a report measured against another repository, which is #312 |
| the adoption record's generated fields are interpolated unescaped into the plan issue, and record:stale is missing from the gap names | review of PR #219 (2026-09-17), non-blocking finding (round 2) | #302 |
| adopt can leave a partial hook install, drops a non-string deny entry silently, and documents a hand edit invariant 4 forbids | review of PR #252 (2026-09-17), non-blocking finding | #302 |
| init --rules computes the required check names separately from the workflow adopt generates, and renderChecks appends jobs to the YAML unchecked | review of PR #248 (2026-09-17), non-blocking finding | #302 |
| the hook marker and the superseded deny rules are restated in a third module | report of PR #252 (2026-09-17), non-blocking finding | #302 |
| the record's own review left speculative exports, a stale cite and a docstring that describes a different function | review of PR #219 (2026-09-17), non-blocking finding | #233 |
| adopt.mts and docs/adopt.md are both within ten lines of the 800-line cap and the next mode must extract first | report of PR #268 (2026-09-17), non-blocking finding (round 2) | #302 |
| milestoneLint miscounts an unterminated comment, an indented code block and a fence with an info string, and names the wrong cause for a non-milestone payload | review of PR #228 (2026-09-17), non-blocking finding | #303 |
| the two contract files and the marketplace manifest still call hooks/ the two PreToolUse hooks after two more shipped | report of PR #235 (2026-09-17), non-blocking finding | #304 |
| the init card names two of the three workflows init copies | report of PR #225 (2026-09-17), non-blocking finding | #304 |
| register item 16 and the item-13 note are still inline while the rule says dated files, and the two READMEs count the exceptions differently | review of PR #247 (2026-09-17), non-blocking finding | #304 |
| the mode: null claim lives at four sites and correcting only two would leave the documents disagreeing | report of issue #212 (2026-09-17), non-blocking finding | #304 |
| tests/init.test.mts is at 798 of 800 lines, so the repo-view failure fallback cannot be covered without a new file | review of PR #283 (2026-09-17), non-blocking finding | #304 |
| the labels test claims no file may map an unseeded scope label while inspecting only the catalogue index | review of PR #272 (2026-09-17), non-blocking finding | #304 |
| three documents say a Blocked-by relation orders two issues without saying the relation may be indirect | report of PR #274 (2026-09-17), non-blocking finding | #305 |
| two documents still say disposable repository without the private, prefixed and delete-line rule | report of PR #286 (2026-09-17), non-blocking finding | #305 |
| the stop gate ignores stop_hook_active, reads a buffer overrun as not spawned, times out the shell rather than its grandchildren, and runs the full suite at every Stop for an adopter | review of PR #235 (2026-09-17), non-blocking finding | #309 |
| land's docs mode reads the type:docs label and not the changed paths, so a change to a test or a contract card lands unreviewed | review of PR #251 (2026-09-17), non-blocking finding | #308 |
| the closeout provenance pin's empty-template message drifted from the script's own, and its per-row gh reads are unbatched | report of PR #224 (2026-09-17), non-blocking finding | #298 |
| five reviews left comment and docblock wording that describes something the code no longer does | reviews of PR #221, PR #240, PR #236 and PR #244, and the report of PR #280 (2026-09-17), non-blocking findings | accepted: each is a comment or a docblock phrase with no consumer, and the pins that would keep them true cost more than the sentences are worth |
| the invariant-6 catalogue citation was copied into three test headers | review of PR #269 (2026-09-17), non-blocking finding | done during the run: the citation is gone from every file under tests/, fixed by PR #253 in its own file and by PR #269 in the other two |
| python3 -m unittest discover collects nothing on a tests-one-directory-down layout and exits 5, which the negative control reads as inconclusive | dogfood 2026-09-06, finding F1, re-raised by the review of PR #275 | already covered by #277, merged as PR #284 |
| a grant written on one issue widened its overlap with another and issue-lint refused the claim | orchestration note of 2026-09-17, the claim of #205 | not work: the lint was right, a grant widens overlap by design, and the ordering rule it implies is an acceptance criterion of #301 |
| issue #238's first premise became false when PR #255 landed and it must be re-scoped before dispatch | report of PR #255 (2026-09-17), non-blocking finding | #238, whose body now carries the note; re-reading an issue against main before claiming is a dispatch step, not new work |
| decision item 16 adopted the Codex route's colours and descriptions for five state labels, which init --force rewrites in adopters | report of PR #220 (2026-09-17), non-blocking finding | already covered by PR #220, which recorded it as register item 16 and left it vetoable by the owner |
