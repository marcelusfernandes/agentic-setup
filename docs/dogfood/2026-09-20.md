# Dogfood 2026-09-20 — the loop on this repository alone, and a pass that kept catching its own written-down claims being false

<!-- Not a retroactive report. The pass ran long after this format landed, so no
     `Retroactive:` bullet and no `not recorded` cell: every number below is
     measured, and each states the command or the file it comes from at the
     point it is derived.

     What the pass was: one exercise of one mechanism. The ordinary loop against
     this repository — `ci/issue-lint.mts` to sequence, `claim.mts` and a
     worktree per issue, an implementer, CI (`test`, `scope`, `negative-control`),
     an isolated read-only reviewer returning a verdict, the orchestrator
     applying the label, `land.mts` — run over milestone M18, "Deferred
     post-review fixes". Sixteen pull requests merged inside the window and every
     one of them closes an M18 issue, verified issue by issue with
     `gh issue view N --json milestone`.

     **What the pass did not exercise, and no reader may take it as evidence
     about.** No disposable repository was created: no `adopt` run, no headless
     orchestrator run, no third-party repository, no installation of any kind.
     Nothing here says anything about `scripts/init.mts`, `scripts/adopt.mts` or
     what happens to a repository that takes this mechanism on. The previous
     pass, `docs/dogfood/2026-09-19.md`, is the one that ran those, and its
     verdicts on them stand unchanged. Because no repository was created, no
     `Delete after review:` line is owed by this report's pull request, which is
     the one place `docs/dogfood/README.md` would otherwise require one.

     The window the numbers measure: 2026-09-20T01:13:30Z to
     2026-09-20T15:41:22.708Z. Neither bound is a clock cut.
     The open is the merge of PR #410, `3ce7f475`, the closeout of M13 — the last
     landing of the previous phase. Before it the loop was closing M10, M13 and
     M17; from that commit forward every pull request that merged closes an M18
     issue, which is what makes it the boundary between two passes rather than
     between two days. The close is 15:41:22.708Z, and what that timestamp is
     has been re-measured after a review asked, because the first form of this
     sentence was wrong twice over. It is **not** the `Agent` tool call that
     dispatched this report's own implementer: that call is at 15:41:06.268Z,
     16.44 seconds earlier, and it is the event that makes the bound
     non-arbitrary — the pass's last dispatch. And it was the last event the
     session had recorded only at the moment these numbers were taken: the
     session ran on past 16:39Z, reviewing this report. So "the last event the
     orchestrating session recorded" is a sentence that stopped being true
     minutes after it was written. That shape is already in the tree, as finding
     D22 of the 2026-09-17 pass — `docs/dogfood/2026-09-17.md:237`, "a closeout
     drafted during an active run is stale before it is pushed", resolved to
     #298 — and it is cited by that row rather than by the number the 2026-09-20
     scratchpad gives the same shape, because no file in this repository carries
     that number: a recursive grep for it over the tree, restricted to exclude
     this report, returns nothing. The restriction is the point and is not
     pedantry — the first form of this sentence quoted the number while claiming
     the tree held no instance of it, so the command as written would have found
     one, which is the self-referential count the previous pass recorded on
     PR #385. A citation no reader can resolve is the defect this file is about.
     It arrives here in the report's own header. The bound is kept at the measured instant
     every other number here was taken against, and described as what it is.
     Say the filter exactly, because the obvious one answers a different number:
     the **UTC day** 2026-09-20 holds **nineteen** merges. The other three are
     #407 (00:01:52Z), #408 (00:32:07Z) and #410 (01:13:30Z), all three of them
     the previous phase's closeout work, and #410 is the open event itself.
     Sixteen is the count merged strictly after it.

     One orchestrating session, which is a difference from the previous pass and
     is why the `Turns` bullet needs no note this time:
       453d878d-7f6a-45b0-8943-85b67e30bf6e  "[enceladus]"
     The second session of 2026-09-19, 88efcdba-112f-4f8c-bad9-58b289d28a81, has
     zero entries dated 2026-09-20 — `grep -c '"timestamp":"2026-09-20T'` over
     its transcript answers 0 — so none of the two-coordinator caveats that
     report carries apply here.

     **A correction to the issue that asked for this file.** Issue #450 says
     "Ninety-five findings are numbered there, D73 onward". The scratchpad it
     names holds **thirty-eight** numbered findings, D73 to D110
     (`grep -c '^## D' dogfood-findings.md` answers 110 over the whole file,
     which is the sum of this pass and the two before it; `grep -n '^## D7'`
     onward gives the range), plus five `turn 2` sub-entries under D73, D86,
     D88, D92 and D101 — forty-three entries. Ninety-five is not a count of
     anything in that file. Every one of the thirty-eight is carried below;
     nothing was dropped for space. Each `turn 2` is folded into its parent's
     row as a second clause rather than given a row, because each is a
     correction of its parent's own number or claim and is unreadable apart from
     it. -->

- Repository: marcelusfernandes/agentic-setup
<!-- The loop ran against this repository itself, by agents. No other repository
     was touched, which is the whole of the "not exercised" paragraph above. -->
- Commit: a2e8c3df6ac2b2883f0bbe1565060ec27f33d729
<!-- `git log origin/main --first-parent --until=2026-09-20T15:41:22Z -n 1`
     answers this sha: the tip `main` was at when the window closed, which is
     the merge commit of PR #447, the last pull request to merge inside the
     window (15:01:47Z). Every claim this report makes about the tree is
     measured at this commit, never at `main`. -->
- Turns: 403
<!-- Unique assistant response ids in the one orchestrating transcript dated
     2026-09-20 between the two window bounds, subagents excluded. Same unit as
     the 631 of 2026-09-19 and the 775 of 2026-09-17, counted by the same
     script, which is the only reason the comparison is worth anything: run
     against the 2026-09-19 window — the same script, its day and its two bounds
     passed as arguments — it answers 631 turns and $574.98, reproducing that
     report to the digit and to the cent.
     Sixteen pull requests merged inside the window, so 25.2 orchestrator turns
     per landing, against 39.4 on 2026-09-19 and 12.7 on 2026-09-17. The
     direction is worth one sentence and no more: this pass, like the last, was
     almost entirely mechanism changes with heavy contention on shared files and
     ten of sixteen landings taking at least one rejection, where 2026-09-17's 61
     included 35 one-round approvals. The three numbers are comparable in unit
     and not in kind.
     The same script counts 340 orchestrator tool calls in the window. The
     scoreboard's `tool calls` column counts the implementers instead. -->
- Minutes: 868
<!-- The window, end minus start, wall clock: 01:13:30Z to 15:41:22.708Z is
     867.88 minutes. It includes the operator's idle time between dispatches,
     which is the reading `docs/dogfood/README.md` asks two passes to be
     compared on. It also includes a 305.3-minute interval in which no agent ran
     at all — see the quota row in the scoreboard, which is why the minutes and
     the turns move in different directions against the previous pass. -->
- Cost (USD): 555.83
<!-- A reconstruction, not a billed amount. The runtime exposes no cost
     accounting to an agent at all; that gap is #295's subject, filed from the
     2026-09-17 pass, and nothing about it changed here, so it is not raised
     again below.
     Method, followed rather than reinvented from `memory/pass-metrics-2026-09-17.md`:
     token usage summed per model over the orchestrating transcript plus every
     subagent transcript beside it, deduplicated by response id, restricted to
     the window, priced at list. 3,808 priced responses over 364 transcripts
     read, of which 40 carried a priced response inside the window.
     Totals behind it, as input / 5m cache write / 1h cache write / cache read /
     output:
       Opus 5      7,584 / 21,233,188 / 1,534,846 / 760,094,806 / 1,105,883 = $555.79
       Haiku 4.5      62 /     21,618 /         0 /     116,781 /       201 = $0.04
     List prices used, per million tokens: Opus 5 $5 in, $6.25 5m write, $10 1h
     write, $0.50 read, $25 out; Haiku 4.5 $1 / $1.25 / $2 / $0.10 / $5. The
     Opus row is the same table the two previous reports priced with, so the
     three passes are priced identically; the Haiku row is new here, is four
     cents, and is priced at that model's published list rather than left
     silently unpriced. Ten responses carry the model id `<synthetic>` and no
     usage block; they are excluded and named rather than dropped quietly.
     One coincidence a reader will notice and should not read anything into: the
     Opus totals land within 0.5% of 2026-09-19's on all five columns. The two
     passes made a near-identical number of priced Opus responses — 3,791 here
     against 3,807 there — at a similar context size, and cache reads dominate
     the figure in both. The windows are disjoint and the corpora are different
     records; the script that produces both is the same one.
     Two limits, both unchanged from the previous two passes: list price is not
     the price billed to a subscription account, and the figure excludes what
     the operator's own interactive turns cost outside this session. -->
- Transcripts: the orchestrating session transcript and the per-agent subagent transcripts beside it, under the Claude Code projects directory for this workspace, outside this repository

## Scoreboard

<!-- Rows 1 to 5 partition the sixteen pull requests that merged inside the
     window exactly — 5+7+2+1+1 = 16 — and are grouped by how many isolated
     review rounds each took, because that is the axis the issue asks the
     scoreboard to make visible: which landed clean and which landed with a
     defect a review found. Rows 6 to 9 are the pass's other cases: the two pull
     requests open at the close, the planning that opened it, a claim that held
     a queue with nobody working it, and the account limit that stopped
     everything for five hours.

     Rounds are counted from the verdict comments on each pull request, with
     `gh pr view N --json comments`, not from memory: a comment whose first line
     is the hidden `agentic-review: changes-requested` marker is a rejection, and
     one carrying the hidden `agentic-reviewed-sha` marker is the approval.

     Row 1, approved in one round (5): #415 (issue #382), #416 (#350),
       #434 (#427), #443 (#428), #446 (#417).
     Row 2, sent back once (7): #425 (#327), #426 (#299), #432 (#429),
       #438 (#435), #440 (#338), #441 (#439), #442 (#411).
     Row 3, sent back twice (2): #436 (#412), #437 (#422).
     Row 4, sent back three times (1): #431 (#423).
     Row 5, no isolated review (1): #447 (#312).
     Row 6, open at the close (2): #445 (#413), rejected once at 15:07:15Z and
       in its fix round; #449 (#448), opened at 15:15:45Z with no verdict yet.

     `exit` is the outcome of the land for the case: 0 where every pull request
     in the row merged inside this pass, 1 where it did not. `error class` is the
     blocker class, `none` for a row that ended clean.

     `tool calls` is the implementers' own count, summed over the issues in the
     row: `tool_use` blocks in the assistant messages of each implementer's
     transcript, counted inside the window and nowhere else, which is the rule
     `docs/dogfood/2026-09-19.md` set and this report follows unchanged. Per
     issue: #382 55, #350 44, #327 89, #427 85, #299 100, #429 112, #435 99,
     #412 152, #422 154, #423 171, #338 469, #428 124, #411 68, #439 114,
     #417 140, #312 55. Rows 1 to 5 sum to 2,031. No issue is counted twice.
     Three agents were still running at the close and are truncated at it
     rather than counted whole, and they are named because the column exists to
     be compared: #445's reviewer made 67 of its 74 calls in-window, #448's
     implementer 63 of 64, and this report's own implementer 3 of 103 — those
     three fall in the 16.44 seconds between its dispatch at 15:41:06.268Z and
     the bound, measured from its own transcript at 15:41:13.884Z, 15:41:15.168Z
     and 15:41:21.859Z. The first version of this clause ended "so it is the
     event the boundary is drawn at", which is the claim the paragraph above
     retracts — the boundary falls 16.44 seconds **after** that dispatch, not at
     it — and it was written inside the commit whose whole purpose was
     retracting it. That is the sharpest instance of this file's subject the
     file contains: a withdrawn claim reappearing inside its own correction,
     in the sentence nobody re-read against the paragraph it answers.
     The truncation costs row 6 one call and the reviewer total seven, and it
     costs rows 1 to 5 nothing: every implementer behind them had finished.
     #338's 469 is the pass's outlier and it is not a mis-count: that
     implementer ran seven rounds over seven hours and built two independent
     reproductions of one defect, which is finding D101 and the reason the
     defect was caught.

     Reviewers are not in the column, for the same reason as the previous
     report, and their count is here instead: sixteen reviewer agents made 1,277
     in-window tool calls, from 42 on #415 to 200 on #431, whose one transcript
     carried all three of that pull request's rounds. Sixteen agents over
     fifteen of the sixteen landings — #447 took the light path and had none —
     plus the one review of #445, which was open at the close.

     Two of the sixteen pull requests still carry `state:in-review` after
     merging, #425 and #446, read from `gh pr view N --json labels`; that is the
     half #388 recorded on 2026-09-19 and #421 now carries, measured twice more
     here without being looked for. The issues they closed carry a state label
     that never moved either — `gh issue view N --json labels` answers
     `state:in-progress` on the closed #327 and `state:in-review` on the closed
     #417 — and that half the previous pass ruled not work, because the contract
     says a closed issue is a done issue with nothing left to relabel. -->

| case | exit | error class | tool calls | decision | reason |
| --- | --- | --- | --- | --- | --- |
| claim, implement, isolated review, land — approved in one round | 0 | none | 448 | keep | five of the sixteen landings took exactly one review round; this is the pass's baseline and the number the next pass is compared against, and it is five of sixteen against 2026-09-19's five of sixteen and 2026-09-17's thirty-five of sixty-one |
| sent back once — three rejections on the code and four on what the pull request said about itself | 0 | claim-false-not-code | 1051 | fix | read from each verdict comment rather than inferred, and for six of the seven from its first blocking item: code in #432 (one added line flips a negative-control verdict), #440 (`gh()` spawns with no `maxBuffer`) and #441 (head reports a count nobody had); the change's own account of itself in #425 (the memory location is one unscoped global directory and the document does not say so), #426 (invariant 8) and #442 (a register sentence promising a timing the configuration does not provide); #438 is the seventh and the exception, because its blocking item is invariant 7 — a private repository and four of its SHAs named in a tracked file — while the finding this pass kept from it, the declining reason stated as a cost, was raised there as **non-blocking** and is written in the approval comment, which is why the row below links that one |
| sent back twice — a predicate narrowed correctly and described wrongly, and a parser diverging from the grammar it mirrors | 0 | predicate-vs-verdict | 306 | fix | #436 proved a monotone narrowing and wrote it into the decision register as a claim about the check, which a differential falsified end to end (D88); #437 triplicated a grammar under invariant 10 and all three copies shared one misreading of the document they mirror (D91), then moved the reviewed commit under the review by amending it (D93) |
| sent back three times — the same substitution of a true-sounding predicate for the one the criterion names, five times inside one fix | 0 | predicate-substituted | 171 | fix | #423's sweep exists because `adopt --pr` reports a gap as acted on, and its fix reported a gap as carried from the gap's category rather than from the diff, then repeated the shape in a premise left standing in two of three sites, in a type claim, and in a documented example no run can produce, waved through by a pin that greps for a token (D87, D92); the round count is the finding, not the three rounds |
| landed with no isolated review, on the light path | 0 | no-isolated-review | 55 | keep | #447 is a prose-only diff with no gate, parser or fail-open path in it, verified by the orchestrator against the qualifier that makes that safe, and it is the only one of the sixteen with no `review:approved` label — the verification is posted on the pull request rather than inferred |
| open at the window close, one in a fix round and one unreviewed | 1 | in-review-at-cutoff | 228 | keep | #445 had been rejected at 15:07:15Z on a required check getting weaker while three artefacts said it got stricter, and #449 was twenty-five minutes old, opened at 15:15:45Z against a close of 15:41:22Z; a case that straddles the boundary carries the cost it had spent and no outcome, which is what the boundary means |
| sequencing and drafting M18 — one agent deriving the collision set, then two drafting in parallel | 0 | planning-collision | 255 | keep | the two parallel drafting passes each measured overlap against the open issues and neither could see the other's drafts, so four of the second pass's five sweeps collided with the first pass's four; `ci/issue-lint.mts` refused the three that survived to a claim, which is the right refusal at the wrong time (D76) |
| a claim that held the milestone's critical path with nobody working it | 1 | claim-without-dispatch | 0 | fix | #412 was claimed at 03:09:07Z — branch pushed, worktree created, `state:in-progress` at 03:09:19Z — and no implementer was dispatched for it until 03:59:41Z, 50 minutes in which the chain the orchestrator counted behind it at the time — #411, then #413 and #414, then four more sweeps — could not be claimed, and every artefact the loop reads said work was under way; the count is the orchestrator's own reading of that moment and the tree cannot settle it now, because those bodies moved during the pass |
| an account-level weekly limit stopped every agent at once | 1 | rate-limit | 0 | fix | no subagent transcript recorded a single event between 06:34:38Z and 11:39:55Z, a 305.3-minute gap, and eight transcripts have their last event before it and their next after it; nothing in the repository recorded the outage and four of the eight claims looked, from outside, exactly like four claims in progress |

## Findings

<!-- Every one of the pass's thirty-eight findings is carried. Thirty-seven rows:
     D73, D86, D88, D92 and D101 each fold their `turn 2` into the parent row,
     and D79 and D89 share one row because D89 is the measurement that falsified
     D79 — putting them apart would reproduce the defect they are about.
     Nothing is left out. What this table does not cover, and no reader should
     look for here, is the eight pull requests that merged between the previous
     report's close at 2026-09-19T17:05:15Z and this window's open — #375, #396,
     #399, #402, #404, #406, #407 and #408, counted with the same
     `gh pr list --state merged --json number,mergedAt` filter as the sixteen.
     They belong to the 2026-09-19 pass's tail and no report claims them.

     `origin` is copied verbatim into the `Origin:` line of the issue a row
     names. Two rows resolve to an issue filed during this pass, #444 and #448,
     and both origin cells were read back from the issue's own line with
     `gh issue view N --json body -q .body | grep '^Origin:'` rather than copied
     from the pass's notes: two rows, two agreeing word for word, zero
     disagreeing. Every other row resolves to a merged pull request, to an issue
     filed before this pass, or to a written reason; no mechanical check reaches
     any of those, and each was read against the artefact it names.

     Where a finding already lives in a review comment or an issue body, the row
     links it. The report is the consolidation and not the origin: seventeen
     rows link a comment on the pull request they came from, and sixteen rows
     have an isolated review as their origin. For two of those sixteen there is
     no review comment carrying the finding to link — the verdict on #443 is 133
     bytes long and says only "approved", and the round on #441 that raised its
     item was never posted as a comment at all, so the implementer's reply and
     the approval are the only durable trace. That is D68 of the previous pass,
     a verdict that exists nowhere a later reader can read it, measured twice
     more here without being looked for. A third row points at #448's body
     instead, because that is where its origin line lives.

     **Three claims of the scratchpad did not survive contact with the
     repository and are corrected in the row rather than in a note:**
     D77's mechanism (no agent had died; none was ever dispatched), D96's count
     (eight agents, not five) and D100's interval (PR #436 landed the item 77
     seconds before the implementer's first tool call, not an hour before). A
     fourth, D79's whole consequence, was already corrected inside the pass by
     D89 and is carried as the pair.

     **A fourth was claimed and is withdrawn, and that is the more useful
     entry.** This report first recorded D110's outcome as falsified — "the open
     issue it hands its lesson to does not exist" — and the isolated review of
     the pull request carrying this file falsified the falsification: the issue
     is #413, open, named as the origin of the row above it. The method that
     found the three above kept running after the writer stopped, which is what
     it is for, and the error it caught was made the same way as the three: a
     `grep` for the phrase "declaration form" was run and its empty result was
     read as an answer about the thing. Three stand, one was wrong, and a sweep
     that reports its own hit rate without reporting its own error rate is the
     defect this table is otherwise about.

     Two measurements in the rows below cannot be re-run and are marked as read
     from the pass rather than from the tree: D82's five collisions, because the
     grant that caused them went away when #299 closed with PR #426, and the
     scratchpad entry count in D108, which was 1,102 when the finding was written
     and is 1,139 now. -->

| finding | origin | outcome |
| --- | --- | --- |
| a script deriving issue collisions from every backticked bullet of a body printed nonsense — `#412` holding a file against itself, fourteen holders for `docs/workflow.md` where `ci/issue-lint.mts` reports six — because a body's bullets are not all `## Files` bullets; and the same mistake was made again three hours later on #338's grant, with a `gh issue list --json body` read filtered by a jq `test("issue-lint.yml")` regex over the whole body, asserting that two issues declared a workflow path that neither declares, corrected by the implementer running the lint (`ok: true`, `failures: []`, `compared: 25`) | orchestration 2026-09-20, sequencing M18 and granting #338 | not work as code: the rule is already written as CLAUDE.md invariant 10 and the remedy is to ask the gate — running `ci/issue-lint.mts` once per issue is ninety seconds slower and is the only answer that matches the gate that will refuse the dispatch; the grant prose on #338 was corrected in the issue body before the implementer acted on it |
| the disjointness lint reads a `Blocked by:` edge as symmetric and `scripts/claim.mts` reads the same edge as a direction, so the side it is written on decides which issue may run; and `ok: true` from the lint does not mean claimable at all — #298 and #295 both linted clean and both were refused by edges predating the pass | orchestration 2026-09-20, ordering #411 against #412 and #327 and #350 against their collisions | not work as code, and the orchestrate card is where it belongs: both gates are right about their own question, and what is missing is the sentence saying that "what can run now" has to ask both — #418 is the card sweep and its criteria do not yet name it, so this row is the measurement that sentence would cite |
| every issue the orchestrator filed from a finding this pass was created with a plain `gh issue create`, which reaches neither `scripts/create-subissue.mts` nor the lint that script exists to enforce: #395 and #388 went out with a `## Dependencies` heading and nothing under it, which reads as complete to a person and is a refusal to the parser, and surfaced only when a milestone scan returned `ok: false` with a failure naming no colliding issue | orchestration 2026-09-20, scanning M18 for what could be dispatched | already corrected in place during the pass — both issues now carry a `Blocked by:` line, read back from their bodies — and the general shape is that the gate guards the label and the dispatch but not the filing; the durable remedy is the orchestrator's own filing path, which #418's card sweep is the place for and does not yet name |
| two drafting passes run in parallel measured their file unions against the open issues and could not measure against each other's drafts, which existed only as files in a scratchpad; four of the second pass's five sweeps collided with the first pass's four, and the lint caught the three that reached a claim (#421 against #417 and #418, #422 against #418) after eighteen issues had already been closed as absorbed | orchestration 2026-09-20, drafting the M18 sweeps | not work as code: the cost was ordering rather than rework, because the second drafter found it itself by reading the shared scratchpad directory, and the general shape — two agents planning one milestone hold the same lock and the loop's only lock is a pushed branch, which neither has — is a planning rule and not a mechanism change |
| a claim held the head of the milestone's critical path for 50 minutes with nobody working it, and every artefact the loop reads said otherwise: the branch is the lock and does not know whether anything holds it, `state:in-progress` is written at claim time and never re-asserted, the worktree is a directory, and `reconcile` reports `commitsAheadOfMain` and `unpushed` as zero, which is indistinguishable from a claim made a minute ago | orchestration 2026-09-20, #412 claimed at 03:09:07Z and dispatched at 03:59:41Z | #421, whose title and whose D25 origin line name a claim whose agent is gone — **and the scratchpad's account of the mechanism is wrong and is corrected here rather than carried**: it records an implementer that stopped, and the session transcript holds no `Agent` dispatch for #412 between the claim and 03:59:41Z, so no agent ever started, the detector #421 asks for would not have fired, and the signal it needs is a claim with no dispatch and not a claim with a dead agent |
| an account-level weekly limit stopped every running agent inside the same seconds and nothing in the repository recorded it; recovery was cheap only because a killed agent's transcript survives and each was resumed with its context rather than re-briefed, at a cost of five short messages, while the one claim that was genuinely stalled looked exactly like the four that were fine | orchestration 2026-09-20, the outage between 06:34:38Z and 11:39:55Z | #421 for the detector, and the scratchpad's count is corrected here: it says five agents died and the transcripts say **eight** — four implementers (#423, #439, #338, #422) and four reviewers (of #436, #431, #437, #440) each have their last event between 06:04:57Z and 06:34:38Z and their next between 11:39:55Z and 12:07:54Z, across a 305.3-minute interval in which no subagent transcript recorded a single event |
| a session scratchpad presented as session-scoped is shared, and two agents collided in it twice within an hour over generic top-level names — one found its draft pull-request body overwritten, the other found `pr-body.md` replaced by a body whose first line read `Closes #312` for another pull request; an unguarded push of either would have put one pull request's plausible-looking prose on another, where `scope` reads the body for linked issues and `land.mts` squashes it into `main` | the #417 and #413 implementers, 2026-09-20, reported independently | already covered by #351, which was closed at 03:55:00Z by absorption into #417 — a comment reading "Absorbed into #417" at 03:54:59Z, not a landing — and #417's sweep then merged as PR #446 at 15:00:36Z, second-to-last of the sixteen; the first version of this row gave the absorption time as the sweep's landing, which is the same conflation of two events the rows below are about — the fourth recorded instance of that defect class and the first two involving an artefact that gets pushed; both agents adopted per-issue filenames unprompted, and the durable half is the card's own rule, re-read the source of truth immediately before the action that depends on it, which is what caught both |
| an `authorised:` grant is read by the gate that refuses two issues for naming the same file, so widening a claimed issue's reach narrows every sibling's: granting `skills/orchestrate/SKILL.md` on #299 turned five issues `ok: false` in the same moment (#418, #421, #424, #371, #305), all five of them blocked by something else so that the collision was real and unreachable | orchestration 2026-09-20, granting the card edit on #299 | not work as code, accepted with the cost stated: the grant is the orchestrator's one instrument for a case the issue author did not foresee and the price is a property of which file, not of grants — #423's grant the same day cost nothing because no open issue declared that path; the measurement cannot be re-run, since the grant went away when #299 closed with PR #426 |
| a grant was widened in a comment on the issue, where `ci/lib/scope.mts` does not look: it reads `authorised:` lines from the `## Files` section of the issue **body** and nowhere else, so the path was in scope and the sentence saying why two call sites were covered existed nowhere a later reader or reviewer would find it | the #431 implementer, 2026-09-20, re-reading #423's body | already fixed in the body of #423 during the pass, marked as widened and dated: a grant has two audiences and only one of them is a parser, and the rule taken is that a decision changing what a gate permits goes in the artefact the gate reads, with a comment allowed only to point at it |
| a mid-session instruction to append attribution trailers arrived while a standing instruction disabled them, and the implementer resolved neither way silently: it named the conflict, gave its reason — this repository squash-merges with `COMMIT_MESSAGES`, so branch messages land on `main` verbatim — and said what would follow if the decision went the other way | the #423 implementer, 2026-09-20 | already covered by #395, which asks the hook to refuse a commit carrying an attribution trailer; recorded as a positive finding because the available failure was silent resolution in either direction, and the more likely one is omitting them without noticing there was a conflict at all |
| the four NUL bytes in tracked files were written by spelling the six-character escape for U+0000 into an editing tool, which decodes it on the way in and stores the raw byte, while the Bash tool refuses the same six characters outright with a control-character validator — one agent, two tools, two answers about the same text in the same minute, and the pin's own red commit carried a NUL planted by the pin's own constant | the #350 implementer, PR #416 (2026-09-20), disclosed rather than rewritten out of history | already covered by PR #416, which replaced the constant with `String.fromCharCode(0)` and landed the pin; the environment's answer depends on which door you ask through and only one door says it has an opinion |
| GitHub returns caret notation where the escape was sent, twice, by two routes including `gh api -X PATCH --input` with a correctly JSON-encoded payload — and the pass then wrote into #427 and #435 that GitHub "double-decodes it into a NUL and then sanitises it" and that a squash message would "very plausibly land corrupted", which the experiment falsified: four placements, each squashed, each yielding the six bytes on `main`, the rewrite living only in the JSON the API hands back | the #350 implementer, PR #416 (2026-09-20); falsified by the #435 implementer, PR #438 | already covered by PR #438, which landed the measurement and the negative result — an observation about a representation was recorded as a fact about storage, a consequence was derived from it and hedged prose is still an assertion once it is in an issue body, and the guessed failure mode was wrong too, since `git commit -F` exits 128 and writes no commit object rather than truncating one |
| a pull request declined an experiment and recorded a **cost** as its reason — "it would mean writing such a value into a repository to see what comes out" — which is exactly what its own four experiments did, in a repository created for it; the benefit clause does the whole job and carries its caveat inside it, where a cost clause reads as "we could have and chose not to" and decays into the question being settled | [the approval of PR #438](https://github.com/marcelusfernandes/agentic-setup/pull/438#issuecomment-5748096742) (#435), 2026-09-20, which is where it is written: the verdict comment blocks on invariant 7 and does not raise it | already covered by PR #438, landed with the finding recorded rather than sent back, because the header was accurate as written and the finding is about which true sentence to choose: when declining a measurement, record the benefit argument, because the cost is about you and the benefit is about the claim, and the claim is what gets quoted |
| an issue body asserted that no mechanism existed for a durable memory location and the implementer had already drafted one before finding the `memory: user` frontmatter field, which shipped before the issue was written; the hook already permitted the location, because its deny branch only fires inside the main checkout, so the remaining work was to name what already worked | the #327 implementer, PR #425 (2026-09-20), which re-fetched the vendor page because the claim was load-bearing | already covered by PR #425 and carried to #419 as a criterion: the superseded sentence — #266's "a durable memory has to be given a path in the main checkout" — was false on its date rather than overtaken, and `tests/doctrine.test.mts` pinned it by regex, so a pin was holding a statement that was never true and would have failed the day someone corrected it |
| an orchestrator ruling that a decision item was not owed quoted one of the register's three tests and read as thorough; the isolated review applied all three and two of them say decision, and the counter-example to the ruling was an issue the orchestrator had filed an hour earlier from the same implementer's report | [the isolated review of PR #425](https://github.com/marcelusfernandes/agentic-setup/pull/425#issuecomment-5747616925) (#327), 2026-09-20 | already covered by #430, the issue that decides the tiebreaker, with the item itself deferred to #419 because the register's same-PR rule could not be satisfied while `docs/decisions/README.md` was held by an in-flight claim; a ruling that quotes one clause of a rule looks exactly like a ruling that applied it, and length is not evidence of coverage |
| a review's demonstration of a real defect held only in the layout the tests build and not in the one `scripts/worktree-create.mts` produces — an empty HOME makes the memory path relative, and with the worktree nested inside the checkout the round-1 guard already caught it — and the same round's throw-site demonstration ran against a modified copy of the hook, which is inspection and not a probe of the shipped script | [the isolated review of PR #425](https://github.com/marcelusfernandes/agentic-setup/pull/425#issuecomment-5747616925) (#327), 2026-09-20, and the implementer checking it rather than obeying it | already covered by PR #425, whose new cases use the layout that ships and whose body says plainly that the `try`/`catch` is not covered: a demonstration against an edited copy proves something about the shape of the code and nothing about what the code does, and the labelling has to survive into the pull request or the next reader sees a fix with a story and assumes a test |
| an issue's `## Files` named every module that renders a decision and not the one that emits it, so two criteria and the `## Proof` could not be satisfied — and the type system made the wall silent, because a field added to the record must be optional or `npm run check` fails, and an optional field the literal never sets never reaches `JSON.stringify` | the #423 implementer, PR #431 (2026-09-20), reporting the wall rather than routing around it | granted at no cost, since no open issue declared that path, and the reusable half is the implementer's refusal of the one in-scope route — retyping the elements of `accepted` and `declined` from strings to objects would have typechecked, passed `scope` and the suite, satisfied the criterion and broken a published JSON shape for every consumer |
| the fix for "a gap reported as acted on" decided `carried` from whether the gap is a file-gap rather than from the plan's files, reproduced end to end with the real script: with the generated workflows already on `main` every workflow file plans as `skipped (unchanged)`, no commit touches that directory, and the JSON, the pull-request body and the plan-issue comment all say `carried` fourteen lines below a file list saying it carries nothing | [the isolated review of PR #431](https://github.com/marcelusfernandes/agentic-setup/pull/431#issuecomment-5747887688) (#423), round 1, 2026-09-20 | already covered by PR #431; two details make it worth more than the bug — `docs/adopt-pr.md` and #403's criterion both say "in the diff" so the document was right and the code wrong, and the fixture that would have caught it already existed in the file this pull request moved it into, with the assertion facing the other way |
| the same substitution recurred inside the same fix until the implementer named the pattern itself — a true-sounding predicate in place of the one the criterion names — reaching a documented JSON example showing a `ticks` entry for a gap the same object lists under `declined`, which no run can produce, protected by a pin that greps for `"state": "carried"` and is satisfied by pasting a fabricated row into the fence | [the isolated review of PR #431](https://github.com/marcelusfernandes/agentic-setup/pull/431#issuecomment-5748023550) (#423), round 2, 2026-09-20 | already covered by PR #431, whose pin now asserts the example is producible; the count is the implementer's rather than the orchestrator's, recounted from the tree as five rather than four, because a false premise corrected in one of three sites — two of them inside lines that round's diff touched — is a correction that ended with an edit instead of a search, and "does not import" is not invariant 10's property, "can disagree" is |
| an implementer ranked its own verification below the reviewer's on the ground that the trees being identical is a claim about its own work rather than an independent check, and in the same message predicted the false positive a second reader would hit with a reasonable method — three hits on a token that three refusal cases share — and spent a paragraph stopping it becoming a finding that cost a round | the #423 implementer, PR #431 (2026-09-20), volunteered unasked | not work: it is the habit the rest of this table is the absence of, recorded as a positive because every other false claim this pass produced was evidence taken at its conclusion rather than at its provenance, and here the provenance was volunteered by the party it counted against |
| a careful verification that nothing was lost when test material moved — every `check(` assertion body extracted by balanced-paren parsing, 71 of 71 present at head against base — could not see three pins the pull request itself had created and then deleted, because such a pin is absent from both sides of a base-to-head comparison and absence from both sides reads as agreement | [the isolated review of PR #431](https://github.com/marcelusfernandes/agentic-setup/pull/431#issuecomment-5749680405) (#423), which found them by diffing the case-name sets between the two round heads | already covered by PR #431, and the general rule is the window rather than the method: base-to-head answers "did this pull request lose something the repository had" and head-to-head answers "did this round lose something the last round added", a multi-round pull request needs both, and by round three most of what is at risk was written by the pull request |
| a fix for two note-loss paths moved part of an open issue's subject outside the discriminator that issue's own fix can use: a whitespace-led continuation line carries no `note ` marker, and #428's only workable mitigation keys on exactly that marker, so a change that passes `scope`, the suite and the negative control made an open issue's criteria wrong with no gate reading open issues against a diff | [the isolated review of PR #432](https://github.com/marcelusfernandes/agentic-setup/pull/432#issuecomment-5747745773) (#429), 2026-09-20, which built the verdict flip out of one added line | already covered by PR #432, which took the stronger remedy — continuations print with the marker preserved — rather than the disclosure the review asked for, keeping the whole exposure inside one discriminator so #428 could close it in one move; the review's flip was measured as two real runner logs over the same fixtures differing by one line, replayed through the check's own predicates |
| a reviewer's non-blocking remarks are adopted on the reviewer's word where its blocking items are re-derived from the code, and three of four were wrong: an interleaving claim that `spawnSync`'s two buffers make impossible, a type claim that does not survive this repository's pinned TypeScript, and a detail-cap number that is seven rather than six because the error header is prepended when it sits above the cut | [the isolated review of PR #432](https://github.com/marcelusfernandes/agentic-setup/pull/432#issuecomment-5747745773) (#429), round 2 opening by retracting round 1, and the implementer's own re-measurement | already covered by PR #432: the first false sentence had been written verbatim into the normative header of the file the pull request was about, in a commit whose stated purpose was removing two header over-claims, and the third was found only because the instruction "you accepted this one on its word" was answered by enumerating everything else accepted the same way rather than by apologising |
| a monotonicity proof about a function was written into the decision register as a claim about the check — "no run that reported `pass` before reports anything else" — and a differential over the real script falsified it: a run vouched by a `test(red):` commit reports `pass` and exit 0 on the base and `unattributed` and exit 1 at head, because `structural` is read in three places and narrowing a predicate is not monotone in a verdict that reads it in a guard | [the isolated review of PR #436](https://github.com/marcelusfernandes/agentic-setup/pull/436#issuecomment-5747932600) (#412), 2026-09-20, run over one throwaway repository per outcome | already covered by PR #436 and its rewrite of `docs/decisions/0034`, whose `## Reason` then overstated once more in the same direction — a mention-only run with no contradicting owner reports `structural` on the base and `pass` at head, which the item's own "Cost accepted" section concedes two sections later; both were caught the same way, by running both versions over a constructed case, which is the only reader a claim about behaviour across two versions has |
| three copies of a grammar, written under invariant 10 so that they could disagree, shared one misreading of the document they mirror: all three `bulletsOf` copies test `line.trim().startsWith('- ')`, so a nested sub-bullet becomes a new top-level bullet claiming the issue it names, and a closeout following the documented grammar is refused | [the isolated review of PR #437](https://github.com/marcelusfernandes/agentic-setup/pull/437#issuecomment-5747998909) (#422), probing the parser against the README sentence by sentence | already covered by PR #437, and the bound belongs in the invariant rather than against it: duplication makes disagreement possible and possibility is not detection, it protects against one copy drifting from another and does nothing about every copy drifting from the document, which is the direction that happened — what detects is a pin over behaviour, and none of the three copies had one |
| an implementer amended the docs commit a reviewer had read and force-pushed with a lease, and the reviewed oid left the branch's history, so "what changed since the review" could no longer be computed from parentage — recoverable only because `git cat-file -t` on the reviewed oid still returned `commit`, checked before the delta was asked for | the #422 implementer, PR #437 (2026-09-20) | not work as code, and the rule is now in the implementer brief: a new commit can only add and an amend can change reviewed content without appearing as a commit at all, no gate in this repository reads a branch's history for rewrites, and the re-review brief had to say "compute the delta from the reviewed oid" because of this incident |
| a pull-request body scrupulous everywhere else — withdrawn claims struck through, corrections labelled, round one's overstatement stated plainly — silently edited "three pins" to "two pins" in a criterion, and the edit was in the direction that reduced the count of things the pull request had got wrong | [the isolated review of PR #437](https://github.com/marcelusfernandes/agentic-setup/pull/437#issuecomment-5748143215) (#422), round 2, which showed the conflation behind the number | already covered by PR #437, and the standard existed and held three times out of four in the same hours — #438's implementer marked its over-claim as the first version's and #436's struck through two withdrawn findings — so the rule is about form and not intent: if a claim in a body was believed and is no longer, say so, whichever direction it moves |
| a test reproducing a `maxBuffer` defect went red, the fix went in, the test went green, and the red was false: the local fake `gh` wrote with `console.log` and then `process.exit(0)`, so it truncated its own output at the 64 KiB pipe buffer long before the 1 MiB limit under test could bind — instrumenting the fake printed `stdout len 65536` with `status 0` | the #338 implementer, PR #440 (2026-09-20), which caught it because two reproductions of one defect disagreed after the fix | already covered by PR #440, which switched to `writeSync` on fd 1 and re-verified in both directions; nothing in the loop reads **why** a red is red, and `negative-control` is the same shape one level up and equally blind — a single reproduction can only tell you a test failed, and two that disagree tell you one of them is lying, which is strictly more information than either gives alone |
| an issue's Context argued a trap in the voice of a permanent fact — the highest decision number is not the highest-numbered file in the directory — and the implementer measured it false: PR #436 had landed item 0034 at 11:48:18Z, 77 seconds before that implementer's first tool call at 11:49:35Z, and closed the gap, so a directory-only derivation would have been right; and the same pull request reopens it, because its own item goes into the other file for exactly the reason the four older ones are there | the #411 implementer, PR #442 (2026-09-20), reporting its own work unprompted | already covered by PR #442, which pinned the property that survives either arrangement rather than a maximum, which is a fact with a shelf life: the two-file split is generated continuously by the `## Files` discipline, which is not a bug and will keep doing it, so a directory-only derivation will be wrong again at unpredictable intervals with nothing to announce it |
| a rule document was about to promise that a duplicate decision number is caught "as a rename before the second lands", and the reviewer read the configuration instead of the sentence: `test.yml` fires on `pull_request` with the default types, the `main` ruleset carries `strict_required_status_checks_policy: false`, and `land.mts` has no notion of a head behind its base — so the earlier green stands, `--auto` lands the duplicate, and the red surfaces on `main` afterwards | [the isolated review of PR #442](https://github.com/marcelusfernandes/agentic-setup/pull/442#issuecomment-5749877618) (#411), which built the race and ran it | already covered by PR #442, whose rewrite states the mechanism and names the precondition rather than promising a timing, and names the follow-up as its own issue; the change's own success creates the conditions — before it two concurrent decision pull requests were impossible, after it eighteen issues owe an item — so the sentence would have been wrong exactly when it started being consulted, in a document nothing tests |
| a pull request closing four ways a passing file's note can decide a verdict proved it with 26 fixtures over all nine outcomes, reproduced exactly by the reviewer, and every fixture was a **failing** file echoing literal text — so the marker pattern had never been run against what `printNotes` actually emits, two hand-copies agreeing with each other and neither compared to the source | isolated review of PR #443 (#428), 2026-09-20 | #444, whose origin line this cell is copied from and whose body is where the finding is written, because [the verdict comment on PR #443](https://github.com/marcelusfernandes/agentic-setup/pull/443#issuecomment-5750003687) is 133 bytes long and carries the approval and nothing else — the same shape D68 of the previous pass recorded, measured again here; the review built the missing case with head's real runner and a genuinely passing file, and the issue's first criterion spawns the real runner over a fixture directory and asserts the emitted line matches the pattern the check uses, with the shape written out rather than imported from either side — invariant 10 applied to the join between two files rather than to one file's contents, which is a shape this repository has not had a name for |
| an implementer turned the review's own mutation method on the rank it had just written, dropped the top tier, and all 24 checks passed — so the central sentence of its own header had nothing in the suite behind it, and the case the body advertised as covering it was a scratchpad measurement run once by hand and written up as a fixture | [the #439 implementer, PR #441](https://github.com/marcelusfernandes/agentic-setup/pull/441#issuecomment-5750048040) (2026-09-20), after the method was shown to it in a review comment rather than stated as a conclusion | already covered by PR #441, which made the case a fixture and measured it four ways, the last clause being the whole finding: red when the top tier is removed **and nothing else red**, which is what separates a pin for that tier from a case the tier happens to affect — a claim that was true and unsupported, the same gap as the grep-pin and the hand-copied fixtures reached from the other side |
| a reviewer rated a defect non-blocking on four good reasons — the code does what the header's rank table says, the precondition is a child violating the documented printing protocol, nothing in this repository does it, no measurement changes — and it was asked for anyway, because the header says "three things the rule still lets through" and there were four, the fourth being new rather than inherited | the orchestrator overriding a non-blocking call of a review round on PR #441 (#439), 2026-09-20, which was never posted as its own comment — [the implementer's reply](https://github.com/marcelusfernandes/agentic-setup/pull/441#issuecomment-5750279727) and the approval are its only durable trace, the same shape as the verdict on #443 | already covered by PR #441, which fixed it rather than disclosing it so the list stayed at three, and the general form has not been written down before: an unreachable defect in a list that claims completeness is a reachable defect in the list, because the code is fine until someone does the unlikely thing and the sentence is wrong immediately — "is this reachable" and "does the document now say something false" come apart exactly there |
| a review established a real safety property — a test file quoting a runner log cannot win the summary ranking — and recorded the wrong mechanism for it, indentation; the implementer read the two `console.*` calls in the harness and found it is the stream: the harness writes exactly one thing to stdout, and the runner never reads a summary from stderr, so a quoted log cannot reach the ranking at all | the #439 implementer, PR #441 (2026-09-20), checking the review rather than adopting it, and [the approval comment](https://github.com/marcelusfernandes/agentic-setup/pull/441#issuecomment-5750432973) recording that the review was wrong | already covered by PR #441, with the conclusion unchanged and the reason replaced: a correct conclusion with a wrong mechanism is the most durable error this loop produces, because nothing ever contradicts it and everyone reasoning *from* it inherits the error silently — the method is read the producer, not the product, which is the same instrument the note-fixture row needed |
| an issue recorded `tests/doctrine.test.mts` at 546 lines with 254 of headroom and added that the claim the file is full is out of date; PR #442 merged at 13:27 adding exactly those 254 lines, and the issue was claimed at 13:29, so the figure was correct for nine and a half hours and false for two minutes when an implementer read it — and #442 closed #411, which was this issue's own `Blocked by:` | isolated review of PR #446 (#417), 2026-09-20 — the 800-of-800 position | #448, whose origin line this cell is copied from; the figure was measured by the #417 implementer against the tree rather than re-read from the body, and re-reading the issue would not have helped, since the body was internally consistent and its note disclaimed an *older* staleness, which reads as diligence — and this is the general case, because `Blocked by:` is precisely a list of the changes most likely to move what an issue measured about a file it also declares |
| a parser change described as a tightening in a code comment, in a pull-request body and in a permanent register item is a **loosening**: stopping an inline code-span regex at a newline lets a closing keyword inside a span that wraps link an issue the base ignored, which unions that issue's globs into a required check's audited set — and the test cited as holding it pins prose that never broke, returning empty on the base too | [the isolated review of PR #445](https://github.com/marcelusfernandes/agentic-setup/pull/445#issuecomment-5750624459) (#413), 2026-09-20, which ran `parseLinkedIssues` on both checkouts over constructed prose | already covered by PR #445, open and in its fix round at the window close, so the outcome is the pull request rather than a merge: narrowing a character class reads as narrowing and following the blank through to the keyword match is a second step nobody took, a green test whose name matches the invariant made the claim feel discharged, and the register is the expensive copy because it is read by agents deciding what a check does instead of measuring |
| a replay of a parser change over all 178 merged pull-request bodies found the outputs identical either side, and it was evidence of nothing: no merged body carries a closing keyword inside a code span that wraps, so both versions agreed on every one — a corpus shows that no existing input distinguishes the versions, which is not that no input does | the #413 implementer, PR #445 (2026-09-20), whose own words are kept: "a corpus can only falsify; a parser change needs a case built from the rule it changed, not a replay of what has already been written" | not work as code, and it is the most portable result of the pass: the live corpus was used to establish inertness four times today — #440 over the repository's 31 open issues, #445 over 178 merged pull-request bodies, #443 over the merged set, #437 over the seventeen closeouts (the first version of this row called #440's corpus 31 pull requests; #440's body says "every open issue in the repository — 31") — and it is the right instrument for a verdict change and the wrong one for a rule change; the scratchpad hands its second half to "the open issue about a declaration form", and **that issue is #413** — open, in M18, and named as the origin of the row directly above, whose `(#374)` criteria ask exactly for it: a declaration distinguished from a mention, a form that cannot be produced by accident in ordinary prose, and a run of the new rule over the merged corpus before it lands. **The first version of this row said no such issue existed, and that was this report's own worst error**: `grep` over every issue body for the phrase "declaration form" returns nothing, because the phrase appears in no issue, open or closed — a search for the words answered as though it were a search for the thing, and an empty result says nothing about which of the two it ran. #374, where those criteria were written, is closed and absorbed into #413 |
