# Dogfood 2026-09-19 — the mechanism exercised three ways at once: the loop on this repository, a headless orchestrator run, and an end-to-end adopt run

<!-- Not a retroactive report. The pass ran long after this format landed, so no
     `Retroactive:` bullet and no `not recorded` cell: every number below is
     measured, and each states the command or the file it comes from at the
     point it is derived.

     What the pass was: three exercises of the same mechanism on one day.
     (1) The ordinary loop against this repository — `claim.mts`, a worktree per
     issue, an implementer, CI (test, scope, negative control), an isolated
     read-only reviewer returning a verdict, the orchestrator applying the
     label, `land.mts` — which merged sixteen pull requests.
     (2) A headless orchestrator run against a disposable repository, M10's last
     exit criterion, issue #345.
     (3) An end-to-end `adopt` run against another disposable repository, M13's
     last exit criterion, issue #344.

     The window the numbers measure: 2026-09-19T00:00:55.039Z to
     2026-09-19T17:05:15.268Z, the first and last timestamp of the two
     orchestrating sessions on that date. The close is not an arbitrary cut: it
     is the dispatch of this report's own implementer, the last event either
     session recorded before this file was written.

     Two orchestrating sessions, which is itself the reason the `Turns` bullet
     needs its note. The 2026-09-17 pass had one; this one had two, running
     concurrently on the same repository:
       453d878d-7f6a-45b0-8943-85b67e30bf6e  "[enceladus]"
       88efcdba-112f-4f8c-bad9-58b289d28a81  "[pendencias agentic]"
     The second opened at 01:11:58Z on the M10 and M13 exit criteria. It drove
     the disposable-repository runs, and the implementer and the review of one
     landing (PR #367) and of one pull request still open at the close (#375).
     It landed nothing in this repository: its own tool calls on that date carry
     no `land.mts` invocation at all — the nine matches on that string are
     prompts, skill arguments and memory writes — and its one `gh pr merge` was
     against the probe repository. The review it obtained said so at the time,
     recording that the approval and the merge waited on the operator because
     that session's own permission layer refused the merge step; the pull
     request merged twelve hours later.
     No duplicated GitHub write was found between the two sessions: of the 46
     issues and pull requests created on this date, three titles appear twice
     and each of the three is an issue beside its own pull request. That is the
     failure mode finding D5 of 2026-09-17 recorded and #301 asks the card
     to state, and this is a search that found nothing rather than a
     guarantee. -->

- Repository: marcelusfernandes/agentic-setup
<!-- The loop ran against this repository itself, by agents. The two disposable
     repositories the pass created carry their own scoreboard rows: the headless
     run's and the adopt run's, both named in the pull request under the
     `Delete after review:` line this document's README fixes. -->
- Commit: bccb04066e916249ea5b2e6256df8b710bff2b27
<!-- `git log origin/main --first-parent --until=2026-09-19T17:05:15Z -n 1`
     answers this sha: the tip `main` was at when the window closed, which is
     also the merge commit of PR #367, the last pull request to merge that day
     (14:14:51Z). Every claim this report makes about the tree is measured at
     this commit, never at `main`, and every command below is either pinned to
     it or restricted to paths that exclude this file. -->
- Turns: 631
<!-- Unique assistant response ids across BOTH orchestrating transcripts dated
     2026-09-19, subagents excluded. That is the same unit as the 2026-09-17
     report's 775, counted over a different population: 775 was one session and
     631 is two, 512 in "[enceladus]" and 119 in "[pendencias agentic]". A
     reader wanting the strictly like-for-like number against a single
     orchestrator should use 512.
     The method was checked before it was used: run against 2026-09-17 with that
     report's own cutoff it answers 775 exactly, and its window answers 954
     minutes exactly. A unit that reproduces the previous pass to the digit is
     the only reason the comparison below is worth anything.
     Sixteen pull requests merged. Against 2026-09-17's 775 turns over 61, that
     is 39.4 orchestrator turns per landing against 12.7 — three times dearer
     per pull request, on work that was almost entirely mechanism changes with
     heavy contention on shared documents, where the previous pass's 61 included
     35 one-round approvals. The two numbers are comparable in unit and not in
     kind, and the scoreboard is where the difference is visible. -->
- Minutes: 1024
<!-- Last timestamp minus first timestamp of that date across the same two
     transcripts, wall clock: 00:00:55.039Z to 17:05:15.268Z is 1024.34 minutes.
     It includes the operator's idle time between dispatches, which is the
     honest reading of "how long did the pass take" and the reading
     `docs/dogfood/README.md` asks two passes to be compared on. The primary
     session alone gives the same two bounds. -->
- Cost (USD): 574.98
<!-- A reconstruction, not a billed amount, and still the one number in this
     header the runtime does not give the writer: an agent running inside Claude
     Code can read its token counts and has no cost accounting exposed to it at
     all. The 2026-09-17 report already carries that as a finding of its own,
     resolved to #295, and nothing about it changed here, so it is not raised
     again below.
     How it was derived, the method of `memory/pass-metrics-2026-09-17.md`
     followed rather than reinvented: token usage summed per model over both
     orchestrating transcripts plus every subagent transcript under both
     sessions' subagents directories, deduplicated by response id, then priced
     at the list prices published at platform.claude.com on 2026-09-17 — the
     same table, so the two passes are priced identically. 3,928 priced
     responses over 310 transcript files.
     Totals behind it, as input / 5m cache write / 1h cache write / cache read /
     output:
       Opus 5       7,610 / 21,030,130 / 1,382,745 / 759,736,484 / 1,104,303 = $552.78
       Fable 5.1      276 /          0 /   626,245 /  21,197,592 /    87,427 = $22.20
     List prices used, per million tokens: Opus 5 $5 in, $6.25 5m write, $10 1h
     write, $0.50 read, $25 out; Fable 5.1 $10 / $12.50 / $20 / $0.25 / $50.
     The single-session reading, for the reader who took 512 turns above, is
     $538.12.
     Run against 2026-09-17 the same script answers $743.16 where that report
     states $742.59 — the Fable and Sonnet totals reproduce to the token and
     Opus is twelve responses high, 0.08%. The difference is stated rather than
     tuned away.
     Two limits, both unchanged from the previous pass: list price is not the
     price billed to a subscription account, and the figure excludes what the
     operator's own interactive turns cost outside these sessions.
     One more measurement, which has no bullet of its own because the format
     carries six: the two orchestrators made 538 tool calls in the window. The
     scoreboard's `tool calls` column counts the implementers instead. -->
- Transcripts: the two orchestrating session transcripts and the per-agent subagent transcripts beside them, under the Claude Code projects directory for this workspace; the headless run's own stream at `agentic-probes/headless-run.jsonl` and the adopt run's captured log at `agentic-probes/adopt-run.log`, all outside this repository

## Scoreboard

<!-- One row per case. For the loop, a case is a group of pull requests that
     exercised the same thing, the way the 2026-09-17 report grouped sixty-one
     into eleven rows. Rows 1 to 9 partition the sixteen pull requests that
     merged on 2026-09-19 exactly — 5 + 2 + 1 + 2 + 2 + 1 + 1 + 1 + 1 = 16 — row
     10 is the two still open when the window closed, and rows 11 to 14 are the
     four disposable-repository runs.

     The sixteen, verified with `gh pr list -R marcelusfernandes/agentic-setup
     --state merged --json number,title,mergedAt,mergeCommit` filtered to
     2026-09-19 UTC, never from memory, with the issue each one closes read from
     its own body:
       342->238  349->330  346->316  360->239  364->332  358->336  376->356
       369->308  377->354  384->355  348->339  385->310  393->352  389->297
       394->357  367->366
     Issue #398, which asks for this report, says fifteen. `gh` says sixteen and
     the sixteen are listed above. The provenance of the disagreement is known
     and worth stating rather than leaving as a discrepancy: fifteen was the
     orchestrator's own count, run once as a filter and then quoted from memory
     twice, and it is not a reading of the data anyone can reproduce. Sixteen is
     what the query answers. This report is held to the query.

     The pull requests behind each row, so the grouping can be checked:
      1. 367 376 384 393 394
      2. 342 349
      3. 385
      4. 346 369
      5. 360 377
      6. 358
      7. 348
      8. 389
      9. 364
     Row 10 is #396 (issue #368) and #375 (issue #373).

     How to read the three numbers:
     - `exit` is the outcome of the land for the case: 0 where every pull
       request in the row merged inside this pass, 1 where it did not.
     - `error class` is the blocker class, `none` for a row that ended clean.
     - `tool calls` is the implementers' own count, summed over the issues in
       the row: `tool_use` blocks in the assistant messages of each
       implementer's transcript. The rule, the same one the previous pass
       applied: a round counts when it *started* inside the window, and it is
       then counted in full. That rule decides row 2 — no implementer round for
       #342 started inside the window, because it was implemented on 2026-09-18
       and only its closing review round is in this pass, so its contribution is
       0 rather than absent. Rows 1 to 9 sum to 2,004.
       Per issue, rounds started in the window: #330 17, #316 49, #239 185,
       #336 83, #332 132, #308 184, #354 199, #356 197, #355 135, #297 173,
       #310 195, #339 82, #352 120, #357 176, #366 77, #368 264, #373 166
       (two rounds, 100 and 66). No issue is counted in two rows.
     - Rows 11, 12 and 14 count the same unit — `tool_use` blocks — read from
       the probe runs' own transcripts. Row 13 cannot: that adopt run had no
       agent of its own, it was six commands driven from one script, so its
       number is the count of `adopt.mts` invocations the captured log records,
       six steps plus the `--pr` re-run after the plan was decided. That is a
       different unit and it is named here rather than left to look like the
       others.
     - Row 14 is the one row whose round outlived the window, and the same rule
       that gives #342 a 0 gives it its full 33: its agent began at 14:17:27Z,
       inside the window, and ran to 19:11:28Z, so it is counted whole. Ten of
       those 33 calls fall after the close and they are the only calls in this
       column that do. Its *result* is post-window evidence as well — the run
       reported after this file was first written — and every clause of that row
       drawn from the report rather than from the state at 17:05:15Z is marked
       `after the window:` in the finding rows it produced, the convention
       `docs/dogfood/2026-09-17.md` set for exactly this. What was true at the
       close and needs no marker: the run had opened no pull request and created
       no issue in its repository.

     Review rounds are counted from the reviewer subagent transcripts, not from
     the verdict comments on GitHub, and the two disagree: twenty-five reviewer
     rounds ran inside the window and nineteen verdict comments were posted, so
     six rounds left no comment at all, because the orchestrator posts a verdict
     at landing rather than when it arrives. That gap is finding D68 and
     it is why row 4's holds and row 5's rejections are readable here and not on
     the pull requests. -->

| case | exit | error class | tool calls | decision | reason |
| --- | --- | --- | --- | --- | --- |
| claim, implement, isolated review, land — approved in one round | 0 | none | 705 | keep | five of the sixteen landings took exactly one review round, which is this pass's baseline and the number the next pass is compared against |
| closed out inside the window after a hold taken the day before | 0 | none | 17 | keep | two pull requests carried their rejection from 2026-09-18 and spent only a narrow closing round here, so the pass paid for the round and not for the finding |
| approved on substance, then held because a decision-register item asserted something false | 0 | register-claim-false | 195 | fix | item 0029 claimed the summary string it replaced had disappeared from the tree and the check still emitted it as the job summary's headline; the register is the one artefact in this repository that no test reads |
| approved on substance, then held for narrow documentation rounds | 0 | docs-round-after-approval | 233 | keep | one pull request took one follow-up round and one took two, each on prose the review could reach and no check can; the substance was never in question and the rounds were cheap |
| sent back once — a rule documented outside the issue's globs left stale | 0 | docs-not-equal-code | 384 | fix | the same shape the previous pass recorded six times: an implementer may not touch a document outside its `## Files`, so a rule stated elsewhere stays stale until a review finds it and the grant arrives a round late |
| sent back once — hygiene the required checks already state | 0 | hygiene | 83 | keep | a test file grew past the 800-line cap and made `scope` red at the head; it cost exactly one round and no judgement |
| rejected on a dependency, and re-running the check did not clear it | 0 | stale-rerun | 82 | fix | the blocker was cleared by another pull request landing, and re-running the failed check re-ran the merge commit the original event pinned, so it failed again with the old message; only a new event resolved it |
| rejected on the pull-request body, not on the code | 0 | body-widened-scope | 173 | fix | a closing keyword in ordinary prose linked a second issue, so the scope check audited the diff against the union of two issues' globs and silently gained six paths, including the one file the body said it was deliberately not touching |
| landed with no isolated review, on the light path | 0 | no-isolated-review | 132 | keep | a prose-only diff with no gate, parser or fail-open path in it was verified by the orchestrator against the qualifier that makes that safe — no issue number outside line 1 of the body and no file path that could widen scope |
| open and waiting on a verdict when the window closed | 1 | in-review-at-cutoff | 430 | keep | two implementers finished and opened their pull requests inside the window, one with its first review round still running at the close and one rejected once and in a fix round; a case that straddles the boundary carries the cost it had spent and no outcome, which is what the boundary means |
| the headless orchestrator run against a disposable repository | 0 | none | 132 | keep | 34 turns, 15.3 minutes, US$10.27 and four merged pull requests took a milestone of three lint-clean issues from ready to closed with no human action, and the run stopped with a summary naming the issue carrying `human:pending`, which is exactly what the criterion asked |
| the same headless run repeated on a correctly named private repository | 1 | plan-no-ruleset | 54 | fix | the setup completed and the run was never launched: on this account's plan a private repository serves no branch rules and no classic protection, both endpoints answering 403, so `land.mts` refuses every pull request there before it picks a mode — the rule that a pass repository is private makes the pass impossible |
| the end-to-end adopt run, run 1 | 1 | pr:stack-not-supported | 7 | fix | five of six documented steps exited 0 and wrote files, a hook, a label and an issue; the sixth refused because the repository's stack is `unknown`, which step 1 had already printed, so an untouched near-empty repository is exactly the input the last step refuses by design |
| the end-to-end adopt run, run 2 | 1 | ruleset:unreadable | 33 | fix | the sequence never started: on a private repository all six documented modes exit 1 at the inventory's ruleset read, stderr empty, before any of them writes anything, because the inventory reads the rules second and returns an error rather than a partial report and every mode needs it — so even the three that write only local files refuse; measured against a control on the same account and token, a public repository answering the same read with `[]` and exit 0 while an unrelated read of the private one succeeds, which makes it one endpoint and not authentication; zero hand-written steps were executed and the one attempted, making the repository public, was refused by the agent's own permission layer and not routed around, so this adds no second answer to the criterion but an environmental precondition that stands in front of it |

## Findings

<!-- `origin` is copied verbatim into the `Origin:` line of the issue a row
     names, so the two records can be compared mechanically. Where a row's
     outcome is a bare issue number, the origin cell below was read back from
     that issue's own `Origin:` line with
     `gh issue view <n> --json body -q .body | grep '^Origin:'` rather than
     copied from the pass's notes, because the two can differ and the issue is
     the artefact a later reader has. Run over every row that resolves to a bare
     issue number: nine rows, nine agreeing word for word, zero disagreeing.
     Four rows failed that check on the first pass and all four are corrected
     above, in the row rather than in a note. Two were wording — an origin cell
     that dropped the tail of the issue's own line — and two were worse and are
     the reason the check is run at all: they named #301 and #359 as the issue
     the finding became, when both were filed earlier from a different
     occurrence, so each is now a written reason naming the issue that covers it
     instead of a bare number claiming an origin the issue does not carry. -->
<!-- The rule the provenance check cannot reach is the one that matters most
     here: twenty-one of these thirty rows resolve to a written reason or to a
     merged pull request rather than to a bare issue number, and no mechanical
     check reaches any of them. They were read one by one against the artefact
     each names; the count is in the pull request.

     The nine findings of this pass are D62 to D70. D66 recurs five times and
     carries five rows rather than one, because each turn is a separate artefact
     and the sequence is the point: three decision-register items, a correction
     to a correction, and a grant, all in one night, and two of the five were
     the orchestrator's own. D67 carries two rows because it produced two
     issues with different subjects.

     The `finding` column was swept end to end before this file landed, not
     sampled: thirty-three descriptions, thirty-three checked, twenty-eight
     held, five false and corrected above in the row rather than in a note.
     Thirty were swept before the report was first pushed; the last three were
     written afterwards, directly from the run-2 report that arrived after the
     window, and checked against it and against the issue they resolve to as
     they were written. The fifth false one was found
     only on a second reading and is the one worth naming here, because it is
     this report making the mistake it is reporting: the row on the leftover
     repositories said three of five sit outside the name prefix when two do,
     and that the single search finds two of five when it finds three.
     Why it survived a first reading is worth more than the arithmetic. The
     numbers were not invented here; they were copied from #397, the issue the
     row resolves to, and believed *because they were written down there*. That
     is the same failure this report devotes five rows to — a claim that pulls
     the reader's way, in a document nobody executes — and it is the first time
     on this run that one has been caught propagating from one artefact into
     another rather than staying in the one that made it. The correction is on
     that issue too, so the next reader of either does not trip over it.
     The method and every one of the five are in the pull request, which is
     where `docs/dogfood/README.md` asks for them.

     Three rows resolve to "#397's widening" and the phrase is precise rather
     than loose, so it is worth saying what was checked. All three criteria
     exist and are written down on that issue; two of them — the `detail`
     promise and the document's own invocation instruction — were added in a
     **comment**, not into the `## Acceptance criteria` list in the issue body,
     which at the time this file was written still held five criteria naming
     neither. They are durable and they are readable, and an implementer
     dispatched on that issue reads the body. Recording it here because it is
     the same shape as the finding this report devotes five rows to, one
     artefact short of it: the arrangement exists, and it exists somewhere the
     consuming artefact does not look. Moving the two into the body is the
     orchestrator's to do and is named in this report's pull request. -->

| finding | origin | outcome |
| --- | --- | --- |
| a wildcard `authorised:` grant is refused when it matches nothing under a directory prefix that already exists, while an exact name that matches nothing is passed through as new, so a split cannot be authorised before the filenames are chosen | orchestration 2026-09-19, dispatching #297 | not work: the lint is right — a wildcard grant matching nothing is a blank cheque whose width nobody has read — and the cost is one round-trip per split, paid once on #352 and landed as PR #393 with the reason written into the issue as a note rather than as a grant |
| every gate-changing issue needs a row in the decision register's index, and the lint compares globs across open issues, so at most one such issue may hold a grant at a time | orchestration 2026-09-19, dispatching #297 and #310 together | already covered by #301, whose fourth criterion is that the orchestrate card says never to run two implementers that both append an item to the register, and what to do instead; that issue was filed from the 2026-09-17 pass, so this row is a second measurement of it and not the issue's origin |
| re-running a failed check re-runs the merge commit the original event pinned, so it answers whether the run was flaky and never whether it would pass now | orchestration 2026-09-19, unblocking PR #348 after #384 landed | not work: a `pull_request` run checks out the commit its event recorded and re-running it re-resolves nothing, which is the platform's documented behaviour rather than a defect of this repository; the remedy is a new event, and the server-side branch update the orchestrator used produced one at a cost of eight minutes of CI learned once |
| the deferred milestone is not a pool as wide as its issue count — 41 when this was written and 49 open at the window close — but a set of narrow queues, one per shared document, because the lint refuses to claim an issue whose globs another open issue holds | orchestration 2026-09-19, widening #379 | not work: the lint is protecting against the failure this run recorded three times, a clean merge invalidating a paragraph another pull request was written against; the remedy is fewer and larger documentation issues, one per file, which is a planning choice and not a change to the mechanism |
| a decision item disposed of the gap it left by naming a sweep issue as already widened to cover it, and the issue said nothing of the kind when the item landed | orchestration 2026-09-19, reading item 0028 against #379 | already covered during the pass: the item's sentence was corrected and #379 was widened to name the verdict, and the same reading found #379's own opening context stale — all four documents it said named nothing already named it |
| the next register item claimed nothing in the tree matched the string it replaced, and the check still emitted that string as its job summary headline, twice, untouched by the diff | review of PR #385 (2026-09-19), non-blocking finding 1 | already covered by PR #385, whose fix round corrected the sentence and recorded what was actually checked and how |
| the correction to that false claim was itself imprecise, because a count of a string in a document that quotes the string is self-referential and changes whenever the paragraph is edited | fix round on PR #385 (2026-09-19) | already covered by PR #385, which landed the stable form: the grep restricted to the code paths, so the command cannot see the prose about the code |
| an item verified sixteen claims with a properly scoped grep and stated the output of the sixteenth from memory, and that command prints nothing rather than the zero the sentence quoted | item 0030, written for PR #389 (2026-09-19) | already covered by PR #389, which replaced it with a command that was then actually run |
| the orchestrator praised a split boundary for being mechanically checkable, wrote both halves of the check into the grant, and did not run it; the second half was false by one case | implementation of #352 (PR #393, 2026-09-19), reported by the implementer | already covered by PR #393: the implementer reported the discrepancy rather than making the code fit the sentence, and the grant's justification now carries the correction inline with the true form |
| every gate passes on a commit that squash-merge discards, so a file crossed the line limit at merge with nothing measuring the commit that landed | implementation of #310 (PR #385, 2026-09-19), reported by the implementer | #386 |
| the scope check answers one question with two base references — changed files from the merge base, base line counts from the base tip — and on one pull request they differed by a whole pull request | implementation of #310 (PR #385, 2026-09-19), reported by the implementer | #387 |
| a verdict that arrives and is not posted until landing exists nowhere a later reviewer can read, so a second round rests on the delta alone | second review of PR #385 (2026-09-19), the one thing it could not do | not work as code: the marker comment already ties a verdict to the head it judged and `land.mts` refuses a stale one, so a verdict posted early and superseded is unambiguous rather than misleading; what is owed is the habit of posting when it arrives, and six unposted rounds of this pass — twenty-five reviewer rounds ran and nineteen verdict comments were posted — are the measured cost of not having it |
| a closing keyword in ordinary prose inside a pull request body widened the audited glob set by a whole issue, and the check prints the merged globs without ever naming the issues it merged them from | review of PR #389 (2026-09-19), the only blocking finding of the night | already covered by #359, filed from the review of PR #346 earlier the same day; this is the second occurrence and the only one anyone has measured on a live body, and the two things it argues before that issue's criteria are settled — that the offending sentence was true and useful, and that a warning naming the extra issue would have caught it at check time — are recorded here because the issue does not ask for either |
| the orchestrator wrote an instruction to the reviewer into an issue body, using the one channel the doctrine says carries no authority | review of PR #393 (2026-09-19), non-blocking finding under invariant 9 | not work: the reviewer flagged it, treated it as data, measured the counts itself and reached the same conclusion independently, which is the behaviour the doctrine asks for; what it cost was the orchestrator's own credibility on that sentence, and the fix is to write the measurement and leave the conclusion alone |
| a test case whose name quotes the structural signature makes the whole run look structural to the negative control | implementation of #297 (PR #389, 2026-09-19), reported by the implementer | #390 |
| the check hardcodes the proof directory while the runner honours a configured one | implementation of #297 (PR #389, 2026-09-19), reported as not reachable within that issue's scope | #391 |
| three files sit at exactly the line limit and the scope check reports none of them | implementation of #352 (2026-09-19), headroom table measured by the implementer and re-measured by the orchestrator | #392 |
| a third of the commits on the default branch already carry an attribution trailer that the repository's squash setting lands verbatim | implementation of #357 (PR #394, 2026-09-19), reported by the implementer against its own commits | #395 |
| the four documents that list the negative control's outcomes are missing its newest verdict, and the sweep issue filed for them named the wrong one: all four already name the verdict it asked for, and the one none of them names is the verdict that landed later the same day | implementation of #354 (PR #377, 2026-09-19), invariant-8 list handed back by the implementer | #379 |
| the state label does not follow the pull request, so an issue sat in progress with its pull request open and green, twice in one pass | orchestration 2026-09-19, two instances in one pass | #388 |
| a pass repository is required to be private and a private repository on this account's plan serves no branch rules and no classic protection, so the land step refuses every pull request there before it picks a mode | the 2026-09-19 pass, measured while setting up the runs for #344 and #345 | #397 |
| the headless run's three issues were written to be easy and disjoint, so its 8.5 orchestrator turns per pull request is not a like-for-like ratio against the attended pass's 12.7 | the headless orchestrator run of #345, 2026-09-19, the author's own bound on what it shows | not work: it is a statement of what the run does and does not prove, recorded so nobody reads more into the number than is there; the attended pass spent its turns on grants, sequencing and review rounds, and this run needed none because its issues were written so it would not |
| the headless run's closeout pull request merged with no review and no reviewed-sha marker | the headless orchestrator run of #345, 2026-09-19, the author's own bound on what it shows | already covered by #308, landed as PR #369 on the same day, after which the exemption reads the changed paths instead of the label |
| the headless run needed hands before it could start and none after: the repository made public, the ruleset created by hand because the script could not, and the bootstrap pull request merged by the owner | the headless orchestrator run of #345, 2026-09-19, the author's own bound on what it shows | #397 for the plan limit that forces the first two, and #373 for the script that could not create the ruleset; the criterion is about the loop running unattended and the setup is outside it, which is why the run answers positively and this row exists anyway |
| one command was denied in the headless run: a reviewer reaching for the suite at a pull request's head through a command beginning with a stashed-state read, which the installed deny list refuses | the headless orchestrator run of #345, 2026-09-19, the author's own bound on what it shows | not work: the deny list did exactly what it is for, the review still returned a verdict, and loosening a deny rule so a reviewer can phrase a command more conveniently is a worse trade than the one denial |
| closed issues keep the in-review state label after their pull request merges | the headless orchestrator run of #345, 2026-09-19, the author's own bound on what it shows | not work: the contract states it — a closing keyword closes the issue when its pull request merges and a closed issue is a done issue with nothing left to relabel — so the run observed the documented behaviour rather than a gap |
| the adopt run's first five steps wrote a record, three workflows, a hook and a deny list before anything read the plan the person is asked to decide | the M13 end-to-end adopt run, run 1, 2026-09-19 | #368, whose pull request reads the ticked boxes and records the decision on the plan issue, and which was open at the window close |
| the plan step creates only the pending half of the decision label, so the label its own refusal asks a person to apply does not exist in the adopted repository | the M13 end-to-end adopt run, run 1, 2026-09-19 | #366, landed as PR #367, the last pull request to merge in this pass |
| the inventory step reports an unreadable ruleset without naming the cause, and the cause is the account's plan rather than the repository | the M13 end-to-end adopt run, run 1, 2026-09-19 | #397, whose criteria make the printed advice for the private-plan path state the two real options instead of one that also refuses, and which is being widened from the required-check half to the precondition itself |
| the adopt document promises that the underlying command's own first line is reported alongside the error name so a caller can branch on the name and still show the cause, and for an unreadable ruleset there is none, because the JSON read discards both streams on failure | after the window: the M13 end-to-end adopt run, run 2, reported 2026-09-19 after this report's window closed | already covered by #397's widening, which is the issue that has to name this cause; the measured cost is that both runs had to reproduce the underlying call by hand to learn why the read failed, and run 1 had made the same complaint before run 2 met it again |
| the adopt document tells a reader to run the script from inside the repository being adopted and prints the command as a relative path, and both cannot hold when the plugin is checked out elsewhere: a reader following it literally gets a module-resolution failure | after the window: the M13 end-to-end adopt run, run 2, reported 2026-09-19 after this report's window closed | #397's widening, which carries it as its own criterion. Unlike the other two findings of that run this one asks for no change to the code at all: the script resolves its own root from `import.meta.url`, so an absolute path already works and the behaviour is right — it is the instruction that is wrong, which is why it is cheap to fix and easy to leave unfixed |
| six failing runs of a mode that writes wrote nothing: the fail-closed promise held exactly, leaving the working tree clean, no label created — not even the pending one run 1 did create — no issue opened and no branch pushed | after the window: the M13 end-to-end adopt run, run 2, reported 2026-09-19 after this report's window closed | not work, and the only finding of this pass that records something working: it is the designed behaviour proved live on six consecutive refusals of three different modes, which is the evidence a fail-closed claim is owed and the reason the blocked run left nothing to clean up |
| two of the five disposable repositories left on the account sit outside the fixed name prefix, so the single search the prefix exists for finds three of five, and one of the three it does find still breaks the rule's other half by being public | the 2026-09-19 pass, measured while setting up the runs for #344 and #345 | #397, which lists all five with what should happen to each; two were created outside the prefix by this pass and renamed once the rule was read, and the two probes that ran were made public because the rule as written made the run impossible — that issue states the search misses three of five, which is the same arithmetic slip corrected here and is worth its own line when it is implemented |
