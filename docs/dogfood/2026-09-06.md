# Dogfood 2026-09-06 — the loop on a third-party Python repository, from init to five merged pull requests

<!-- Retroactive report. Its source is #96, closed by it. #96 was written as
     prose before this format existed, so the numbers below are only the ones
     #96 actually recorded; every other cell says `not recorded` rather than a
     reconstruction. `Commit` is the tip of `main` on the day of the pass
     (`git log --first-parent --date=short`), the one number derivable after the
     fact. Note on the finding numbering: #96 says "twelve findings, F1-F12",
     but its body carries eleven bullets — there is no F7. The eleven that exist
     are reproduced below; F7 was never written. -->

- Repository: marcelusfernandes/synthetic-users
- Commit: f44f0762325646b26c2056b7e25430263f450b68
- Turns: not recorded
- Minutes: not recorded
- Cost (USD): not recorded
- Transcripts: not kept — the pass predates this format; its record is #96
- Retroactive: #96

## Scoreboard

| case | exit | error class | tool calls | decision | reason |
| --- | --- | --- | --- | --- | --- |
| init on a Python repository with no packaging marker | not recorded | detect-unknown | not recorded | fix | the tests are `phb/test_engine_v3.py` behind their own runner, so detection answered `stack: unknown`, `test: null` and every check that reads it had no command |
| init --rules against a claude/... default branch | not recorded | default-branch | not recorded | fix | the ruleset was written correctly, but nothing printed that the default branch is not `main`, which the installed templates assume |
| the bootstrap pull request | not recorded | expected-reds | not recorded | fix | `scope` (no linked issue) and `negative-control` (`no-tests`) are red by design on that one pull request and no card says so |
| the first pull request creating a new test tree | not recorded | vacuous | not recorded | fix | the base's `make test` enumerates directories, so a brand-new `app/tests/` cannot go red on the base |
| five issues built through the loop and merged with land.mts | not recorded | none | not recorded | keep | 5 of 5 merged, 3 round-1 rejections, 0 of them on preference — the loop works on a foreign repository |
| round-1 review of the front-end pull request | not recorded | ui-unproven | not recorded | fix | HTTP tests and a DOM-shim review passed an invisible bar fill; a real render caught it |
| an orchestrator grant declared in prose | not recorded | grant-in-prose | not recorded | fix | the grant was written in the dispatch prompt instead of an `authorised:` line, and `scope` failed the pull request a round later |

## Findings

| finding | origin | outcome |
| --- | --- | --- |
| a Python repository with no pyproject/requirements/pytest.ini/setup.py detects as unknown | dogfood 2026-09-06, finding F1 | #256 |
| templates and guard-main assume the default branch is main | dogfood 2026-09-06, finding F2 | #261 |
| the bootstrap pull request's two reds are expected and undocumented | dogfood 2026-09-06, finding F3 | #263 |
| the negative-control toolchain block is commented out in the template | dogfood 2026-09-06, finding F4 | accepted as it is: `templates/.github/workflows/agentic-checks.yml:62-64` marks the block and `skills/init/SKILL.md:116` tells the installer to mirror it; #96 itself recorded the candidate as "keep" |
| isolation: worktree makes a worktree of the session repository, not of the target | dogfood 2026-09-06, finding F5 | #266 |
| the missing-dependency failure never quotes the accepted Blocked by form | dogfood 2026-09-06, finding F6 | #258 |
| a new test tree cannot go red on a base whose test command enumerates directories | dogfood 2026-09-06, finding F8 | #257 |
| the reviewer card does not spell the round-2 label swap after a rejection | dogfood 2026-09-06, finding F9 | already covered by PR #140, which spells the swap in `agents/reviewer.md`, `skills/orchestrate/SKILL.md` step 5 and `docs/orchestration.md` |
| an acceptance criterion naming a symbol outside ## Files forces a workaround | dogfood 2026-09-06, finding F10 | #267 |
| a UI change passes HTTP tests and a DOM-shim review with an invisible render | dogfood 2026-09-06, finding F11 | #264 |
| an orchestrator grant written as prose instead of an authorised: line | dogfood 2026-09-06, finding F12 | already covered by PR #210, which makes `scope` read `authorised:` grants from the linked issue body |
