# Closeout M8 — Codex workflow labels and human gates

- Closed (UTC): 2026-09-10
- main SHA: 19e432c2cb0a126abe7607097b761cec559c279d

Reconstructed retroactively (#174), not written at the close: M8 closed on
2026-09-10, before the closeout format (#170, PR #192) and
`scripts/close-milestone.mts` (#172, PR #199) existed, so the row below was read
back from GitHub and `git log`. The milestone's description states the objective
but no exit criterion; written retroactively it is: the native Codex plugin
publishes the evidence-derived GitHub labels and the scoped human checkpoints, a
marketplace upgrade from GitHub is verified, and the Claude route survives it
unchanged.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #107 | feat(codex): publish label synchronization and scoped human gates | #109 | 19e432c2cb0a126abe7607097b761cec559c279d |

## Left out

- #108 `chore(codex): publish workflow labels and human gates phase` — the phase's
  parent spec issue. It shipped no diff of its own and closed with the phase.
- The human gate did not settle inside this phase. #110, #111, #114 and #116 split
  `human` into `human:pending`/`human:reviewed` and then renamed the second to
  `human:decided` across both routes on 2026-09-10 and 2026-09-11 (PRs #112, #113,
  #115, #117). They carry no milestone at all, so they belong to no closeout; they
  are named here because the labels this phase published are not the labels the
  repository ended up with.

## Dogfood

- No report exists. M8 predates `docs/dogfood/` and the rule that a phase touching
  `hooks/`, `ci/` or `scripts/` owes a `docs/dogfood/<date>.md` (#182, PR #246,
  merged 2026-09-17). The run closest to this phase is issue #129, "lessons from
  running the loop by hand and dogfooding the human states (2026-09-10/11)", which
  covers exactly the human gates M8 published and is still open. It is an issue
  with findings in prose — the dogfood README names it and #96 as the two passes
  holding thirty-four findings between them, "and no two of them are measured the
  same way" — not a `docs/dogfood/<date>.md`.
