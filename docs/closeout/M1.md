# Closeout M1 — improvements

- Closed (UTC): 2026-09-06
- main SHA: b4dfb9622231f8b21842e643ba8da97a7ec9c685

Reconstructed retroactively (#174), not written at the close: M1 closed on
2026-09-06, before the closeout format (#170, PR #192) and
`scripts/close-milestone.mts` (#172, PR #199) existed, so every row below was read
back from `gh issue list --milestone`, `gh pr list --state merged` and `git log`
rather than produced by the close. The milestone carried no description, so it had
no exit criterion either; written retroactively it is: the smoke suite runs as
per-area files behind one discovering runner, `protect-main` refuses every quoted,
substituted and redirected form of a push to `main`, `reconcile` prints the loop
state as JSON and does not read a superseded check run as red, and
`negative-control` refuses to call a structural failure on the base a pass.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #3 | test: split the smoke suite into per-area files with a discovering runner | #11 | 7c452e633bab7bd82d576a048ff9b67ae743872f |
| #4 | feat(ci): scope accepts Fixes/Resolves and several linked issues | #13 | 73bdbf8b0fc2370b4e2939781e0fa3d76f6512c6 |
| #5 | feat(ci): detect Ruby, Elixir, Gradle and Maven test commands | #12 | c7e520037f7dc7c1c4b6406ad4d72f2be40a0346 |
| #6 | feat(hooks): quote-aware command segmentation in protect-main | #14 | 5a716ac8f36458921386ad1140a84f30554d8e39 |
| #7 | feat(init): --dry-run | #15 | 148f0f04476217aa21d902784e3b8120e342815d |
| #8 | feat(orchestrate): scripts/reconcile.mts prints the loop state as JSON | #18 | 80843481a14afaae0091e7192b089532b705e728 |
| #9 | ci: run the suite under Bun as well as Node | #17 | 93d0715e2be0800bbce9b30285b9d631e21a2a3b |
| #10 | fix(ci): negative-control must not count a structural failure on the base as a pass | #20 | d75ff3ee7c0f0e600c14c59bdaf000749d404d13 |
| #16 | fix(hooks): protect-main misses quoted refs, backticks and a lone & | #22 | 488afed1ce202c1958681e7212165923a95cf794 |
| #19 | docs: bring every document up to date with #11–#15 | #26 | 385fa10cb0b57b7c085140f27f3d6a8f14338f72 |
| #21 | fix(orchestrate): reconcile reads superseded cancelled check runs as red | #23 | fc95121f9a31602bb7e6299a8bbaba3dba978e1e |
| #24 | fix(orchestrate): a re-running check reads red while its cancelled predecessor completed later | #27 | 721f3e59ff2edd2868826ae05e6c52339d3363a6 |
| #25 | fix(hooks): protect-main tokenization gaps left after #16 ($(), quoted flags, $"…", redirections) | #28 | b4dfb9622231f8b21842e643ba8da97a7ec9c685 |

## Left out

- #2 `M1 improvements` — the phase's parent spec issue. It shipped no diff of its
  own and closed with the phase, which is why it is here and not a row above.
- The Bun leg #9 added did not survive the repository: M4 removed it again (#64,
  PR #68). Nothing was left undone in M1 by it; the record is here so the table's
  row is not read as a mechanism that still exists.

## Dogfood

- M1's own dogfood left no file. The rule that a phase touching `hooks/`, `ci/` or
  `scripts/` owes a report landed in M16 on 2026-09-17 (#182, PR #246) and its format
  the same day (#183, PR #253), long after this close. All that survives of the M1
  pass is one sentence in M2's milestone description — "two orchestrator mistakes
  that prose in the skills did not prevent" — and the issues it produced, which are
  M2's. No transcript, no counts, nothing that can be re-read.
- [`docs/dogfood/2026-09-06.md`](../dogfood/2026-09-06.md) — the retroactive record of
  issue #96, which it closes, and the nearest pass to this phase. It ran the loop on a
  third-party Python repository from `init` to five merged pull requests at commit
  `f44f0762325646b26c2056b7e25430263f450b68`, the tip `main` reached at the end of M5,
  so what it exercised is what M1 to M5 built together rather than M1 alone. Its
  verdict on the loop is the one case marked `keep`: 5 of 5 issues merged, 3 round-1
  rejections, none on preference. Eleven findings: eight became issues #256, #257,
  #258, #261, #263, #264, #266 and #267, and three were already covered by work that
  had landed since.
