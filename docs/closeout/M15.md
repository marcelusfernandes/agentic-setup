# Closeout M15 — Decision trail

- Closed (UTC): 2026-09-18
- main SHA: a0b4771dead04fe5926c36bca230bdbe13dbc210

Exit criteria, read from the milestone's own description on the day above, all three
checked: a pull request touching `hooks/`, `ci/` or `scripts/` with no decision
recorded in `docs/decisions.md` or under `docs/decisions/` in the same diff carries
the `scope` warning (#177, PR #196, with the failing-or-warning question settled as
a warning by #180, PR #218); every item of the register states its status (#176,
PR #189); and the content-is-data doctrine pin is green in the required `test` check
(#179, PR #188). Three criteria met and a register that the pass below found the merge
gate exempting from review: the phase built the trail and did not yet protect it.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #176 | docs(docs): a decision register with statuses and the rule for an ADR against a note | #189 | 4730736b5308b117dd298cd019e4018673e9b12a |
| #177 | feat(ci): scope warns when a PR changes a mechanism without a decision entry | #196 | e4aa1b1694f2010fa31b48e1912cf00a4db84dc0 |
| #178 | feat(scripts): one logged line per pointed orchestrator decision | #194 | d6c6c985074c1d5eb0c3ac500c0d9fc175577e9c |
| #179 | feat(docs): pin the content-is-data doctrine in CLAUDE.md and the agent cards | #188 | 9f301ac70133bafbe318ff1c7f2b528c20baec09 |
| #180 | chore(ci): decide when the decision nudge starts failing, and over which paths | #218 | e5d6d2ab0b82a8b6997f603c6f69d13a0ee27bb8 |

## Left out

- #175 `spec: the decision trail — a register with states, a mechanical nudge, an
  orchestrator log and the content-is-data doctrine (M15)` — the phase's parent spec
  issue, still open. It ships no pull request of its own.

## Dogfood

- [`docs/dogfood/2026-09-17.md`](../dogfood/2026-09-17.md) — the pass that ran every
  row above, and the one that found this phase's register unguarded by the gate built
  to guard it. Its scoreboard row "landed with no isolated review at all", error class
  `docs-mode-no-review`, covers four decision-register pull requests, and two of the
  four are this phase's own: PR #189 (#176) and PR #218 (#180). They carried
  `type:docs`, which puts `land.mts` in docs mode and asks for no reviewed-SHA marker,
  so across that row of four the label exempted from review three changes to the
  register the whole gate exists to protect. That is #308, which makes the mode read
  the changed paths instead of the label. The other three rows of this phase merged on
  one round, in the scoreboard's baseline row of thirty-five.
- What the same pass found about the log and the register as mechanisms, all of it
  still open work: findings D12 and D13 became #300 — two agents recorded register
  items under one delegation with different `Status` values, and the decision log has
  a kind for opening every human gate and none for closing one, so a gate reads as
  open forever in the trail this phase shipped. Finding D21 became part of #301: two
  implementers that both append a decision item always collide on the register file,
  which is a consequence of the single-file register #176 chose. And the review of
  PR #194 (#178) produced #203 — `log-decision.mts` accepts a bare number where its
  own documentation says a hash-prefixed ref, and the lifecycle of its staging
  directory was unpinned — merged as PR #221 in M17. No finding of the pass contradicts
  an exit criterion above; every one of them is about a mechanism that works and is
  reachable around.
