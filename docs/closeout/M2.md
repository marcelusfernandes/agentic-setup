# Closeout M2 — orchestrator guards

- Closed (UTC): 2026-09-06
- main SHA: 0e8861c1d9b379739da3509b397eb3d991a917a6

Reconstructed retroactively (#174), not written at the close: M2 closed on
2026-09-06, before the closeout format (#170, PR #192) and
`scripts/close-milestone.mts` (#172, PR #199) existed, so every row below was read
back from GitHub and `git log`. The milestone's description states the objective
but no exit criterion; written retroactively it is: every orchestrator step that
mutates GitHub is a script with a refusal path — `land.mts` for the merge,
`claim.mts` for the lock, `issue-lint` for dispatch — and the orchestrator's card
runs those scripts instead of typing the `gh` calls.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #30 | feat(orchestrate): scripts/land.mts merges only when the server will accept it and relabels only after MERGED | #36 | d7322330a11e5c1ee4f419c7947d3cdc0a6677b8 |
| #31 | feat(orchestrate): scripts/claim.mts locks an issue in one step and refuses what is not claimable | #34 | ce5555ef703c2930f7044bbf5a9cab13f676ecac |
| #32 | feat(ci): issue-lint validates an issue's contract, disjointness and entry-point references before dispatch, locally and in CI | #35 | 234846d02d32352a07b423e15867242344c1df02 |
| #33 | docs: the orchestrator's card runs the scripts, and decisions.md says why | #39 | 0e8861c1d9b379739da3509b397eb3d991a917a6 |
| #37 | fix(ci): issue-lint accepts a literal new path in a new directory | #38 | bcb62c96220848d07e0f673f352b9247fe7d4f06 |

## Left out

- #29 `M2 orchestrator guards` — the phase's parent spec issue. It shipped no diff
  of its own and closed with the phase.
- The entry-point half of #32's `issue-lint` did not last: M4 removed it (#62,
  PR #71) as an issue-time warning nobody acted on.

## Dogfood

- M2's own dogfood left no file. The rule that a phase touching `hooks/`, `ci/` or
  `scripts/` owes a report landed in M16 on 2026-09-17 (#182, PR #246; the format is
  #183, PR #253), long after this close. What exercised M2's three scripts is the run
  M3's milestone description names — "the plugin's own session (`claude --plugin-dir`,
  `/agentic-setup:orchestrate`) as the first end-to-end test of the orchestrate skill
  and the hooks" — and its findings are M3's issues. No file, no counts.
- [`docs/dogfood/2026-09-06.md`](../dogfood/2026-09-06.md) — the retroactive record of
  issue #96, which it closes, and the first pass written down at all. It ran at commit
  `f44f0762325646b26c2056b7e25430263f450b68`, the tip `main` reached at the end of M5,
  so it measures what M1 to M5 built together. Two of its cases bite directly on what
  the orchestrator guards M2 built: a grant declared in the dispatch prompt as prose
  instead of an `authorised:` line, which `scope` failed a round later (finding F12),
  and a missing-dependency failure that never quotes the accepted `Blocked by` form
  that `issue-lint` reads (F6, now #258).
