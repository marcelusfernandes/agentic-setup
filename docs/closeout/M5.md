# Closeout M5 — loose ends

- Closed (UTC): 2026-09-06
- main SHA: f44f0762325646b26c2056b7e25430263f450b68

Reconstructed retroactively (#174), not written at the close: M5 closed on
2026-09-06, before the closeout format (#170, PR #192) and
`scripts/close-milestone.mts` (#172, PR #199) existed, so every row below was read
back from GitHub and `git log`. The milestone's description lists the candidates
but states no exit criterion; written retroactively it is: the four candidates the
M4 reviews left are closed — `protect-main` matches the long form of a push to
`main`, `reconcile` reports uncommitted and unpushed work in dead worktrees,
`scope`'s summary states the verdict when only dangling references fail, and the
agent cards describe what `negative-control` actually reads.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #87 | fix(hooks): protect-main matches the long form of a push to main | #92 | 67979f42d9df9775088baee5c4a6f65402af07fb |
| #88 | feat(orchestrate): reconcile reports uncommitted and unpushed work in dead worktrees | #95 | f44f0762325646b26c2056b7e25430263f450b68 |
| #89 | fix(ci): scope's summary states the verdict when only dangling references fail | #93 | 2788478929c4f2e93f39ff1ee02afaf39d76430a |
| #90 | docs: negative-control reads the diff; the test(red) commit is a convention | #94 | af0bf42e2032b6bb80b15e70890be557207b6046 |

## Left out

- #91 `M5 loose ends` — the phase's parent spec issue. It shipped no diff of its own
  and closed with the phase.

## Dogfood

- No report exists. M5 predates `docs/dogfood/` and the rule that a phase touching
  `hooks/`, `ci/` or `scripts/` owes a `docs/dogfood/<date>.md` (#182, PR #246,
  merged 2026-09-17). The first third-party run of this loop happened hours after
  M5 closed — `synthetic-users`, 2026-09-06 — and was written up as issue #96,
  "lessons from the first third-party run", which is still open. It is the closest
  thing to a dogfood of everything M1 to M5 shipped, and it is an issue with
  findings in prose, not a report with the three per-case numbers the format now
  requires.
