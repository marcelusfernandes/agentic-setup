# Closeout M17 — Post-review fixes of M11-M16

- Closed (UTC): 2026-09-18
- main SHA: a0b4771dead04fe5926c36bca230bdbe13dbc210

Exit criteria, read from the milestone's own description on the day above, two of
three checked. Checked: every row of the sub-issue table in spec #201 is an issue
that exists and carries an `Origin:` line in its `## Context` naming the review or
the run the finding came from; and #212 (blocked on #148) and #209 (blocked on #172)
both merged, as PR #276 and PR #250, rather than closing the milestone silently on an
unresolved external dependency. Unchecked, and the one that keeps this phase open:
every sub-issue of #201 that is not blocked on #148 or #172 is merged. Twenty-three
have; twenty-eight have not, because the 2026-09-17 pass that fixed the first
twenty-four findings produced findings of its own and they were filed here. This file
records what this phase has merged as of the day above, which is what a closeout is —
a dated snapshot, not a claim that the phase is finished.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #202 | fix(docs): pin AGENTS.md/CLAUDE.md Map and init's copy list against scripts/*.mts | #269 | 25fae6a7fa395238e4071f0a84c4749950281e78 |
| #203 | fix(scripts): harden log-decision.mts ref parsing and the stage/mkdtemp lifecycle | #221 | 5a0fd5ce723cae9e695fad02841fa8d99fdf2cfa |
| #204 | fix(hooks): deny gh pr merge behind a global flag and prune stale deny rules on re-init | #242 | 124d5785afdb4684e3d2b6a8877d32b4cc1686a0 |
| #205 | fix(docs): document all six reviewer checks and milestoneLint, and fix the invariant-6 test header | #240 | 03d6fd7787158fc452974d8e071d382570ef7a7e |
| #206 | fix(scripts): reconcile.mts parses fenced/HTML-comment milestone text and reads every milestone | #228 | d7e336582e1d2a6735c12d9fab87f5a44752d11f |
| #207 | fix(ci): guard-main.yml separates stderr, pins its two copies identical, and covers dedupe failure | #223 | 5c5ec67543205c15286124c4e8181e70b802b392 |
| #208 | fix(tests): harden the closeout provenance pin's ref resolution, gh calls and messages | #224 | abdf4853e32809e7d834963bc3e4aa19789ff758 |
| #209 | docs(docs): the closeout evidence file is a required step before a milestone closes | #250 | 348d1d67ee472fac66468ee72982cc30a3cb9001 |
| #211 | refactor(docs): relocate decision register items 14-15 to dated files and fix the index | #247 | 0a65e217362dd8788d530a86b837b297d88db401 |
| #212 | fix(docs): correct required_approving_review_count claims across decisions, orchestration and land | #276 | e2aae23bf1c3600fdae195135477c175b131f1a9 |
| #213 | fix(scripts): claim.mts drops the unreachable type-label branch and clears a disagreeing type | #222 | 7a653d63e15c75c87354dc078ffd478a1f458616 |
| #214 | fix(ci): close the negative-control own-code-skip hole and the docs-writer type:docs gap | #290 | 5bc9933fc4c11f1ddc9582bdcdee4e1435eb95e5 |
| #215 | fix(scripts): adopt.mts hardening batch — label paging, named errors, quoting | #243 | 0e4fc31d4e0dcaa7ea9c0fcb937464763511f8c3 |
| #226 | fix(scripts): create-subissue reads its parent strictly and names its post-creation failures | #245 | 0c2e222ad21efeb6bedce24e9b12356d78a98d4b |
| #227 | fix(scripts): close-milestone reads every issue of the milestone and refuses instead of crashing | #306 | 00debb99be97385b0ba3ae833daffcc2f91d350f |
| #229 | fix(scripts): init --rules refuses on an unreadable read instead of creating a second ruleset | #317 | b336d69763877022122ca2e0ebccd600cac8fa34 |
| #230 | fix(ci): negative-control fails closed on a proof declaration it cannot read | #313 | 5af9ada92a64116168651fd67267b629994c5335 |
| #231 | fix(ci): a bullet-form authorised grant is read once, as a grant | #311 | 4cbbfdbe33189956d5c11c4220cfc218566091e7 |
| #233 | refactor(scripts): adopt's inventory derives the lists it restates from the installer | #249 | 432d1ee647d7e120384fe9dbbeb3668a8122b8a2 |
| #234 | fix(hooks): the worktree's node_modules symlink is ignored like the directory | #244 | c4890b0ffde87031931701441ffe81b694ac7fc5 |
| #237 | fix(hooks): protect-main denies an authorised grant written from a worktree | #331 | 4572524bcefe30e17d11b93eca86f6ecf53ffbf2 |
| #302 | fix(scripts): adopt's plan-issue selection, temp-index lifecycle and the adoption pull request's own gates | #329 | fc99b8ed88bd431c871759ed2b738d70086f6a81 |
| #309 | fix(hooks): the stop gate's refusal paths behave as its header says | #319 | 1ca7a5c800520b8f0489362596bf26722d80bdf7 |

## Left out

- #201 `spec: post-review fixes of M11-M16, one issue per mechanism (M17)` — the
  phase's parent spec issue, still open. It ships no pull request of its own.
- #232, #238, #239, #291, #293, #294 and #295 — still open, the first seven of the
  twenty-eight sub-issues this phase has not merged. Named individually rather than
  counted, so a reader can check the list against `gh issue list --milestone`.
- #296, #297, #298, #299, #300, #301, #303 and #304 — still open. Eight of the
  findings the 2026-09-17 pass raised against mechanisms this phase had already
  touched; #298 is the one that catches the defect this closeout corrects by hand.
- #305, #308, #310, #312, #316, #318 and #321 — still open. #312 is the description
  half of a finding whose evidence half landed: M10's milestone description still
  cites a report measured against another repository.
- #322, #323, #324, #326, #327 and #330 — still open. #326 records that #233 above
  closed with half a criterion met, `PROOF_DIR` still exported, so the row for #233
  says what merged and not that the criterion was reached.

## Dogfood

- [`docs/dogfood/2026-09-17.md`](../dogfood/2026-09-17.md) — the pass this phase was
  created by and then extended by. The twenty-four non-blocking findings of the
  isolated reviews of PR #185 to PR #195 that #201 was decomposed into are rows of
  this report's findings table, and that table is the `Origin:` line of almost every
  issue above; the same pass then raised most of the twenty-eight that are still open
  in `## Left out`. What it found about this phase's own landings: #214 (PR #290) is
  one of the two pull requests in the scoreboard's `in-review-at-cutoff` row, opened
  inside the window with no verdict when it closed and merged the next day, its cost
  recorded and its outcome deliberately left out of the pass; #204 (PR #242) is in the
  `docs-not-equal-code` row, sent back once because a rule stated outside the issue's
  `## Files` was left stale; and the report of PR #222 (#213) carries a process
  finding of this phase's own making — an implementer ran `git stash` inside its
  worktree, which its own card's `Never` list forbids, accepted as no damage rather
  than turned into a hook.
- Two results of that pass bear on how much of this table is settled. The review of
  PR #240 (#205) is one of the five the report groups as "comment and docblock wording
  that describes something the code no longer does", accepted rather than fixed,
  because the pins that would keep such sentences true cost more than the sentences
  are worth; and the review of PR #250 (#209) found that every `gh` list read in the
  repository was bounded at 200 with no truncation signal while two checks comparing
  against such a list fail open, which is #303 and still open. Seven of the rows above
  — #227, #229, #230, #231, #237, #302 and #309 — merged on 2026-09-18, after the
  window this report measures ended at 18:03:07Z, so it speaks to the findings that
  created them and to nothing about how they landed.
