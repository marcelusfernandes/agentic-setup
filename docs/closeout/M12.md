# Closeout M12 — Machine-checkable merge and scope

- Closed (UTC): 2026-09-17
- main SHA: 668cba7845fc6845333a215949a26825e5e92f06

**Provisional. M12 has not closed.** Four issues are still open — `#153`, `#157`,
`#158` and `#159` — against the three merged below, so the date above is the day this
snapshot was taken (#174) and the SHA is the tip of `main` at that moment, not a close.
The grammar in [`README.md`](README.md) has one date field and one SHA field and no way
to say "still running", so this paragraph says it instead. **A person has to replace
both before the close.** `scripts/close-milestone.mts` never writes to a closeout file
— its only write is the PATCH on the milestone's description — and its `evidence:sha`
check asks only whether each sha is an ancestor of `origin/main`, which a snapshot
months out of date still is. Nothing mechanical catches a stale date or SHA in this
file.

Exit criterion, quoted from the milestone's description and **not yet met**: `land`
names the review mode in its output and merges only when every condition of that mode
is met — `agent` by default and `approved` only when asked for, with no downgrade
between them — a hand-typed `gh pr merge` is denied in the session while
`node scripts/land.mts` still merges, `scope` reads its grants from the linked issue,
and each route refuses a task the other already locked. Three of those four are in the
table: the declared review mode (#156, PR #255), the denied hand-typed merge (#154) and
the grant read from the linked issue (#155). The fourth, one lock both routes respect,
is #157 and #158, both still open.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #154 | feat(hooks): a hand-typed gh pr merge is denied while land.mts still merges | #191 | 280d9462187d85ef237a4ce4030761514b93a016 |
| #155 | feat(ci): scope reads authorised grants from the linked issue, not from the pull request | #210 | 363f39e2bf5bd0bf3841aafc0f54e9d1c0f899b6 |
| #156 | feat(scripts): land declares its review mode — agent by default, approved as an opt-in | #255 | 4b97e3ebf17f874eb81a003e19827c95ba0da935 |

## Left out

- #153 `spec: machine-checkable merge and scope (M12)` — the phase's parent spec
  issue, still open.
- #157 (`reconcile` and `claim` read a `codex/task-<n>` branch as a held lock) and
  #158 (the Codex route refuses a task the Claude route already locked) — both still
  open. Together they are the other half: one lock both routes respect.
- #159 (the README describes the merge gate this repository actually has) — still
  open.
- #154 has two pull requests closing it on GitHub, #191 and #200. #191 is the one
  that shipped the deny rule and closed the issue at 12:10:07 UTC; #200 is a
  decisions-register change that also wrote `Closes #154` and merged thirteen
  minutes later. The row names #191. A second `gh pr merge` hardening followed in
  M17 (#204, PR #242) and is not part of this phase.

## Dogfood

- No report covers this phase. [`docs/dogfood/`](../dogfood/) holds two, written
  retroactively from issues #96 and #129 and closing them: the pass of 2026-09-06
  against a third-party repository, and the pass of 2026-09-10 to 2026-09-12 against
  this one. Both are a week older than every row above, so neither is evidence for
  any of them.
- The rule that a phase touching `hooks/`, `ci/`, `scripts/` or a
  `skills/**/SKILL.md` must name a `docs/dogfood/<date>.md` here landed in M16 on
  2026-09-17 (#182, PR #246). All three rows changed exactly those paths — #154 the
  hooks, #155 `ci/`, #156 `scripts/land.mts` and `skills/orchestrate/SKILL.md` — so
  `scripts/close-milestone.mts` will refuse this phase's close with
  `missing: ['dogfood']` until a pass is run against them and named here. That
  refusal is correct, and this section deliberately writes no dated
  `docs/dogfood/<date>.md` filename: the check matches that shape anywhere in a
  bullet, so citing the older reports would silence a refusal this phase has earned.
