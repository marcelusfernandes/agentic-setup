# Closeout M12 — Machine-checkable merge and scope

- Closed (UTC): 2026-09-18
- main SHA: a0b4771dead04fe5926c36bca230bdbe13dbc210

Exit criteria, read from the milestone's own description on the day above, all four
checked and every one of them in the table below: `land` names the review mode in its
output and merges only when every condition of that mode is met — `agent` by default
and `approved` only when asked for, with no downgrade between them (#156, PR #255); a
hand-typed `gh pr merge` is denied in the session while `node scripts/land.mts` still
merges (#154, PR #191); `scope` reads its grants from the linked issue (#155,
PR #210); and each route refuses a task the other already locked, which is the pair
#157 and #158, merged on 2026-09-18 as PR #285 and PR #314. Those two and #159
(PR #320) are the three rows that landed after the snapshot this file replaces, and
they are why it no longer reads as a phase with four issues still open.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #154 | feat(hooks): a hand-typed gh pr merge is denied while land.mts still merges | #191 | 280d9462187d85ef237a4ce4030761514b93a016 |
| #155 | feat(ci): scope reads authorised grants from the linked issue, not from the pull request | #210 | 363f39e2bf5bd0bf3841aafc0f54e9d1c0f899b6 |
| #156 | feat(scripts): land declares its review mode — agent by default, approved as an opt-in | #255 | 4b97e3ebf17f874eb81a003e19827c95ba0da935 |
| #157 | feat(scripts): reconcile and claim read a codex/task-<n> branch as a held lock | #285 | 54594f6e7571a0498c5b8e69edf1f845a6142a71 |
| #158 | feat(scripts): the Codex route refuses a task the Claude route already locked | #314 | c56b75d12f3ab6be636d92e5ae7e5690741497f0 |
| #159 | docs(docs): the README describes the merge gate this repository actually has | #320 | 35f229e6aceacbe06db55f4fed57d88dd8bb11a6 |

## Left out

- #153 `spec: machine-checkable merge and scope (M12)` — the phase's parent spec
  issue, still open. It ships no pull request of its own, so it belongs here and not
  in a row: an issue that closed without a pull request did not ship, and this one has
  not closed at all.
- #154 has two pull requests closing it on GitHub, #191 and #200. #191 is the one
  that shipped the deny rule and closed the issue at 12:10:07 UTC; #200 is a
  decisions-register change that also wrote `Closes #154` and merged thirteen
  minutes later. The row names #191. A second `gh pr merge` hardening followed in
  M17 (#204, PR #242) and is not part of this phase.

## Dogfood

- [`docs/dogfood/2026-09-17.md`](../dogfood/2026-09-17.md) — the pass that ran this
  phase's merge gate for a day and caught it exempting the one thing it was built to
  protect. Its scoreboard row "landed with no isolated review at all", error class
  `docs-mode-no-review`, covers four decision-register pull requests, and PR #200 —
  the second pull request closing #154, named in `## Left out` above — is one of the
  four: it carried `type:docs`, which puts `land.mts` in docs mode and asks for no
  reviewed-SHA marker, so the label exempted from review a change to the register.
  A phase whose subject is "move the merge decision out of silent fallbacks" shipped a
  label that is one, and the fix is #308, still open. The other rows of the window:
  #154 (PR #191) is in the `docs-not-equal-code` row, sent back once because prose
  outside the issue's globs still said the merge deny covers only `--admin` (finding
  D3, closed by this phase's own PR #191 round-2 grant and by #204); #155 (PR #210)
  and #156 (PR #255) are both in the `head-changed-after-approval` row, two of the
  five pull requests that took a merge from `main` after approval and needed a fresh
  review.
- The cross-route lock is the phase's hardest case and the report records it in full.
  #157 (PR #285) has a scoreboard row of its own, `fix-worse-than-bug`, exit 1 because
  it had not merged when the window closed at 18:03:07Z: the round-1 review found the
  fix worse than the bug — resolving a Codex lock in `branchFor` moved a Codex-held
  issue into the bucket the orchestrate card dispatches straight to a Claude
  implementer — and the round-2 review, running with no verdict at the cutoff,
  rejected it too for writing the exception into three rows of the state table instead
  of the two describing the bucket the lock lands in. It merged on 2026-09-18, and its
  reviews are also where the report found that the loop's rules are stated in both the
  orchestration document and the card with clauses on one side only in both directions
  (#305, open). #158 (PR #314) and #159 (PR #320) merged on 2026-09-18 as well, after
  the window this report measures, so it says nothing about how they landed.
- The reviews of PR #210 (#155) alone produced three more issues in M17 — #231, #232
  and #237: a bullet-form `authorised:` grant read twice, once as a
  grant and once as a misplaced line; `issue-lint` never validating the globs it passes
  through; and nothing denying a grant written from a worktree, so an implementer could
  grant itself. The first and third have merged, as PR #311 and PR #331; #232 is open.
