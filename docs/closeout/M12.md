# Closeout M12 — Machine-checkable merge and scope

- Closed (UTC): 2026-09-17
- main SHA: 0ebbf70826ecaf9a9b9818e4d3c837c529ecf71a

**Provisional. M12 has not closed, and most of it has not shipped.** The milestone is
still open on GitHub with `#153`, `#156`, `#157`, `#158` and `#159` open against two
merged issues, so the date above is the day this snapshot was taken (#174) and the SHA
is the tip of `main` at that moment, not a close. The grammar in
[`README.md`](README.md) has one date field and one SHA field and no way to say
"still running", so this paragraph says it instead; both values are replaced when the
phase closes through `scripts/close-milestone.mts`. Exit criterion, quoted from the
milestone's description and **not met**: `land` names the review mode in its output
and merges only when every condition of that mode is met — `agent` by default and
`approved` only when asked for, with no downgrade between them — a hand-typed
`gh pr merge` is denied in the session while `node scripts/land.mts` still merges,
`scope` reads its grants from the linked issue, and each route refuses a task the
other already locked. Of those four, the hand-typed merge (#154) and the grant from
the linked issue (#155) are below; the review mode `land` declares and the shared
lock are still open issues.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #154 | feat(hooks): a hand-typed gh pr merge is denied while land.mts still merges | #191 | 280d9462187d85ef237a4ce4030761514b93a016 |
| #155 | feat(ci): scope reads authorised grants from the linked issue, not from the pull request | #210 | 363f39e2bf5bd0bf3841aafc0f54e9d1c0f899b6 |

## Left out

- #153 `spec: machine-checkable merge and scope (M12)` — the phase's parent spec
  issue, still open.
- #156 (`land` declares its review mode — agent by default, approved as an opt-in) —
  still open, with PR #255 open against it. It is half of the exit criterion.
- #157 (`reconcile` and `claim` read a `codex/task-<n>` branch as a held lock) and
  #158 (the Codex route refuses a task the Claude route already locked) — both still
  open. Together they are the other half: one lock both routes respect.
- #159 (the README describes the merge gate this repository actually has) — still
  open.
- #154 has two pull requests closing it on GitHub, #191 and #200. #191 is the one
  that shipped the deny rule and closed the issue at 12:10:07 UTC; #200 is a
  decisions-register change that also wrote `Closes #154` and merged thirteen
  minutes later. The row names #191. A second `gh pr merge` hardening followed in
  M17 (#204, PR #242) and is not part of this phase.

## Dogfood

- No report exists. `docs/dogfood/` holds only `README.md` and `TEMPLATE.md`; this
  repository has never written a pass to a file. The only written passes are issues
  #96 (2026-09-06) and #129 (2026-09-10/11), both still open and both a week older
  than this phase.
- The rule that a phase touching `hooks/`, `ci/`, `scripts/` or a
  `skills/**/SKILL.md` must name a `docs/dogfood/<date>.md` here landed in M16 on
  2026-09-17 (#182, PR #246). Both rows above changed exactly those paths — #154 the
  hooks, #155 `ci/` — so `scripts/close-milestone.mts` will refuse this phase's close
  with `missing: ['dogfood']` until a real report is written and named in this
  section. That refusal is correct and this section will not pre-empt it by naming a
  file that does not exist.
