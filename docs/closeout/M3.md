# Closeout M3 — lint and reconcile polish

- Closed (UTC): 2026-09-06
- main SHA: 20486f014d94a67e874f1c377dfb4f79329bc9ca

Reconstructed retroactively (#174), not written at the close: M3 closed on
2026-09-06, before the closeout format (#170, PR #192) and
`scripts/close-milestone.mts` (#172, PR #199) existed, so every row below was read
back from GitHub and `git log`. The milestone's description states the objective
but no exit criterion; written retroactively it is: the follow-ups the M2 reviews
left are closed — `issue-lint` reads wildcards, single-character wildcards and new
directories correctly, runs one `git grep` for the whole covered set and refuses an
issue it would fail at claim time; `reconcile` fetches once, reports resumable
issues after a restart and does not count a worktree locked by a dead pid as a live
agent; and the hooks gate an implementer where it actually stops.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #41 | fix(ci): issue-lint treats a wildcard under a new directory as new | #47 | 5feded976b1b7142e6ac6c62ceab3e6300c44e95 |
| #42 | fix(ci): ? is a single-character wildcard in globs | #54 | cf26175ab4ba473f06eaaa4a9d985bacd1e762d5 |
| #43 | refactor(orchestrate): reconcile reads remote branches from local refs after one fetch | #48 | 194037e6b32f75a13755dab870f5f5bd7b3d5c48 |
| #44 | perf(ci): issue-lint runs one git grep for the whole covered set | #57 | 692dd997832e15581392c197360e8fefff0ffc9d |
| #45 | docs(orchestrate): the card documents wildcard new directories, reconcile's --no-fetch, and stops folding lint warnings for type:bug | #59 | 20486f014d94a67e874f1c377dfb4f79329bc9ca |
| #46 | feat(orchestrate): reconcile reports resumable issues after a restart | #55 | cc4da2a6780b279aa706463d7e4711d8b2560b2a |
| #49 | fix(hooks): stop-gate never ran for implementers — register it for SubagentStop | #52 | 1cae8df2620f011e20037af57825adeda05200c9 |
| #50 | fix(orchestrate): claim.mts refuses an issue that fails issue-lint | #53 | a49687646de62572ed0ed422cfb48260403340ea |
| #56 | feat(orchestrate): a worktree locked by a dead pid does not count as a live agent | #58 | 54762db85cb34997d32ebcf244b09d5da0b2ba30 |

## Left out

- #40 `M3 lint and reconcile polish` — the phase's parent spec issue. It shipped no
  diff of its own and closed with the phase.
- The `stop-gate` hook #49 fixed was deleted the next day by M4's trim (#61,
  PR #70) and only returned in M16 (#137, PR #235). The row above is history, not a
  description of the hooks this repository has.

## Dogfood

- No report exists. M3 predates `docs/dogfood/` and the rule that a phase touching
  `hooks/`, `ci/` or `scripts/` owes a `docs/dogfood/<date>.md` (#182, PR #246,
  merged 2026-09-17). M3 is itself the only phase whose milestone description names
  a run — the plugin's own session, `claude --plugin-dir` with
  `/agentic-setup:orchestrate`, as the first end-to-end test of the orchestrate
  skill and the hooks — and what it left behind is this phase's nine issues, not a
  report. The next written evidence in the repository is the independent audit of
  2026-09-06, [`docs/research/2026-09-06-auditoria-overengineering.pt-BR.md`](../research/2026-09-06-auditoria-overengineering.pt-BR.md),
  which read the code and the 27 merged pull requests and became M4.
