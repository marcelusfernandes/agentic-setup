# Closeout M10 — Continuous orchestration and gates before the PR

- Closed (UTC): 2026-09-19
- main SHA: 62b1f86472ac181bb3902c1fbf097e05b3b348a9

Exit criteria, read from the milestone's own description on the day above, one of
three checked. Checked: `init --rules` creates or updates the ruleset on a disposable
repository with `scope`, `negative-control` and `test` required, or reports precisely
why it cannot (#133, PR #139, and see the second `## Left out` bullet for what that
first version got wrong). Unchecked: one headless orchestrator run taking a disposable
repository's milestone of three lint-clean issues from `state:ready` to a closed
milestone with no human action, stopping with a summary when the only remaining issue
carries `human:pending`; and every pull request of this milestone passing the negative
control, the file-growth rule and the `test` check as `main` required them on the day
it merged, with its `type:` label applied by the orchestrator. The milestone's own
"Out of this phase" note records that the third criterion was rewritten to the gates
that existed while the phase ran, because the strict negative control (#135) and the
SubagentStop gate (#137) postdate every pull request it could apply to.

Since that reading, both of the unchecked criteria have been answered. The paragraph
above is left as it stood, because it is what the milestone's description said on
2026-09-18 — the date this file carried while that paragraph was the whole of it, and
why it reads "one of three checked" against a description that showed two when this
paragraph was written on 2026-09-19, and that gains a tick each time one of the
criteria is answered. The header date above has since been moved to the close this
file is written for; this paragraph records what has become of those two criteria in
between. The third was verified on 2026-09-18 from the check-runs API at each merged
head — #140, #139 and #138 each report `negative-control`, `scope` and `test` success —
and the milestone's description carries both that verification and the removal of the
criterion's label-authorship clause, dropped as unanswerable rather than unmet because
the orchestrator and every implementer authenticate as one GitHub login. The first was
answered on 2026-09-19, positively, by the run it asks for: one headless orchestrator
run against a disposable repository (#345) took a milestone of three lint-clean issues
from `state:ready` to a closed milestone. It spent 34 orchestrator turns, 15.3 minutes
and US$10.27 over four merged pull requests. The two halves of that criterion this
repository cannot show for itself — that nobody intervened, and that the run stopped
with a summary naming the remaining `human:pending` issue — are the paragraph below.
The `## Dogfood` section further down names the report that carries the run, and with
it the six bounds the run's own author put on what it shows; the one that bears
hardest on this criterion is that preparation needed hands even though the run did
not. An answer that arrives after a closeout's date does not move that date: this file
is the evidence the phase's close is checked against and lands before it, so it
records the phase as it stood at the SHA above and asserts nothing about the close
itself.

A third layer, added on 2026-09-19 with the last two rows of the table below and later
than the paragraph above, which is why it is marked rather than folded into it: those
two halves are the run author's own account, given in the second comment on #345 and
resting on a transcript kept outside this repository. What that account says is that
nobody intervened — no grant written mid-run, no dependency edited, no label applied by
hand — and that the session's last message named the remaining `human:pending` issue as
the reason it stopped. The `## Dogfood` bullet below sorts its sources into three named
buckets — the report, that comment, and a read-back of the disposable repository — and
these two belong to the second alone, which is why they are attributed here rather than
stated in this file's own voice. What the disposable repository can show was checked
against them and agrees: no issue of it carries an `authorised:` line, and all
thirty-five of its label events fall either in the setup before the run opened or inside
the run's own window, none after it. That makes them consistent, not verified. Every one
of those events names the same login, which is exactly the ambiguity the milestone's
description records as unanswerable, so no record in either repository separates an
orchestrator's hand from a person's.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #132 | feat(orchestrate): continuous loop over open milestones with a closed list of stop reasons | #140 | d0fca141cb00f608ecdc2d4e4c579024ffc1ebd1 |
| #133 | feat(init): --rules creates or updates the ruleset with the required checks | #139 | fc34f550785df44322d1e59208e14af6c5e8aeb5 |
| #134 | feat(ci): scope fails files that are new or grew past 800 lines | #138 | f252a02d751ee0a6a6a95b165c52dfd600faca01 |
| #345 | chore(infra): one headless orchestrator run against a disposable repository (M10's last criterion) | #404 | 62b1f86472ac181bb3902c1fbf097e05b3b348a9 |
| #373 | fix(infra): init --rules creates a ruleset GitHub accepts, and a refused write exits 1 | #375 | aba0d7a8b526bc73d96012a50cfac1428a4874fc |

## Left out

- #131 `spec: continuous orchestration on the Claude route and gates before the PR
  (M10)` — the phase's parent spec issue, still open. It ships no pull request of its
  own, so it belongs here and stays here: an issue that closed without a pull request
  did not ship, and `## Issues` records what shipped.
- #405 `docs: closeout M10` — the issue this file answers. Its own merge commit cannot
  be written into the file that carries it, and it ships nothing else, so it is
  accounted for here; the pull request that lands this edit is its evidence. Its body
  holds the phase's decision-log lines verbatim, which is where they stay: this grammar
  has three sections and no fourth (`README.md`, "The phase's decision log lives in the
  closeout issue").
- What #133 shipped did not hold: `init --rules` created a second ruleset instead of
  updating the existing one, which M11 had to fix (#143, PR #186). The row above is
  what merged in this phase, not a statement that the behaviour is still current.

## Dogfood

- [`docs/dogfood/2026-09-10.md`](../dogfood/2026-09-10.md) — the retroactive record
  of issue #129, which it closes. It folds the runs of 2026-09-11 and 2026-09-12 into
  one report, and three of its cases are this phase's own work: "parallel headless
  dispatch of M10 pass 1 (2026-09-12)", which produced exactly PRs #138, #139 and
  #140 with nine green checks (23 turns, 37 minutes, about USD 22, verdicts 1
  approved and 2 rejected on mechanical defects); "headless round 2 of M10 pass 2,
  and round 3 (2026-09-12)", where round 2 ran headless for #140 and #138 but round 3
  needed a person; and the #133 implementer leaving a public disposable repository
  behind. Its findings land as #259, #260, #264, #265, #266 and #267.
- [`docs/dogfood/2026-09-17.md`](../dogfood/2026-09-17.md) — the pass that ran this
  phase's three mechanisms at scale five days after they merged: sixty-one pull
  requests through claim, isolated review and land in one day, driven by the
  continuous loop #132 shipped. What it found about each of them. The 800-line
  file-growth rule of #134 fired for real and is half of the report's `hygiene`
  scoreboard row — a test file crossed the cap and made `scope` red, costing exactly
  one round and no judgement — and the report's finding that `tests/init.test.mts`
  sits at 798 of 800 lines, so the repo-view failure fallback cannot be covered
  without a new file, is that rule working as designed (now part of #304). `init
  --rules` from #133 was found to create a second ruleset on a read it could not
  parse, swallowed as an empty object, which the isolated review of PR #186 caught and
  no check could; that is #229, merged as PR #317 in M17. The loop of #132 produced
  the pass's orchestration findings rather than defects in itself: three label edits
  starting three runs so latest-run picks a cancelled one (D16), applying the approval
  label re-triggering the checks so a `land` run straight after hits a queued merge
  (D10), and a weekly account limit ending six agents in one minute with nothing able
  to tell finished work from lost work (D25) — #239, #265 and #291.
- One claim in the milestone's description still does not check out, and the same pass
  confirmed it: it says "Wave 1 of the 2026-09-12 report measured against lohra-ts",
  but the report's own `Repository:` line is `marcelusfernandes/agentic-setup` and no
  case in it names lohra-ts. What the 2026-09-12 wave measured, by the only written
  record of it, is this repository. The report of PR #271 raised it and it is #312,
  still open — a description this closeout does not and cannot fix.
- The rule that a phase touching `hooks/`, `ci/`, `scripts/` or a
  `skills/**/SKILL.md` must name a report here landed in M16 on 2026-09-17 (#182,
  PR #246), after everything in the table merged. This phase changed `ci/` (#134),
  `scripts/` (#133) and `hooks/` (#132), and both reports above answer it: the first
  ran against this phase's three pull requests while they were open, the second ran
  the mechanisms they shipped for a full day. Neither is a citation of convenience.
- [`docs/dogfood/2026-09-19.md`](../dogfood/2026-09-19.md) — **an addendum, written on
  2026-09-19 after the four bullets above and kept last so that the fourth's "both
  reports above" goes on naming the two it was written about. The link leads the bullet
  because the parsers of this file read a bullet's first line and nothing else, so prose
  in front of it would hide the report from them.** The pass that carries the
  headless run of #345, and so the report this phase's first exit criterion is answered
  from. Its scoreboard row for that run records 34 orchestrator turns, 15.3 minutes,
  US$10.27 and four merged pull requests, beside the 775 turns, 954 minutes and
  US$742.59 the 2026-09-17 report measured over sixty-one attended landings. The report
  bounds its own comparison rather than leaving it to be read: the run's three issues
  were written to be easy and disjoint, so 34 against 775 is 8.5 turns per pull request
  against 12.7 on work with no contention, and the attended pass spent its turns on the
  grants, sequencing and review rounds this run was written not to need. Five more
  bounds sit beside that one in the report's findings, each recorded as a statement
  about the run and not as work. The closeout pull request merged in mode `docs` with
  no review and no reviewed-sha marker, which #308 removes by making the exemption read
  the changed paths instead of the label (PR #369, the same day). One command was
  denied — a reviewer reaching for the suite at a pull request's head through a command
  that opened with a read of the stashed state, which the installed deny list refuses —
  and the review returned a verdict anyway. Closed issues keep `state:in-review`, which
  is the contract and not a gap. The run found two defects in the issue it was told to
  stop on and recorded both there instead of acting on them, which is the pass's only
  evidence that an unattended orchestrator stops where an attended one would. And
  preparation needed hands even though the run did not: the repository had to be public
  for rulesets to exist on this plan (#397), `init --rules` could not create the
  ruleset so it was made by hand with a corrected payload (#373), and the bootstrap
  pull request was merged by the owner. The repeat of that run on a correctly named
  private repository is a scoreboard row of its own and never launched at all, for the
  same plan limit. What the report does not carry, and the second comment on #345 does,
  is the run's window, the plugin sha and the name of the disposable repository; the
  state left behind there was read back from that repository rather than from the run's
  own summary — the milestone closed at 03:20:36Z with none open and five closed, the
  three work pull requests each merged carrying `review:approved` and one reviewed-sha
  marker comment, the closeout pull request merged carrying neither, and the next
  milestone still open with the spec and the `human:pending` issue.
