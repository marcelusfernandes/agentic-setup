# Closeout M10 — Continuous orchestration and gates before the PR

- Closed (UTC): 2026-09-17
- main SHA: 0ebbf70826ecaf9a9b9818e4d3c837c529ecf71a

**Provisional. M10 has not closed.** The milestone is still open on GitHub with
`#131` open, so the date above is the day this snapshot was taken (#174) and the SHA
is the tip of `main` at that moment, not a close. The grammar in
[`README.md`](README.md) has one date field and one SHA field and no way to say
"still running", so this paragraph says it instead. **A person has to replace both
before the close.** `scripts/close-milestone.mts` never writes to a closeout file — its
only write is the PATCH on the milestone's description — and its `evidence:sha` check
asks only whether each sha is an ancestor of `origin/main`, which a snapshot months out
of date still is. Nothing mechanical catches a stale date or SHA in this file, so
refreshing it, and moving `#131` from `## Left out` into a row, is a hand step before
the close. Exit criterion, from the milestone's own description and not yet met: the Claude route
runs milestones end to end without a person except at `human:pending` gates, and the
mechanical gates that catch round-1 defects run before the pull request.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #132 | feat(orchestrate): continuous loop over open milestones with a closed list of stop reasons | #140 | d0fca141cb00f608ecdc2d4e4c579024ffc1ebd1 |
| #133 | feat(init): --rules creates or updates the ruleset with the required checks | #139 | fc34f550785df44322d1e59208e14af6c5e8aeb5 |
| #134 | feat(ci): scope fails files that are new or grew past 800 lines | #138 | f252a02d751ee0a6a6a95b165c52dfd600faca01 |

## Left out

- #131 `spec: continuous orchestration on the Claude route and gates before the PR
  (M10)` — the phase's parent spec issue, still open. It is why this file is
  provisional: the phase's own specification has not been closed out.
- What #133 shipped did not hold: `init --rules` created a second ruleset instead of
  updating the existing one, which M11 had to fix (#143, PR #186). The row above is
  what merged in this phase, not a statement that the behaviour is still current.

## Dogfood

- [`docs/dogfood/2026-09-10.md`](../dogfood/2026-09-10.md) — the retroactive record
  of issue #129, which it closes. It folds the runs of 2026-09-11 and 2026-09-12 into
  one report, and three of its cases are this phase's own work: "parallel headless
  dispatch of M10 pass 1 (2026-09-12)", which produced exactly PRs #138, #139 and
  #140 with nine green checks (23 turns, 37 minutes, about USD 22, verdicts 1
  approved and 2 rejected on mechanical defects); "headless round 2 of M10 pass 2,
  and round 3 (2026-09-12)", where round 2 ran headless for #140 and #138 but round 3
  needed a person; and the #133 implementer leaving a public disposable repository
  behind. Its findings land as #259, #260, #264, #265, #266 and #267.
- One claim in the milestone's description still does not check out. It says "Wave 1
  of the 2026-09-12 report measured against lohra-ts", but the report's own
  `Repository:` line is `marcelusfernandes/agentic-setup` and no case in it names
  lohra-ts. What the 2026-09-12 wave measured, by the only written record of it, is
  this repository.
- The rule that a phase touching `hooks/`, `ci/`, `scripts/` or a
  `skills/**/SKILL.md` must name a report here landed in M16 on 2026-09-17 (#182,
  PR #246), after everything in the table merged. This phase changed `ci/` (#134),
  `scripts/` (#133) and `hooks/` (#132), and the report named in the first bullet is
  what satisfies that rule — a pass that ran against this phase's three pull requests
  while they were open, not a citation of convenience.
