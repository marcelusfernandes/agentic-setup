# Closeout M14 — Closure with evidence

- Closed (UTC): 2026-09-18
- main SHA: a0b4771dead04fe5926c36bca230bdbe13dbc210

Exit criteria, read from the milestone's own description on the day above, two of
three checked. Checked: `scripts/close-milestone.mts` is the script that closes a
milestone and refuses — never closes — on open issues, on an `Exit criteria:`
checklist that is absent or unchecked, and on a closeout that is absent from `main`,
misnamed, unparsable or lists a SHA that is not an ancestor of `main` (#172, PR #199);
and the provenance pin proves every SHA under `docs/closeout/` is an ancestor of
`main` (#170, PR #192). Unchecked: `docs/closeout/M14.md` on `main`, filled in, and
naming the merge commit of every issue of this milestone. This file is that criterion,
and it is met by the pull request that adds it, not before.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #141 | docs: README quick start for Claude Code and Codex as the main distribution channel | #225 | bf67ab95a42e796d90c70758023ac4c59d7a913a |
| #170 | feat(tests): milestone closeout format proved by a provenance pin test | #192 | 9be1b1bd2bd7adf45239673ef01fd1ba0a7ecfb0 |
| #171 | feat(scripts): one milestone description format, reported by reconcile | #190 | f264f87d7a452bf6f50e9d93dbea30c17dd1472b |
| #172 | feat(scripts): close a milestone through a script that refuses without evidence | #199 | 62e3206fc2b9c4b14b160355195aaff0d070a24d |
| #173 | feat(scripts): create and link a sub-issue through a script | #197 | d326822191cf916d42f7fab31195090772aa53d5 |
| #174 | docs(docs): retroactive closeout for M1-M13 and a README that points at it | #271 | 282cedbe7eb17175c45916cdb1e7e9fce8a2998e |

## Left out

- #169 `spec: closure with evidence — milestone closeout, provenance, and the last
  prose steps become scripts (M14)` — the phase's parent spec issue, still open. It
  ships no pull request of its own, so it belongs here and not in a row.
- #315 (`docs(docs): a current closeout for every open phase, and none left
  provisional`) — the issue this file closes. Its merge commit cannot be written into
  the file that carries it, so it is accounted for here rather than as a row; the
  pull request that lands this file is its evidence.

## Dogfood

- [`docs/dogfood/2026-09-17.md`](../dogfood/2026-09-17.md) — the pass that ran this
  phase's own mechanisms against sixty-one landings in a day, and found the defect
  this closeout corrects. Its scoreboard row "the retroactive closeout of M1 to M13"
  is #174 (PR #271), exit 0 with error class `brief-false-premise`: the orchestrator's
  brief told the implementer that `close-milestone.mts` rewrites closeout files, which
  it never does, and the file was drafted while the run was still merging, so four
  issues that shipped during the drafting were written up as left out — #157, #158 and
  #159 in M12 and #168 in M13 — and both gates accepted it. Findings D22 and D27 are
  the same defect from two sides and became #298: a closeout drafted during an active
  run is stale before it is pushed, and the paragraph #174 wrote to say so also told
  the next reader to move a spec issue out of `## Left out` and into the evidence
  table, which the grammar in [`README.md`](README.md) forbids — an issue that closed
  without a pull request did not ship. That paragraph was deleted, not reworded, and
  its instruction was not carried out. The report of PR #224 adds that the pin's
  empty-template message had drifted from the script's own and its per-row `gh` reads
  are unbatched, also #298.
- The same pass reviewed every mechanism in the table above and left each of the other
  five rows with a successor issue: #171 (PR #190) is the scoreboard's whole
  `crash-policy-drift` row — `reconcile.mts` added a third soft exception without
  saying so in its own header, which only the isolated review reads — and its review
  produced #205 and #206; #172 (PR #199) produced #227, close-milestone reading one
  page of the milestone's issues and crashing where it should refuse; #173 (PR #197)
  produced #226; #170 (PR #192) produced #208 and #209, the second of which is the
  rule that a closeout lands before the close; and #141 (PR #225) produced two of
  #304's clauses, the init card naming two of three workflows and the contract files
  still opening Codex-first. Two of the six took a send-back — PR #190 and PR #271,
  each with a scoreboard row of its own — and the other four merged on one round.
