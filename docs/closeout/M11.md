# Closeout M11 — Real review gate and baseline fixes

- Closed (UTC): 2026-09-17
- main SHA: 668cba7845fc6845333a215949a26825e5e92f06

**Provisional. M11 has not closed.** One issue is still open, `#142`, the phase's own
spec; everything else in the milestone has shipped or is accounted for below. The date
above is the day this snapshot was taken (#174) and the SHA is the tip of `main` at
that moment, not a close. The grammar in [`README.md`](README.md) has one date field
and one SHA field and no way to say "still running", so this paragraph says it instead.
**A person has to replace both before the close.** `scripts/close-milestone.mts` never
writes to a closeout file — its only write is the PATCH on the milestone's description
— and its `evidence:sha` check asks only whether each sha is an ancestor of
`origin/main`, which a snapshot months out of date still is. Nothing mechanical
catches a stale date or SHA in this file.

Exit criterion, quoted from the milestone's description and now met in full: `land`
declares the review mode in its output and refuses a head that changed after the review
or that no reviewed-SHA marker covers (#144, PR #216; the declaration half landed as
#156, PR #255, in M12), `init --rules` updates the existing ruleset instead of creating
a second one and keeps `required_approving_review_count` at 0 by default (#143,
PR #186), one versioned label dictionary feeds the installer (#145, PR #220), and
`docs/decisions.md` records the review mode with its accepted cost and the opt-in
`approved` mode (#148, PR #251).

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #122 | fix(ci): retry the commit-to-PR lookup in guard-main before opening a human:pending issue | #185 | 6ae66d2ca383852c83d32ab7c64d547560d2bd8a |
| #135 | feat(ci): strict negative control by path class, and type labels written by the orchestrator | #187 | 473dacafd3a64962c54b23aa8485a5d11cfa36b2 |
| #143 | fix(scripts): init --rules updates the ruleset governing the default branch and keeps review opt-in | #186 | 20f5f739502cf31fe3cf28826d1c86cb1b875497 |
| #144 | fix(scripts): land pins the merge to the commit the review approved | #216 | 8b1640268cc802dda86996d090a1a69400fcfb45 |
| #145 | feat(infra): one versioned label dictionary read by the installer and checked against the Codex route | #220 | ff910b7e898c88509ac1c093d8b8214881acf110 |
| #146 | docs(docs): the contract stops contradicting the code | #241 | dcb91370cc4d67a4105d0c658612c7db7e558992 |
| #148 | docs(docs): record the review mode — isolated agent review, label applied by the orchestrator | #251 | 0a049ea5f25f96720e8243888c8df57a37813ab1 |
| #150 | chore(infra): decide what happens to the M9 discipline agent catalogue | #254 | 5774c7ddb81515933824b4715bf5144e2e05de59 |
| #152 | chore(infra): the agent catalogue index cannot name a scope label nothing seeds | #272 | 89f3fbafaff7b81fffb483f394289d61cba3d2cb |

## Left out

- #142 `spec: real review gate and baseline fixes (M11)` — the phase's parent spec
  issue, still open. It is the only thing keeping this milestone open.
- #149 and #151 — closed `not_planned` as duplicates of #150 and #152, each created
  by a concurrent run of the planning script, each carrying the same title as its
  twin and each saying so in its closing comment. They shipped nothing.
- #147 `chore(infra): turn the review gate on and create the reviewer identity for
  this repository` — closed `not_planned` on 2026-09-17 with the same comment,
  "Duplicate of #148". It is the one of the three that is not a title-for-title
  duplicate: no other issue in this repository asks for the reviewer identity or for
  the gate to be switched on, and #148 (PR #251) recorded the review mode in
  `docs/decisions.md` without touching a GitHub setting. So the work #147 named — the
  identity and the setting on this repository — is not in any row above and is
  covered by no open issue either. It is written down here rather than lost.

## Dogfood

- No report covers this phase. [`docs/dogfood/`](../dogfood/) holds two, written
  retroactively from issues #96 and #129 and closing them: the pass of 2026-09-06
  against a third-party repository, and the pass of 2026-09-10 to 2026-09-12 against
  this one. Every row above merged on 2026-09-17, a week later, so neither is
  evidence for any of them. Their dated filenames are deliberately not written out
  in this section: `close-milestone.mts` matches the literal
  `docs/dogfood/<date>.md` shape anywhere in a bullet here, and a citation would
  buy this phase a close it has not earned.
- The rule that a phase touching `hooks/`, `ci/`, `scripts/` or a
  `skills/**/SKILL.md` must name a `docs/dogfood/<date>.md` here landed in M16 on
  2026-09-17 (#182, PR #246), after every row above merged, and its format the same
  day (#183, PR #253). This phase changed `ci/` and `scripts/`, so
  `scripts/close-milestone.mts` will refuse its close with `missing: ['dogfood']`
  until a pass is run against this phase's work and named in this section. Stating
  that is the honest version of this section; pointing at a report that ran a week
  before the work would buy the close and lose the point.
