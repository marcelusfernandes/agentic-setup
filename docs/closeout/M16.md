# Closeout M16 — Dogfood by substance and executable proof

- Closed (UTC): 2026-09-18
- main SHA: a0b4771dead04fe5926c36bca230bdbe13dbc210

Exit criteria, read from the milestone's own description on the day above, three of
four checked. Checked: #96 and #129 are closed with a count and every finding they
held is an issue with an `Origin:` line or a written reason not to be one (#184,
PR #270); proof declarations are read by the negative control (#136, PR #217); and
the pre-PR gate blocks a red stop with a cap of three (#137, PR #235). Unchecked:
the closeout of M16 names the dogfood report of the phase. That report is #278
(PR #307), a row of the table below, and the `## Dogfood` section of this file is
the criterion being met — for this phase alone the report and the phase that
produced it are the same thing.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #136 | feat(ci): optional proof declaration per branch slug consumed by the negative control | #217 | bacf304635d82ca24f21a6dc8e8eab696bc7ea01 |
| #137 | feat(hooks): SubagentStop gate runs the detected checks before an implementer stops | #235 | fb60b101540721a3e1b67f34b022199018ef739d |
| #182 | feat(ci): a change to the mechanism asks for a dogfood report | #246 | 6326de8290c208fc6a2393b349211e2e0be1c96b |
| #183 | feat(docs): dogfood report format with a scoreboard per case and an Origin line on every finding | #253 | 0ebbf70826ecaf9a9b9818e4d3c837c529ecf71a |
| #184 | docs(docs): decompose the two dogfood specs into issues that carry an Origin line | #270 | 243aab907d1d1690370756ee35b4c9c6370b35c0 |
| #256 | fix(ci): detect a Python test tree that carries no packaging marker | #275 | bde02c89c20e1a571ef383b418c2ee79f65397c8 |
| #257 | fix(ci): the negative control names the entry point that made it vacuous | #273 | b046af57f7ee69ff169897e076efa6e35d6cf8b5 |
| #258 | fix(ci): issue-lint sequences a transitive Blocked by chain and quotes the forms it accepts | #274 | 668cba7845fc6845333a215949a26825e5e92f06 |
| #259 | feat(scripts): reconcile reports the issues that carry no milestone | #282 | 05655b1c0d938c1f7a5cccb8c2efbcc9cf89f13e |
| #260 | feat(scripts): land --wait returns only once the pull request is merged | #288 | 4acb6612b4933ed7749243a568d3ac5ff48e7e2c |
| #261 | feat(scripts): init reports a default branch that is not main or master | #283 | 124eb667033b0c38c15d44e6d824f8d35fd3e462 |
| #262 | feat(scripts): sync-codex-plugin bumps the version it publishes | #281 | 0fbfc7a4f1e366c889d656b8d522dc7bc224a034 |
| #263 | docs(docs): the init card names the bootstrap pull request's two expected reds | #279 | fb60643b01022a47ae48750bf56388c0e70d0184 |
| #264 | docs(agents): the implementer and reviewer cards state what the two dogfood passes cost | #280 | 3778dee76f0b996b9fa2da0baaa44c93d710e7d3 |
| #265 | docs(docs): the orchestrate card names the relabel order, the index lag and the post-merge pull | #287 | 1ca6b85a17a8fd72c7bc87049f11022fb524508d |
| #266 | docs(docs): orchestration records the cross-repository limit and where agent memory lives | #325 | 353c9d3e1819649073c34e22a8a0cc5ac41c63c7 |
| #267 | docs(docs): the issue, workflow and dogfood cards state four rules the passes needed | #286 | 55ecf9aadb12d9527f6fd7e4544fb1c9cc8db361 |
| #277 | fix(ci): a marker-less Python tree gets a command that collects its tests | #284 | 58e53d9223896c7440bd3006f295792edbda6088 |
| #278 | docs(docs): the dogfood report of the 2026-09-17 pass | #307 | a0b4771dead04fe5926c36bca230bdbe13dbc210 |

## Left out

- #181 `spec: dogfood by substance, findings that become issues, and an executable
  Proof (M16)` — the phase's parent spec issue, still open. It ships no pull request
  of its own.
- #289 (no document tells a person to poll for a merge `land --wait` waits for) —
  still open. It is the documentation half of #260, which shipped the waiting itself.
- #332 (the dogfood report's finding descriptions are swept, not sampled) — still
  open, and raised against #278's own report after it merged. It is written down here
  because the phase's last deliverable is the thing it audits.

## Dogfood

- [`docs/dogfood/2026-09-17.md`](../dogfood/2026-09-17.md) — this phase's own report,
  shipped as #278 (PR #307), covering 2026-09-17T02:09:03Z to 18:03:07Z: sixty-one
  pull requests through claim, isolated review and land, 775 orchestrator turns, 954
  wall-clock minutes and a reconstructed USD 742.59, with fifteen scoreboard rows and
  a hundred and two findings. What it found about the phase that produced it, mechanism
  by mechanism. The SubagentStop gate of #137 (PR #235) has a scoreboard row of its
  own, error class `gate-silent-no-op`, and it is the pass's sharpest result: three
  headless runs against a disposable repository showed the gate blocking three stops
  when the session cwd or an isolated agent worktree was the repository, and doing
  nothing at all when the subagent merely changed directory into a worktree — which is
  the pattern the orchestrator brief used all day, so the gate was a no-op for the
  entire run it was built for (runtime proof P4 with finding D17, now #301). The proof
  declaration of #136 (PR #217) is in the `head-changed-after-approval` row with #264
  (PR #280): five pull requests took a merge from `main` after approval and needed a
  fresh review. The report format of #183 (PR #253) is in the `hygiene` row with #259
  (PR #282) — a raw NUL byte turned a test file into a binary diff and a test file
  crossed the 800-line cap — and its own review found the report regex unanchored, so
  a nested path or a backup file counts as a report and silences the nudge (#295).
  #257 (PR #273) and #263 (PR #279) are the `vacuous-pin` row: a card slice bounded on
  a blank line ran to the end of the file, proved by mutation, and a message claiming
  the entry point is never overlaid was disproved by probe (#296 and #297). The
  dogfood nudge of #182 (PR #246) is in the `docs-not-equal-code` row, sent back once
  for a rule stated outside its `## Files`.
- Two of this phase's issues were still in flight when that window closed and are
  recorded honestly as such: #265 (PR #287) is the `unreproduced-claim` row, whose
  review reproduced the command on a real clone four minutes after the cutoff and
  found that the form the card printed as failing exits 0 — a 2026-09-10 finding taken
  at its word and taught as a cause that does not exist (D24, now #295) — and #260
  (PR #288) is the `in-review-at-cutoff` row, cost recorded and no outcome. Both
  merged on 2026-09-18, as did #266 (PR #325) and the report itself (PR #307), which
  the window does not reach at all. The report's own last finding, from the review of
  PR #307, is that the format never says whether a finding row may cite evidence from
  after the window it measures (#295); and finding D29 uses PR #307 as its clearest
  instance of the identity collapse — the implementer opened it carrying
  `state:in-review` alone and the orchestrator added `type:docs` five minutes later,
  with the timeline attributing both to one login.
