# Closeout M13 — Automatic adoption

- Closed (UTC): 2026-09-18
- main SHA: a0b4771dead04fe5926c36bca230bdbe13dbc210

Exit criteria, read from the milestone's own description on the day above, one of two
checked. Checked: `doctor` prints `ok: false` naming the exact missing field on a
repository whose review gate is off (#168, PR #292, merged 2026-09-18 and the last row
of the table below). Unchecked: `adopt` takes an untouched disposable repository from
inventory to a merged, self-verifying adoption pull request with no hand-written step.
Every step of that path shipped — the inventory (#162), the record (#163), the proof
runner (#164), the generated checks (#165), the hooks (#166) and the adoption pull
request itself (#167, PR #268) — but the criterion asks for one run end to end on a
real repository, and the 2026-09-17 pass found the adoption pull request's own `scope`
and `negative-control` jobs cannot run, which is #302 and merged as PR #329 on
2026-09-18. The criterion is about a run, not about the issues, and no such run is
recorded.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #161 | chore(infra): decide whether an adoption record may exist beside detection | #195 | b11abb4e0464d88943cdaaedf321b3959f5ea8e7 |
| #162 | feat(scripts): adopt reports what a repository has and asks before writing anything | #193 | af4c96caf9c5c970148ab7127b721d26c19ab33e |
| #163 | feat(scripts): adopt writes the adoption record every later step reads | #219 | 9a03887ef2d5e13ddd903f5c8dc152070cb88b5c |
| #164 | feat(scripts): the proof runner executes the proof a slug declares | #236 | c93c1b0fdf90cc541d918fb08e82b711ceffb692 |
| #165 | feat(scripts): adopt generates the required checks from the adoption record | #248 | edcef63befcf17375ee8df4a1e7acf6087bf5472 |
| #166 | feat(scripts): adopt installs the hooks the adoption record names | #252 | eea6d53521c4dbbe876732039c2c88f5648f6bda |
| #167 | feat(scripts): adopt opens an adoption pull request that proves itself | #268 | 6b27bf4460d299197de882539714b9285cb5e899 |
| #168 | feat(scripts): doctor refuses to say ok when the gate it reads is off | #292 | c1e6b413d5caf4a37521b31c080bf44cb6eacf3b |

## Left out

- #160 `spec: automatic adoption and a doctor that reads the real configuration
  (M13)` — the phase's parent spec issue, still open. It ships no pull request of its
  own, so it belongs here and not in a row.
- Two of the eight rows were corrected inside M17 before this phase closed: #215
  (PR #243) hardens what #162 shipped in PR #193, and #233 (PR #249) reworks the
  inventory across PR #193 and PR #219 (#162 and #163). The table records what this
  phase merged, not that each row landed right the first time — and #326, still open,
  records that #233 itself closed with half a criterion met, `PROOF_DIR` still
  exported.

## Dogfood

- [`docs/dogfood/2026-09-17.md`](../dogfood/2026-09-17.md) — the pass that finally ran
  against this phase, and the gap named at the end of this section is the one it
  closes only in part. Seven of the eight rows merged inside the window it measures.
  #162 (PR #193) is the whole `sent back twice, landed on round three` scoreboard row:
  `adopt`'s inventory read `.git/hooks` and ignored `core.hooksPath` in round one, and
  round two found a second defect, so three rounds on one issue each found a different
  class — the report's clearest case of a review finding what no check reaches. #163
  (PR #219) and #167 (PR #268) are in the `behaviour-defect` row, where a gap list
  printed from the wrong object and a human gate reachable around through a documented
  sequence were found by the isolated review alone. #161 (PR #195) is in the row that
  landed with no isolated review at all, and it is also where runtime proof P1 comes
  from: `guard-main`'s retry loop ran live on `main` after that merge and answered on
  its first attempt, so the loop is proved live and its retry branch is not. #164
  (PR #236) is in the `docs-not-equal-code` row, sent back once for a rule stated
  outside its `## Files`; #165 (PR #248) and #166 (PR #252) merged on one round.
- What the pass found about `adopt` as a mechanism is the reason the first exit
  criterion above is still unchecked. The reports and reviews of PR #268, PR #248,
  PR #252 and PR #219 together found that the adoption pull request's own `scope` and
  `negative-control` jobs cannot run and its plan-issue selection can pick a closed
  decided issue; that `adopt` leaves a temporary index directory behind on one refusal
  path, can leave a partial hook install, drops a non-string deny entry silently and
  documents a hand edit invariant 4 forbids; that the adoption record's generated
  fields are interpolated unescaped into the plan issue with `record:stale` missing
  from the gap names; and that `init --rules` computes the required check names
  separately from the workflow `adopt` generates. All of that is #302, merged as
  PR #329 on 2026-09-18. The same reports noted that `adopt.mts` and `docs/adopt.md`
  were both within ten lines of the 800-line cap, so the next mode must extract first.
- #168 (PR #292) is the one row the window does not cover as a landing, and the report
  records why by name: its scoreboard row `an implementer ended mid-task on a weekly
  account limit` is this issue. The round was ended at 18:10:42Z with three commits
  unpushed in the worktree of #168 while the issue read `state:in-progress` against an
  empty lock branch — which is exactly how an implementer that had done nothing reads
  from outside, and is why nothing could tell finished work from lost work (#291,
  still open). The work resumed on 2026-09-18 after the limit reset and merged that
  day, outside the window this report counts.
- The gap this phase still has is worth naming plainly, because the report does not
  close it: a phase whose whole subject is adopting a real repository has no recorded
  run against one. The 2026-09-17 pass ran against this repository, not against an
  adoptee; its one disposable repository was the SubagentStop experiment's, which
  belongs to M16. The first exit criterion asks for that run, and it has not happened.
