# Closeout M13 — Automatic adoption

- Closed (UTC): 2026-09-17
- main SHA: 668cba7845fc6845333a215949a26825e5e92f06

**Provisional. M13 has not closed.** Two issues are still open, `#160` and `#168`, so
the date above is the day this snapshot was taken (#174) and the SHA is the tip of
`main` at that moment, not a close. The grammar in [`README.md`](README.md) has one
date field and one SHA field and no way to say "still running", so this paragraph says
it instead. **A person has to replace both before the close.**
`scripts/close-milestone.mts` never writes to a closeout file — its only write is the
PATCH on the milestone's description — and its `evidence:sha` check asks only whether
each sha is an ancestor of `origin/main`, which a snapshot months out of date still is.
Nothing mechanical catches a stale date or SHA in this file.

Exit criterion, quoted from the milestone's description and half met: `adopt` takes an
untouched disposable repository from inventory to a merged, self-verifying adoption
pull request with no hand-written step (#167, PR #268, in the table below), and
`doctor` prints `ok: false` naming the exact missing field on a repository whose review
gate is off (#168, still open). The second half is what keeps this phase running.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #161 | chore(infra): decide whether an adoption record may exist beside detection | #195 | b11abb4e0464d88943cdaaedf321b3959f5ea8e7 |
| #162 | feat(scripts): adopt reports what a repository has and asks before writing anything | #193 | af4c96caf9c5c970148ab7127b721d26c19ab33e |
| #163 | feat(scripts): adopt writes the adoption record every later step reads | #219 | 9a03887ef2d5e13ddd903f5c8dc152070cb88b5c |
| #164 | feat(scripts): the proof runner executes the proof a slug declares | #236 | c93c1b0fdf90cc541d918fb08e82b711ceffb692 |
| #165 | feat(scripts): adopt generates the required checks from the adoption record | #248 | edcef63befcf17375ee8df4a1e7acf6087bf5472 |
| #166 | feat(scripts): adopt installs the hooks the adoption record names | #252 | eea6d53521c4dbbe876732039c2c88f5648f6bda |
| #167 | feat(scripts): adopt opens an adoption pull request that proves itself | #268 | 6b27bf4460d299197de882539714b9285cb5e899 |

## Left out

- #160 `spec: automatic adoption and a doctor that reads the real configuration
  (M13)` — the phase's parent spec issue, still open.
- #168 (`doctor` refuses to say ok when the gate it reads is off) — still open. It is
  the half of the exit criterion this phase has not reached.
- Two of the seven rows were corrected inside M17 before this phase closed: #215
  (PR #243) hardens what #162 shipped in PR #193, and #233 (PR #249) reworks the
  inventory across PR #193 and PR #219 (#162 and #163). The table records what this
  phase merged, not that each row landed right the first time.

## Dogfood

- No report covers this phase. [`docs/dogfood/`](../dogfood/) holds two, written
  retroactively from issues #96 and #129 and closing them: the pass of 2026-09-06
  against a third-party repository, and the pass of 2026-09-10 to 2026-09-12 against
  this one. Neither touches `adopt`, which did not exist when either was run.
- The rule that a phase touching `hooks/`, `ci/`, `scripts/` or a
  `skills/**/SKILL.md` must name a `docs/dogfood/<date>.md` here landed in M16 on
  2026-09-17 (#182, PR #246). Six of the seven rows changed `scripts/` — every one but
  #161, which is a decision — so `scripts/close-milestone.mts` will refuse this
  phase's close with `missing: ['dogfood']` until a pass is run against them and
  named here. No dated `docs/dogfood/<date>.md` filename is written in this section,
  because the check matches that shape anywhere in a bullet and citing an older report
  would silence a refusal this phase has earned. The gap is worth naming plainly: a
  phase whose whole subject is adopting a real repository has no recorded run against
  one.
