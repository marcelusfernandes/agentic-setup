# Closeout M13 — Automatic adoption

- Closed (UTC): 2026-09-17
- main SHA: 0ebbf70826ecaf9a9b9818e4d3c837c529ecf71a

**Provisional. M13 has not closed.** The milestone is still open on GitHub with
`#160`, `#167` and `#168` open, so the date above is the day this snapshot was taken
(#174) and the SHA is the tip of `main` at that moment, not a close. The grammar in
[`README.md`](README.md) has one date field and one SHA field and no way to say
"still running", so this paragraph says it instead; both values are replaced when the
phase closes through `scripts/close-milestone.mts`. Exit criterion, quoted from the
milestone's description and **not met**: `adopt` takes an untouched disposable
repository from inventory to a merged, self-verifying adoption pull request with no
hand-written step (#167, still open), and `doctor` prints `ok: false` naming the exact
missing field on a repository whose review gate is off (#168, still open). Everything
below is the machinery those two last steps would use.

## Issues

| issue | title | PR | merge commit |
| --- | --- | --- | --- |
| #161 | chore(infra): decide whether an adoption record may exist beside detection | #195 | b11abb4e0464d88943cdaaedf321b3959f5ea8e7 |
| #162 | feat(scripts): adopt reports what a repository has and asks before writing anything | #193 | af4c96caf9c5c970148ab7127b721d26c19ab33e |
| #163 | feat(scripts): adopt writes the adoption record every later step reads | #219 | 9a03887ef2d5e13ddd903f5c8dc152070cb88b5c |
| #164 | feat(scripts): the proof runner executes the proof a slug declares | #236 | c93c1b0fdf90cc541d918fb08e82b711ceffb692 |
| #165 | feat(scripts): adopt generates the required checks from the adoption record | #248 | edcef63befcf17375ee8df4a1e7acf6087bf5472 |
| #166 | feat(scripts): adopt installs the hooks the adoption record names | #252 | eea6d53521c4dbbe876732039c2c88f5648f6bda |

## Left out

- #160 `spec: automatic adoption and a doctor that reads the real configuration
  (M13)` — the phase's parent spec issue, still open.
- #167 (`adopt` opens an adoption pull request that proves itself) — still open. It
  is the first half of the exit criterion and the step that would make the six rows
  above add up to an adoption anyone can run.
- #168 (`doctor` refuses to say ok when the gate it reads is off) — still open, the
  second half.
- Two of the six rows were corrected inside M17 before this phase closed: #215
  (PR #243) hardens what #162 shipped in PR #193, and #233 (PR #249) reworks the
  inventory across PR #193 and PR #219 (#162 and #163). The table records what this
  phase merged, not that each row landed right the first time.

## Dogfood

- No report exists. `docs/dogfood/` holds only `README.md` and `TEMPLATE.md`. The
  only written passes this repository has are issues #96 (2026-09-06) and #129
  (2026-09-10/11); neither touches `adopt`, which did not exist when they were
  written.
- The rule that a phase touching `hooks/`, `ci/`, `scripts/` or a
  `skills/**/SKILL.md` must name a `docs/dogfood/<date>.md` here landed in M16 on
  2026-09-17 (#182, PR #246). Five of the six rows changed `scripts/` — every one but
  #161, which is a decision — so `scripts/close-milestone.mts` will refuse this phase's close
  with `missing: ['dogfood']` until a real report is written and named in this
  section. The gap is worth naming plainly: a phase whose whole subject is adopting
  a real repository has no recorded run against one.
