[project]
name = "python-uv-fixture"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8.0",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
