{
  "name": "agentic-setup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous planning, specification and implementation loops for Codex, with durable human checkpoints. Node built-ins only; no build step. The legacy Claude plugin remains available as a separate opt-in route.",
  "scripts": {
    "test": "node tests/run.mts",
    "setup:codex": "node scripts/setup-codex.mts",
    "loop": "node .agents/skills/autonomous-loop/scripts/run.mts",
    "check": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22.18"
  },
  "license": "MIT"
}
